# Continue Here — 2026-09-07 새벽 세션 종료 (D1 쿼터 사건 대응 완료)

## Current Position
D1 쿼터 소진 사건(9/2~9/6 아침 이메일 미발송) 원인 분석 + 코드 수정 전부 완료.
DB 직접 조작(purge/인덱스/검증)만 쿼터 리셋(07:00 로컬 = 00:00 UTC) 후 남음.

## Completed This Session
1. **원인 확정**: D1 free tier 500만 rows/일 소진 → 06:00 파이프라인 0건 → 브리핑/이메일 스킵 연쇄
2. threadsp `PENDING_TTL` 15s→65s (commit 28d13f3, 배포 a5f1718e) — HITL 폴링 캐시 미스 해소
3. news_collector pub_date ISO 정규화 + 재시도 기능(FAILURE_MARKERS) + d1_query 로깅 (commit 0e2cbc4)
4. news.astro 14일 윈도우 (commit 3aa360b, Pages 배포, 라이브 200 확인)
5. pub_date 백필 ~98% (2,989/3,039, 잔여 50건 무시)
6. **db_reader CASE 제거** (commit f46446e) — 77만→~1만 rows/일
7. **get_existing 7일 윈도우 + 브리핑 아카이브 LEFT JOIN 전환** (commit 4de92c1) — 53만→~2만 rows/일
8. GraphQL Analytics 감사로 전 소비원 특정 (background_search LIKE 99.5만/일 = 최대 항목 확인)

## Remaining Work — 내일(9/7) 오전 07:00 이후 순서대로

⚠️ **예약 스크립트(PID 94661) 사망 — 07:10 자동 실행 안 됨. 1번을 수동 실행해야 함**

1. 수동 실행: `/var/folders/6r/kjl8wkw53t1bnr1dypqtccj80000gn/T/opencode/d1_maintenance.sh`
   - 내용: aikeep24lite-db export 백업 → posts/articles 전체 DELETE (사용자 승인됨, 수동 발행 전용) → 백필 잔여 → 검증
   - 로그: `/tmp/d1_maintenance_20260907.log`
2. 백필/인덱스 확인:
   - `SELECT COUNT(*) FROM news WHERE pub_date NOT LIKE '____-__-__%' AND pub_date != ''` → ~250(기타형식)면 성공
   - `SELECT name FROM sqlite_master WHERE type='index' AND name='idx_news_pubdate'` → 없으면 `CREATE INDEX IF NOT EXISTS idx_news_pubdate ON news(pub_date)` (f46446e 커밋 메시지는 인덱스 언급하나 실제 생성 여부 미검증 — 쿼터 소진으로 확인 불가였음)
3. FK 확인 후 90일 purge (아카이브 보존):
   ```sql
   SELECT name FROM sqlite_master WHERE type='table' AND sql LIKE '%REFERENCES news%';
   DELETE FROM news WHERE pub_date < date('now','-90 days')
     AND id NOT IN (SELECT news_id FROM briefing_items);
   ```
4. background_search LIKE → Python 이관 (99.5만/일 최대 소비원, 미수정 상태)
   - `pipeline/threads/contrast/background_search.py` L39, L118 — db_reader 메모리 풀 재활용
5. threads 파이프라인 수동 1회 → rows_read 확인
6. D1 insights 재조사 (24h): 계정 전체 실측. 목표 <400만/일
7. CHANGES.md 갱신 + 커밋

## Decisions
- aikeep24lite-db posts/articles 전체 삭제 승인 (threadsp 수동 발행 전용화 — 이미 cron 없음/trigger 410)
- 90일 purge 시 `NOT IN (SELECT news_id FROM briefing_items)`로 아카이브 인용행 보존
- 프리티어 유지 판정 기준: 전체 최적화 후 ~200만/일 예상 (한도 60% 여유)
- threadsp 원격 5+ 커밋 미푸시 (28d13f3 포함) — push 필요 시 `git -C ~/projects2/threadsp push`

## Blockers
- D1 쿼터 소진 상태 — 07:00 로컬(UTC 자정) 리셋까지 모든 D1 execute 불가. Analytics API는 사용 가능

## Bugs & Issues Discovered
1. **9/2~9/5 아침 silent-0** (에러 없이 D1 조회 0건)
   - 증상: wrangler exit 0 + 빈 results, stderr에 에러 블록 없음
   - 원인: 미확정. 쿼터 초과 시 정상 에러 형식(exit 1 + code 7403)과 상이
   - 해결: d1_query warning 로깅 추가 (commit 0e2cbc4) — 재발 시 원인 포착 가능
   - 미해결: 재발 시 로그 즉시 캡처. 재발 없으면 종결
2. **예약 스크립트 사망** (이번 세션 발견)
   - 증상: PID 94661 소멸, /tmp/d1_maintenance_20260907.log 부재
   - 원인: nohup 슬립 프로세스가 세션 종료와 함께 사망한 것으로 추정
   - 해결: 1번 수동 실행으로 대체
   - 미해결: 없음 (수동 실행이 해결)

## Environment Notes
- wrangler 모든 명령 앞 `env -u CLOUDFLARE_API_TOKEN` 필수 (전역 규칙)
- D1 Analytics GraphQL은 환경변수 토큰 권한 부족("not authorized") — `wrangler auth token` OAuth 토큰 사용:
  `env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler auth token | tail -1`
- 쿼리 패턴 조회: `d1QueriesAdaptiveGroups` (dimensions.query 지원) — `d1AnalyticsAdaptiveGroups`엔 query dimension 없음
- 로컬 TZ +07, 쿼터 리셋 07:00 로컬. KST 로그 스탬프는 +09
- 임시 스크립트: `/var/folders/6r/kjl8wkw53t1bnr1dypqtccj80000gn/T/opencode/d1_maintenance.sh`, `backfill_pubdate.py`

## Resume Instructions
```
cd ~/Projects/aikorea24
cat 이 파일 → 1~7 순서대로. D1 작업은 07:00 로컬 이후만.
```

## Uncommitted Changes (의도적 미커밋)
- `scripts/briefing_dedup.json` — 6천줄 데이터 캐시, 커밋 안 함
- `tmp_test/*` — 테스트 잔재
- `scripts/scripts/**` — 중복 경로 유물(junk), 무시
- `functions/api/d1test.ts` — 스크래치 테스트 파일
- kicker7_writer.py / prompts.py / publish_kicker7_drafts.py — ⚠️ 이번 wip 커밋에 포함됨 (threads contrast 진행분)
