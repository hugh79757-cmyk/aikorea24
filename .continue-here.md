# Continue Here — aikorea24.kr Handoff

**Timestamp**: 2026-07-26 10:58 KST  
**Session**: Quick Tasks 실행 — 배포 조건 버그 수정 + 썸네일 placeholder 버그 수정 + 미배포 19건 배포 + 7/25 중복 썸네일 재생성 + pipeline import 방어 추가 + **블로그 description 문장 경계 자르기 수정**  
**Branch**: main (origin/main과 동기화됨)

---

## Current Position

| Item | Status |
|------|--------|
| **Active Phase** | Phase 28 완료 (28-01/02/03), Quick Tasks 3개 완료 |
| **Completed Plans** | Phase 28: 28-01, 28-02, 28-03 (3/4) — 28-04 폐기 |
| **Quick Tasks Done** | fix-blog-deploy-condition, fix-thumbnail-placeholder, fix-pipeline-import, **fix-blog-prompt-truncation** |
| **ROADMAP** | Phase 28 추가됨, Phase 22/23 대기 |
| **Phase Dir** | `.planning/phases/28-thumbnail-deepseek-fix/` (3개 플랜) |

---

## Completed This Session

### 1. Quick Task: fix-blog-deploy-condition (`.planning/quick/20260726-fix-blog-deploy-condition/`)
**파일**: `scripts/blog_draft_generator.py` (라인 602~609)

| 문제 | 수정 |
|------|------|
| 배포 조건 `if generated:` → 신규 생성된 글만 배포 | `if generated or untracked_blog_files:` → 미커밋 파일 감지 시에도 배포 |
| 저녁 실행 시 `generated=[]`로 배포 안 됨 | `git status --porcelain src/content/blog/`로 untracked 파일 감지 |

**효과**: 저녁 22:15 실행 때도 미배포 블로그 파일 있으면 자동 배포

---

### 2. Quick Task: fix-thumbnail-placeholder (`.planning/quick/20260726-fix-thumbnail-placeholder/`)
**파일**: `scripts/auto_thumbnail.py` (라인 280, 109) + `public/images/news-keyword-og.webp`

| 문제 | 원인 | 수정 |
|------|------|------|
| placeholder 12.7KB < 15KB 품질 기준 | 품질 검증 실패 → 재시도 → 동일 Pexels 이미지 재사용 | placeholder 45.6KB로 재생성 (quality=98) |
| DeepSeek 모델명 `deepseek-chat` 사용 | API 변경으로 400 에러 (`deepseek-v4-pro`/`deepseek-v4-flash`만 지원) | `deepseek-v4-pro`로 변경 |

**결과**: 
- placeholder 품질 검증 통과 (45.6KB, 800×800, WebP)
- 7/25 중복 썸네일 5개(002~006) 모두 고유 이미지로 재생성 완료 (MD5 모두 다름)

---

### 3. 미배포 19건 블로그 배포 완료 ✅
```bash
git add src/content/blog/2026-07-23-* src/content/blog/2026-07-24-* src/content/blog/2026-07-25-*
git commit -m "blog: 7/23~7/25 블로그 19건 추가"
npm run build
env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler pages deploy dist --project-name aikorea24 --branch main --commit-dirty=true
```

**배포된 포스트**:
- 7/23: 6건 (007~012)
- 7/24: 12건 (001~012)  
- 7/25: 6건 (001~006) — 001은 고유 썸네일, 002~006 중복 해소 후 재생성

---

### 4. 7/25 중복 썸네일 5개 재생성 (MD5 모두 고유해짐)
| Before (동일) | After (모두 고유) |
|---------------|------------------|
| 8c93b87916afb3bc1e227bdda928b569 (5개) | 002: 65b85948...<br>003: 690a565d...<br>004: ecd4aa25...<br>005: 982d1ec2...<br>006: 245f0c9f... |

---

### 5. Quick Task: fix-pipeline-import (`.planning/quick/20260726-fix-pipeline-import/`)
**파일**: `scripts/blog_draft_generator.py` (라인 1-20)

| 문제 | 수정 |
|------|------|
| launchd 실행 시 `ModuleNotFoundError: No module named 'pipeline'` | 스크립트 **최상단** (shebang 직후, 모든 import 전)에 `sys.path.insert` 추가 |
| 8:15 AM 실행 4회 모두 pipeline import 실패 | 방어적 경로 설정으로 venv/launchd 환경 모두에서 import 보장 |

**변경 전**:
```python
#!/usr/bin/env python3
"""
docstring...
"""
import os, re, json, glob, sys, time  # import 이후 sys.path 설정
```

**변경 후**:
```python
#!/usr/bin/env python3
"""
docstring...
"""
# launchd 환경 방어: 가장 먼저 sys.path 설정 (모든 import보다 앞)
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import os, re, json, glob, time  # sys 제외 (이미 import됨)
```

**검증**:
- `python3 -m py_compile scripts/blog_draft_generator.py` → OK
- `/Users/twinssn/Projects/aikorea24/.venv/bin/python3 -c "import pipeline.infra.env_loader"` → OK
- 수동 실행 테스트 → pipeline import 성공, 배포 단계까지 정상 진행

---

### 6. Quick Task: fix-blog-prompt-truncation (`.planning/quick/20260726-fix-blog-prompt-truncation/`)
**파일**: `scripts/blog_draft_generator.py` (`_save_file()` 함수 + `_truncate_at_sentence_boundary()` 헬퍼)

| 문제 | 수정 |
|------|------|
| frontmatter `description`이 250자 하드컷으로 문장 중간에서 잘림 | `_truncate_at_sentence_boundary()` 헬퍼 추가로 한국어 종결어미(`.`, `!`, `?`, `다`, `요`, `함`, `습니다`, `입니다`, `했습니다`, `임`, `음` 등) 기준 자름 |
| 예: "배경에는 디", "자율성과", "보고서를 인용하" | 예: "배경에는 디지털 기술에 대한 피로감이 자리잡고 있습니다.", "자율성과 안전성에 대한 우려가 제기됩니다.", "보고서를 인용하고 있습니다." |

**변경 내용**:
1. `_truncate_at_sentence_boundary(text, max_len)` 함수 추가 (라인 299-340)
2. `_save_file()`에서 description 추출 시 `[:250].strip()` → `_truncate_at_sentence_boundary(desc_raw, 250)` 변경 (라인 359)

**검증**:
```python
# Before: "디지털 시대의 역설... 배경에는 디" (mid-word)
# After:  "디지털 시대의 역설... 요구합니다." (ends with sentence)

# Test: 275/277 tests pass (2 pre-existing failures)
```

---

## Remaining Work

| Priority | Task | Status | Notes |
|----------|------|--------|-------|
| 🔴 High | Phase 22: 발송 시스템 활성화 | 미완료 | launchd plist + day 0 hook + 3개 강좌 오픈 |
| 🟡 Medium | Phase 23: SNS 자동화 (Instagram Carousel + Shorts) | Planned | 7개 플랜 대기 |
| 🟢 Low | Phase 28-04 모니터링/알림 | 폐기 | 사용자 판단: "모니터링해도 나아지지 않을 것" |

---

## Decisions (이번 세션 추가)

1. **배포 조건 확대**: `generated`만 체크 → `generated or untracked_blog_files`로 변경 (미커밋 파일도 배포)
2. **placeholder 품질 상향**: 15KB 미만 → 45.6KB (quality=98, 800×800) — 품질 검증 통과 보장
3. **DeepSeek 모델명**: `deepseek-chat` → `deepseek-v4-pro` (API 변경 반영)
4. **pipeline import 방어**: 스크립트 최상단에 `sys.path.insert` 추가 — launchd 환경에서 import 보장
5. **blog description 문장 경계 자르기**: 250자 하드컷 → 한국어 종결어미(`.`, `!`, `?`, `다`, `요`, `함`, `습니다`, `입니다`, `했습니다`, `임`, `음` 등) 기준 자름으로 문장 완결성 보장

---

## Bugs & Issues Discovered (이번 세션)

### 1. 썸네일 placeholder 15KB 미만으로 품질 검증 실패 → 중복 재사용
- **증상**: `news-keyword-og.webp` 12.7KB → `validate_thumbnail_quality` 실패 → 재시도 2회 → 동일 Pexels 이미지(ID 중복) 재사용 → 7/25 002~006 5개 동일 썸네일
- **원인**: placeholder 원본 이미지가 12.7KB(800×447)로 품질 기준(15KB, 800×800) 미달. Phase 28-03 품질 검증 도입 후 placeholder가 검증에서 걸러짐
- **해결책**: 
  1. placeholder를 800×800 WebP quality=98로 재생성 (45.6KB)
  2. DeepSeek 모델명 `deepseek-chat` → `deepseek-v4-pro` 수정
- **미해결 여부**: ✅ 해결됨 (재생성된 5개 모두 고유 MD5 확인)

### 2. 블로그 저녁 배포 미실행 (Quick Task로 해결)
- **증상**: 오전 08:15 생성 → `deep_dive_url` DB 저장 → 저녁 22:15 재실행 시 "이미 연결됨" 스킵 → `generated=[]` → 배포 안 함
- **원인**: `blog_draft_generator.py` 라인 602 `if generated:` 조건이 신규 생성만 체크
- **해결책**: `if generated or untracked_blog_files:`로 확대 + `git status --porcelain`로 미커밋 파일 감지
- **미해결 여부**: ✅ 해결됨 (다음 저녁 실행부터 적용)

---

## Environment Notes

| Item | Value |
|------|-------|
| **OS** | macOS (Darwin) |
| **Python** | 3.14.5 (kaggle-env) |
| **Node** | npm/wrangler v4.110.0 (OAuth profile) |
| **Deploy** | Cloudflare Pages (`wrangler pages deploy dist --project-name aikorea24 --branch main`) |
| **Auth** | wrangler OAuth profile `hugh79757` (CLOUDFLARE_API_TOKEN env var 해제 필요) |
| **Scheduler** | launchd (`kr.aikorea24.tools-collector`, `kr.aikorea24.blog-draft`) |
| **Key Paths** | `PROJECT_DIR=/Users/twinssn/Projects/aikorea24`, `PUBLIC_IMAGES_DIR=/public/images` |

---

## Resume Instructions

```bash
# 1. 프로젝트 디렉토리 이동
cd /Users/twinssn/Projects/aikorea24

# 2. 최신 상태 확인
git status --short
git log --oneline -3

# 3. Phase 22 발송 시스템 활성화 (다음 작업)
./scripts/install_course_sender.sh
# enroll.ts day 0 hook 추가
# 7day-infra / 7day-agent 등록 오픈
# E2E 테스트 (등록→발송→완강 3개 강좌)

# 4. Phase 23 SNS 자동화 착수
# .planning/phases/23-sns-automation/ 플랜 검토

# 5. 테스트 재실행
python3 -m pytest tests/ -v --tb=short
```

---

## Quick Status Check

```bash
# 사이트 헬스 체크
curl -s -o /dev/null -w "%{http_code}" https://aikorea24.kr
# → 200 OK

# 썸네일 중복 확인 (7/25 002~006 — 모두 고유해야 함)
for f in public/images/2026-07-25-00{2,3,4,5,6}-*/thumbnail.webp; do md5 -q "$f"; done | uniq -c
# → 1 each (5개 고유)

# placeholder 검증
python3 -c "
import sys; sys.path.insert(0, 'scripts')
from auto_thumbnail import validate_thumbnail_quality
print(validate_thumbnail_quality('public/images/news-keyword-og.webp'))
# → (True, 'OK')
"
```

---

## Next Session Recommended Order

1. **Phase 22: 발송 시스템 활성화** — `./scripts/install_course_sender.sh` + day 0 hook + 강좌 3개 오픈
2. **Phase 23: SNS 자동화** — Instagram Carousel + Shorts 파이프라인 (7개 플랜)
3. **테스트 환경 정리** — 크롤러 BeautifulSoup 설치, Writer 프롬프트 키워드 테스트 수정

---

**Last Commit**: `779b8b7 wip: save before pause — Quick Tasks 완료 + 썸네일 placeholder 수정 + 미배포 19건 배포`
