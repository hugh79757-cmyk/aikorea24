# .continue-here.md — Session Handoff

**Timestamp**: 2026-07-10 15:45 KST
**Last session**: 2026-07-10 13:30–15:45 (Community + Funnel 개선)

---

## Current Position

**Phase 17~18 작업**: Phase 17(MVP-1 등록흐름)은 이미 완료, Phase 18(MVP-2 커뮤니티 게이트)+MVP-3(퍼널개선)까지 확장되어 진행 중.

**실제 완료된 스코프** (ROADMAP 기준 Phase 17 + Phase 18 + α):
- ✅ Phase 17: D1 스키마 + 시드 데이터 + enroll API + 랜딩 페이지
- ✅ Phase 18 (MVP-2): 커뮤니티 visibility 게이트 + 어드민 역할 + 편집 UI + 마크다운 렌더링
- ✅ Phase 18~19 확장: 카테고리 분리(강의), 인피드 광고, 강의/일반 게이트 분기, enrollment 연동

---

## Completed This Session (2026-07-10)

### 1. 마크다운 렌더링
- `marked` 설치, `sanitizeHtml()` → `renderMarkdown()` 구현
- `prose-container` CSS (Tailwind typography 미사용, 순수 CSS)
- 작성자: 기존 `\n`→`<br>` → `marked`로 교체
- 배포/확인 완료 (post 14 `##` → `<h2>` 정상)

### 2. 어드민 역할 + 편집 UI
- D1 `users` 테이블: `role TEXT DEFAULT 'member'` 컬럼 추가 (ALTER TABLE)
- `twinssn@gmail.com` → `owner` 지정 (UPDATE)
- Google OAuth callback → 세션에 `role` 포함
- `isOwner()` 헬퍼 in `src/lib/auth.ts`
- API routes 3개: visibility toggle, update (category+content+title), delete (soft delete)
- `/community/[id]/edit.astro` — 마크다운 textarea, visibility select, category dropdown
- `[id].astro` — 수정/visibility토글/삭제 버튼 (어드민만)
- `index.astro` — visibility 배지 + 수정 링크 (어드민만)
- **트레일링슬래시+CSSF 이슈 해결**: 모든 fetch URL `.../` 종결 + `Content-Type: application/json` 필수

### 3. 커뮤니티 페이지 개선
- Posts 14-20 `category` → `'강의'` (D1 UPDATE)
- `index.astro`: `categoryColors` / `categoryLabels` 맵 → 강의=violet, 일반 members=🔒 잠금 아이콘
- `[id].astro`: `insertInfeedAds()` — 첫 `</p>` 뒤 + 마지막 `</p>` 앞에 AdSense in-feed 2개 삽입
- 강의 전용 게이트 분기: 비로그인(강좌신청+로그인) / 로그인+미등록(수강생전용+신청) / 로그인+등록(전체본문)
- 일반 members 게이트: 기존 Google 로그인 그대로 (enrollment 검증 안 함)
- `/courses/7day-starter/` 강좌 신청 CTA 링크 연결

### 4. Enrollment 게이트 (핵심 퍼널)
**방식 A**: `course_lessons.community_post_id`로 `course_slug` 역조회
**옵션 X**: 등록 즉시 전체 열람 (순차 잠금 없음)
**보안**: SSR frontmatter에서 D1 조회 → HTML에 본문 미포함 시 소스보기 차단

SSR 로직:
```
post.visibility === 'members'
  → post.category === '강의'
    → course_lessons WHERE community_post_id = id → course_slug
      → !currentUser → gate (강좌신청+로그인)
      → enrollments WHERE user_id AND course_slug
        → 없음 → gate (수강생전용+신청)
        → 있음 → 전체본문 ✅
    → (매핑 없음) → 기존 members 게이트
  → (일반 members) → 기존 Google 로그인 게이트
```

### 5. 배포
- `npm run build` + `npm run deploy` (Cloudflare Pages) — 3회 이상 배포
- 최종 URL: `https://aikorea24.kr`

---

## Remaining Work

### Phase 18~19 계획된 작업 중 미완료
- [ ] **강의 콘텐츠 보강**: Posts 14-20의 시드 콘텐츠를 실제 강의로 교체 (7일치)
- [ ] 이메일 자동 발송 (Phase 19 MVP-3): 기존 auto_email_sender.py와 강좌시스템 연동
  - Brevo 트랜잭셔널 API → `send-daily` Workers API → launchd cron
- [ ] 완강 분기 (Phase 20 MVP-4): 완강 태그, 드롭오프 추적, 다음 단계 CTA

### 추가 검토 필요
- [ ] **"수강생 전용" 게이트 검증**: 본인 브라우저에서 미등록/등록 상태로 강의 글 접근 테스트
- [ ] AdSense 인피드 광고 실제 노출 확인 (현재 시드 콘텐츠가 짧아 미노출, 콘텐츠 교체 시 자동 활성화)

---

## Decisions

| Decision | Rationale |
|----------|-----------|
| **방식 A** (course_lessons 역조회) | 기존 스키마 그대로 사용, 새 컬럼 불필요 |
| **옵션 X** (등록 즉시 전체 열람) | 순차 잠금은 이탈 유발, 이메일 시퀀스 안정화 후 별도 실험 |
| **category='강의'일 때만 enrollment 검증** | 일반 members 글은 기존과 동일, 의도치 않은 잠금 방지 |
| **SSR 서버사이드 분기** | 클라이언트 소스보기로 본문 노출 차단 |
| **Layout.astro 상하단 광고 유지** | 이미 모든 페이지에 적용, 별도 작업 불필요 |

---

## Blockers

None currently.

---

## Resume Instructions

```bash
# 1. 현재 상태 확인
git status --short
cat CHANGES.md | tail -30

# 2. 강의 콘텐츠 교체 (다음 세션)
# Posts 14-20의 content를 실제 7일 강의로 교체
# edit.astro → 각 post 수정

# 3. 로컬 개발
npm run dev

# 4. 배포
npm run build && npm run deploy
```

---

## Uncommitted Changes

```
Modified:
  src/lib/auth.ts                    — isOwner() helper 추가
  src/pages/api/auth/callback/google.ts  — role을 세션에 포함
  src/pages/community/[id].astro     — 마크다운 렌더링 + 인피드 광고 + 게이트 3분기 + enrollment 검증
  src/pages/community/index.astro    — 카테고리 색상 + 강의 배지 + 잠금 아이콘
  src/styles/global.css              — prose-container 스타일
  package.json / package-lock.json   — marked 의존성 추가

New (untracked):
  src/pages/api/community/           — visibility, update, delete API routes
  src/pages/community/[id]/          — edit.astro 편집 페이지
  scripts/migrations/20260710_add_admin_role.sql  — role 컬럼 마이그레이션

Operational (gitignored):
  .omo/run-continuation/             — 세션 연속성 데이터
  scripts/threads/logs/launchd.log   — launchd 로그
  scripts/threads/posted.json        — 발행 추적
```
