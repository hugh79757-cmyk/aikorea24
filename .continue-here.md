# .continue-here.md — Session Handoff

## Current Position
- **Milestone**: v1.0
- **Phase**: 13 (card-separation-fix) — **COMPLETE** (committed `ebcbaac`)
- **Next**: Phase 14 — Run full E2E dry run to validate all fixes; then stabilize card output

## Completed This Session
### Phase 13 — 3 plans executed

**13-01: Persistent Failed Article Tracking**
- `scripts/threads/failed_articles.py` — load/save/is_failed/clear with retention
- `main_v3.py` — startup load, write failure save, exclude_ids passed to get_pitches
- ✅ Article 38290 infinite retry loop resolved

**13-02: Validation & Parsing Fixes**
- `validator.py`: `sentence_enders`에 `\u3002`(중국어 마침표) 추가
- `writer.py`: `_remove_duplicate_links()` 신규 + `parse_cards()` 마지막에 호출
- `main_v3.py`: ENG_LEAK_RE cleanup regex 개선 (MixCase + `은`/`는` 처리)

**13-03: Repair Logic + Model Routing + Delimiter**
- `writer.py`: `_repair_truncated_cards()` backward pass + MIN_COUNT guard
- `writer.py`: `humanize_cards()`/`fix_cards()` → `model_override='openai'` (44s→4s)
- `pitch.py`: 피치 생성 → `model_override='openai'` (JSON 모드 실패 회피)
- `writer.py`: 프롬프트 구분자 `---` → `\n\n\n` (카드/stanza 구분 명확화)
- `writer.py`: `parse_cards()`에 `\n\n\n` 우선 분할 로직
- `writer.py`: `assemble_final()`에 6개 미만 시 split 패딩 (안전장치)
- `validator.py`: `FORMAT_CARD_COUNT_TOLERANCE` D: (5,7) → (4,7)
- `validator.py`: Hook 문장 종결 과다 한도 5→10
- `tests/test_writer.py`: `TestRepairTruncatedCards` (6 tests)
- `tests/test_failed_articles.py`: persistent tracking tests (13 tests)

## Remaining Work
1. **Full E2E dry run**: Last dry run showed 4/5 attempts failing due to `\n\n` split inconsistency and Hook 문장 종결 과다. `\n\n\n` delimiter should improve this.
2. **Hook length/과다 issue**: 구조적 문제로 `\n\n` split이 너무 늦게 발생해 Hook에 여러 문장 포함. 검증 완화(10→15?) or `\n\n\n` delimiter가 실제로 해결하는지 확인.
3. **Stable 6-card output**: `\n\n\n` delimiter가 LLM 출력에 적용되는지 실제 검증 필요.

## Key Decisions
| Decision | Rationale |
|----------|-----------|
| `\n\n` 유지 | `---`가 프롬프트 누출 위험 |
| 카드 구분자 `\n\n\n` 도입 | stanza용 `\n\n`과 구분 명확화 |
| GPT-4o-mini로 통일 | MiMo 느림(16s) + JSON 모드 미지원 |
| Tolerance 4~7로 완화 | `\n\n` split 불안정 대응 |
| `_remove_duplicate_links()` 2중 방어 | parse_cards + assemble_final |

## Blocker
- E2E dry run이 오래 걸려 중단됨 (이전 세션)
- `\n\n\n` delimiter 적용 후 드라이런 미실행 → 효과 검증 필요

## Resume Instructions
```bash
cd ~/Projects/aikorea24

# 1. Run E2E dry run to validate \n\n\n delimiter
.venv/bin/python3 scripts/threads/main_v3.py --dry-run

# 2. Check results
tail -5 scripts/threads/logs/launchd.log
cat scripts/threads/logs/drafts/v3_2026-07-05-*.txt

# 3. If too slow, abort with Ctrl+C
```

## Uncommitted Changes
- `pipeline/posted.json` (runtime data)
- `scripts/threads/posted.json` (runtime data)
- `scripts/threads/logs/launchd.log` (runtime logs)
- `*.pyc` (cache, gitignored)

All source code committed and pushed (`ebcbaac` → `origin/main`).
