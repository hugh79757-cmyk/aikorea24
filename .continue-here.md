# Continue Here — 2026-07-23 세션

## Current Position

| Phase | Status |
|-------|--------|
| **Phase 27: 인프라 안정화** | ✅ 완료 (썸네일 + deploy retry + link retry) |
| **Phase 26-mobile-readability-followup** | ✅ 완료 (commit b9d0db7에서 이미 적용) |
| **Phase 22: 발송 시스템 활성화** | 🚧 Deferred (0/5 plans, 코드는 완료, launchd만 미설치) |
| **Phase 23: SNS 콘텐츠 자동화** | 📋 Planned (0/7 plans) |
| **Triage: korean-slug-404-debug** | 🔄 대기 중 (대표님 실제 404 URL 미제공) |

**Milestone**: `v2.0-complete` — 27 phases / 56 plans complete (100%)

---

## Completed This Session

### 1. Phase 27 — 인프라 안정화 (7 commits)
| 커밋 | 작업 | 파일 |
|------|------|------|
| `4cd7878` | 썸네일 frontmatter replace 패턴 수정 + 24개 글 backfill | `blog_draft_generator.py` |
| `adb5a86` | deploy.sh wrangler 3회 retry (for+if, set -e 유지) | `deploy.sh` |
| `4d8515d` | deep_dive_url 3회 retry (for...else + 2초 sleep) | `blog_draft_generator.py` |
| `54954e7` | description YAML 이스케이프 + 1개 파일 복구 | `blog_draft_generator.py` |
| `b456de0` | ROADMAP.md Phase 27 완료 표시 | `.planning/ROADMAP.md` |
| `fb83c60` | ROADMAP.md Phase 26-mobile 완료 표시 | `.planning/ROADMAP.md` |
| `0c41664` | `_add_image_to_frontmatter()` frontmatter-agnostic 개선 + 14개 글 복원 | `blog_draft_generator.py` |

### 2. 썸네일 전수 복구
- **24개 글 backfill**: `public/images/`에 썸네일 있으나 `image:` 필드 누락된 7/21~7/23 발행분 복구
- **15개 글 신규 생성**: Pexels API로 썸네일 생성 (7/2~7/12 + evergreen 6개)
- **전수검사 결과**: 썸네일 O + image: X = **0개**, 둘 다 없음 = **0개**

### 3. deploy 실배포 검증
- `env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler pages deploy`
- 1회 시도 성공 (불필요한 retry 없음)

### 4. Phase 26-mobile-readability-followup 확인
- 4개 task가 이미 commit `b9d0db7`에서 적용되어 있음 → ROADMAP.md에 완료 표시

---

## Remaining Work

### [Next] Phase 22: 발송 시스템 활성화 🚧 Deferred
- **상태**: 코드 완료, launchd plist만 미설치
- **남은 5개 작업**:
  1. `install_course_sender.sh` 실행 → launchd plist 설치
  2. `enroll.ts`에 day 0 등록 즉시 발송 hook 추가
  3. 7day-infra 등록 오픈 (분기 로직 연동)
  4. 7day-agent 등록 오픈 (완성 후)
  5. 전체 사이클 E2E 테스트

### [Later] Phase 23: SNS 콘텐츠 자동화 📋 Planned
- Instagram Carousel + Shorts/Reels 자동 생성/발행 파이프라인
- 7개 plan (17-01~17-07)

### [Still Blocked] Triage: korean-slug-404-debug 🔄
- 대표님 실제 404 URL 제공 시 Step 29-31 진행
- `run_pipeline.py` trailing slash 누락 vs `blog_draft_generator.py` 일관성 (수정 대기)

---

## Decisions

| 결정 | 내용 |
|------|------|
| retry 간격 | deploy 5초, link 2초 → 실제 retry 관측 후 30초/5초로 튜닝 예정 (시기상조) |
| 썸네일 복원 범위 | 15개 최근 글만 복원 (311개 구형 글 image: 필드는 보류 — 사용자 선택) |
| `_add_image_to_frontmatter()` | `---` split 기반 frontmatter-agnostic 방식으로 개선 (draft: false 위치/존재 무관) |

---

## Blockers

| Block | 이유 |
|-------|------|
| korean-slug-404-debug triage | 대표님 실제 404 URL 미제공으로 Step 27 이후 진행 불가 |

---

## Bugs & Issues Discovered

| 증상 | 원인 | 해결책 | 상태 |
|------|------|--------|------|
| 7/21 이후 썸네일 미삽입 | `_add_image_to_frontmatter()` replace 패턴 `"draft: false\\n---"`가 실제 frontmature `"draft: false\\n\\n---"`(빈 줄)와 불일치 | `re.sub(r'^(draft: false\\s*\\n)(---)$', ...)` 정규식 사용 → 최종적으로 `---` split 기반 frontmatter-agnostic 방식으로 개선 | ✅ 해결 |
| 24개 글 image: 필드 누락 | 위 버그로 thumbnail 파일은 있으나 frontmatter에 image: 없음 | backfill 스크립트로 일괄 추가 | ✅ 해결 |
| description YAML 파싱 에러 | description에 이스케이프되지 않은 큰따옴표 포함 → Astro/YAML 빌드 차단 | `desc_escaped = desc_raw.replace('"', "'")` + 기존 1개 파일 복구 | ✅ 해결 |
| 311개 구형 글 image: 필드 | Pexels 썸네일 도입 전 생성 → `image:` 경로만 있고 실제 파일 없음 (broken image) | 사용자 선택으로 보류 (기본 placeholder fallback 배치 작업 별도 필요) | 🔄 보류 |

---

## Environment Notes

- Cloudflare auth profile: `hugh79757` (default, OAuth)
- Wrangler: 4.110.0 (brew) — deploy 시 `env -u CLOUDFLARE_API_TOKEN` 필수
- Astro 5 SSR + Cloudflare D1 + Pages
- Python 3.14
- `.gitignore`에 `public/images/` 포함되어 있어 썸네일 파일은 git에서 untracked
- 8개 commit 미푸시 상태 (`@{u}..HEAD`)

---

## Resume Instructions

```bash
# 1. 현재 상태 확인
git log --oneline -5
git status --short

# 2. Phase 22 (발송 시스템 활성화) 진행하려면:
#    - scripts/install_course_sender.sh 읽고 launchd plist 설치
#    - enroll.ts에 day 0 hook 확인
#    - 7day-infra/agent 등록 오픈 분기 로직 확인

# 3. 또는 Phase 23 (SNS 콘텐츠 자동화) 계획 수립:
#    - .planning/ROADMAP.md Phase 17 / 23 참조
#    - 7개 plan (17-01~17-07) 중 17-01부터 시작

# 4. korean-slug-404 triage 재개:
#    - .planning/triage/20260721--korean-slug-404-debug.md 참조
#    - 대표님께 실제 404 URL (브라우저 Network 탭) 요청
```

---

## Uncommitted Changes

많은 미커밋 파일이 있습니다 — 대부분은 `git diff`로 확인. 핵심 변경사항:

```
 M blog posts (image: 필드 추가된 7/2~7/23 파일들)
 M src/pages/*.astro (sitemap, index, search 등)
?? src/content/blog/2026-07-21-* (새 블로그 12개)
?? src/content/blog/2026-07-22-* (새 블로그 12개)
?? src/content/blog/2026-07-23-* (새 블로그 6개)
?? scripts/thread_topics/topics/20260721~23/
 M .wrangler/state/...
```
