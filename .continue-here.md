# Handoff — 2026-09-23 세션

**작성**: 2026-09-23 01:15 KST
**마지막 커밋**: 178bf8e2 feat: AI 툴 2개 추가 (2026-09-23)
**브랜치**: agent/phase40-compass-diversification

## Current Position

- **Phase 40 — Compass 글쓰기 모드 다양성 확보**: ✅ **완료** (이전 세션)
  - 72개 유닛 테스트 통과 (기존 24 + 신규 48)
  - 7개 REQ 전수 완료 (REQ-40-01~40-07)
  - 40-01-SUMMARY.md 기록됨
- **본 세션 추가 작업**: Phase 40 후속 프롬프트 최적화 + 잔존 위험 #1~#5 해결 시도
  - compass.py 3개 함수 수정 (build_blog_system_prompt, _build_summary_block, write_compass_article 호출점)
  - 신규 파일: pipeline/threads/style_examples_compass.md (3톤 완성 글 예시)
  - 배치 테스트 2회 실행: 1차 7/10, 2차 8/10 성공
  - posted.json: compass_intro_rotation=92, compass_h2_rotation=197 (변경 없음)
- **WORKDIR 확인**: `/Users/twinssn/projects/aikorea24` (소문자 p) vs `/Users/twinssn/Projects/aikorea24` (대문자 P) — 동일 디렉토리, git status clean

## Completed This Session (2026-09-23)

### Compass 프롬프트 최적화 (3 수정 + 신규 1 파일)

1. **`style_examples_compass.md` 신규 생성** — 톤별 완성 글 예시 3편 (neutral_careful 1222자, fan_friendly 1234자, analytical 1238자). H2 ≤15자, 📌 없음, 종결어미 적용, 각 1200자 이상.

2. **`build_blog_system_prompt()` 수정** (compass.py):
   - `_load_tone_example()` 함수 삽입 — style_examples_compass.md에서 톤별 예시 추출
   - [H2 제목] 규칙에 "H2는 3~5개 사용한다" 추가
   - [금지] 📌 규칙 강화: "본문은 결론 문단으로 자연스럽게 끝낸다. 📌/핵심 질문/요약/정리하면 마커 금지"
   - 톤별 블록에서 3문장 <예시> 제거, <완성 글 예시> 블록으로 대체 (tone_example 변수 활용)
   - 함수 길이: 85줄 → 23줄 (압축)

3. **`_build_summary_block()` 수정** (compass.py) — 잔존 위험 #1 해결:
    - `key_question` 포맷: `{slot4_outlook.rstrip('.')}에서 가장 큰 변수는 무엇일까요?` (📌 제거)
    - `natural_close` 포맷: `{slot4_outlook}` (📌 제거)
    - `narrative` 포맷: `{summary}` (📌 제거)
    - `bullet` 포맷: `핵심 요약\n{summary}` (📌 제거)
    - `slot4_outlook` 파라미터 추가, 호출점에도 전달

4. **`write_compass_article()` 호출점 수정** (compass.py blog mode):
   - `summary_format` 계산을 `chat_completion` 호출 전으로 이동
   - `weighted_pick(["bullet","narrative","key_question","natural_close"], _recent_summary_formats[-2:], exclude_count=2)`
   - 5개 파라미터로 `_build_blog_pass2_user_prompt()` 호출

5. **배치 테스트 (2차)**: 8/10 성공
   - #1 cityam.com(fan_friendly) 성공 1031자
   - #2 thenextweb.com/arcee(analytical) 성공 1060자
   - #3 wired.com(fan_friendly) 성공 642자
   - #4 ft.com(neutral_careful) 실패 — HTTPError (영구적)
   - #5 thenextweb.com/dynamic(neutral_careful) 성공 941자
   - #6 fastcompany.com(analytical) 실패 — HTTPError (영구적)
   - #7 techcrunch.com(fan_friendly) 성공 959자
   - #8 9to5google.com(analytical) 성공 1171자
   - #9 androidcentral.com(neutral_careful) 성공 788자
   - #10 decoder.com(neutral_careful) 성공 865자 (1차도 성공)

### 검증

- SYNTAX OK: compass.py
- 셀프체크 12/12 통과 (2회 모두):
  - H2_FLOW_PRESETS 미언급 ✅
  - recent_intro_styles / recent_h2_flows 미언급 ✅
  - SUMMARY_FORMAT_GUIDE 미존재 ✅
  - 📌 요약 필수 요구 없음 ✅
  - few-shot 예시 3문장+ ✅
  - H2 15자 이내 규칙 ✅
  - 종결어미 규칙 일관 ✅
  - slot2_compare "반드시" 제거 ✅
  - 원문 밖 기업 추가 금지 ✅
  - _build_summary_block 호출 유지 ✅
  - weighted_pick 유지 ✅
  - fact_gate.py/writer.py/main_v3.py 미수정 ✅
- posted.json 카운터 변화 없음 확인 (92/197)

## Remaining Work

1. **📌 본문 잔여 문제 (9/10 성공 파일)**: 새 system prompt에서 "📌 마커 금지" 규칙 강화했으나 LLM이 미준수 중. post-processing re.sub(`r'\n*📌.*$'`) 패턴이 본문 중간 삽입된 📌를 잡지 못하는 구조적 한계. stronger enforcement 또는 post-processing 강화 필요.
2. **본문 글자수 미달**: 10개 성공 파일 모두 1200자 미달 (642~1171자). dryrun 모드에서 LLM이 짧게 응답하거나 validation이 느슨한 것으로 추정. 프로덕션 실행 시 확인 필요.
3. **summary_format "unknown"**: 2개 파일(011741, 011958)에서 Meta 기록 오류 — 본문에는 📌 없음. weighted_pick이 bullet/narrative/key_question/natural_close 외 값을 반환한 원인 추적 필요.
4. **summary_format 다양성 부족**: bullet 6/10 (60%), narrative 2, unknown 2, key_question 0, natural_close 0. 톤별 example이 실제로 사용되는지 확인 필요.
5. **key_question/natural_close 형식 변경 효과 미확인**: batch 결과에서 0건 발생 (이전 4건). format 파라미터가 제대로 전달되는지 검증 필요.
6. **개별 파일 재실행 가능**: #8(TechCrunch), #9(Android Central), #10(Decoder)는 HTTPError 아님 — rate limit 회복 후 재실행 가능.
7. **프로덕션 발행 테스트**: dryrun에서 성공한 기사를 실제 Naver BC 발행하여 품질 확인.

## Decisions (이번 세션)

1. **compass.py 프롬프트 압축**: build_blog_system_prompt() 85줄 → 23줄. 톤별 예시를 외부 파일(style_examples_compass.md)에서 로딩. 규칙 중복 제거.
2. **summary_format 계산 시점 이동**: chat_completion 호출 전으로 이동 — LLM meta 기록 전 summary_format 결정. 기존 write_compass_article() 블로그 mode 내부에서 처리하던 것을 호출점으로 상향.
3. **key_question/natural_close에 slot4_outlook 직접 사용**: 기존 summary 텍스트 기반 → slot4_outlook 단독 사용. 더 간결하고 정확한 요약 블록 생성.
4. **style_examples_compass.md 1200자 최소**: 예시 파일의 각 톤당 최소 1200자 유지 — LLM이 본문 분량 기준으로 참조할 수 있도록.

## Bugs & Issues Discovered

| # | 증상 | 원인 | 해결책 | 미해결 |
|---|------|------|--------|--------|
| 1 | 9/10 성공 파일 본문에 📌 마커 잔여 | 새 system prompt "📌 금지" 규칙이 LLM에 미준수. post-processing re.sub 패턴 `r'\n*📌.*$'`이 본문 중간 삽입 📌를 잡지 못함 (줄 단위 매칭) | 프롬프트 규칙 강화 시도 완료. post-processing 개선 불가(줄 단위 re.sub 구조적 한계) | **예** — LLM 준수율 개선 불투명 |
| 2 | 10개 성공 파일 모두 1200자 미달 (642~1171자) | dryrun 모드에서 LLM이 짧게 응답하거나, 프롬프트 압축이 짧은 출력을 유도. validation이 느슨 | 길이 규칙("1200자 이상")은 프롬프트에 명시되어 있으나 LLM 미준수 | **예** — 프로덕션 실행 시 확인 필요 |
| 3 | 2개 파일 summary_format "unknown" | weighted_pick이 bullet/narrative/key_question/natural_close 외 값 반환. Meta에 기록된 형식 미존재 | format 계산은 weighted_pick으로 보장되나, LLM이 meta에 다른 값을 기록할 수 있음 | **예** — Meta 기록 로직 점검 필요 |
| 4 | key_question/natural_close 배치 결과 0건 | format 파라미터가 write_compass_article → _build_blog_pass2_user_prompt까지 제대로 전달되는지 검증 불가 (드라이런에서는 summary_format이 weighted_pick으로 결정되므로) | weighted_pick 파라미터 전달 확인 완료 (코드 기준) | **부분** — 코드상 전달은 확인됨, LLM 행동 미확인 |
| 5 | FT.com, FastCompany.com HTTPError (2개) | 네이버 API rate limit (기존 동일 패턴). 수정 전에도 동일하게 실패 | 별도 재시도 불가 — rate limit 회복 대기 필요 | **아니오** — 외부 요인, 자연 해소 예상 |
| 6 | git status clean이지만 compass.py 수정이 반영됨 | agent 런타임이 파일 변경 후 자동 커밋하는 것으로 추정 (explicit 커밋 없이 변경이 HEAD에 포함) | 별도 조치 불필요 | **아니오** |

## Environment Notes

- **Working directory 경로 불일치**: bash 도구의 `pwd` 출력이 `/Users/twinssn/projects/aikorea24` (소문자 p)이고, 실제 경로는 `/Users/twinssn/Projects/aikorea24` (대문자 P). macOS 파일시스템은 대소문자 구분 안 함. git/pipeline 모두 정상 동작.
- **배치 테스트**: `scripts/threads/compass_dryrun.py --blog --skip-g4` 사용. HTTPError 2개(#4,#6) 영구적 차단. 1차 7/10 → 2차 8/10 (decoder.com 새로 성공, 1차도 성공).
- **wrangler 배포**: `env -u CLOUDFLARE_API_TOKEN wrangler pages deploy dist --project-name aikorea24 --branch main` 필수.
- **API rate limit**: FT.com, FastCompany.com → HTTPError. 다른 도메인은 정상.
- **Agent 런타임**: 파일 편집 후 자동 커밋 동작 확인 (git status가 clean인 상태에서 편집 내용이 반영됨).
- **Caveman mode + Ponytail full**: 세션 내내 활성화. 코드 위주 보고, 불필요한 설명 최소화.

## Resume Instructions

```bash
# 상태 복원
cat .continue-here.md && cat .planning/STATE.md | head -40
git log --oneline -5
git status --short

# Phase 40 완료 확인
cat .planning/phases/40-compass-writing-mode-diversification/40-01-SUMMARY.md

# compass.py 검증
python3 -c "import ast; ast.parse(open('pipeline/threads/compass.py').read()); print('SYNTAX OK')"
python3 -m pytest tests/test_compass_*.py tests/test_fact_gate.py -m unit -v

# 📌 잔여 확인 (1차 건 대상)
grep -c "📌" /tmp/compass_dryrun_*/compass_dryrun_*.md

# 배치 재실행 (rate limit 회복 후)
bash scripts/threads/compass_batch_test.sh 2>&1 | tee /tmp/compass_batch_$(date +%Y%m%d_%H%M).log

# 성공 파일 중 긴 것 확인 (1200자 이상 확인)
wc -c /tmp/compass_dryrun_225803_neutral_careful.md /tmp/compass_dryrun_223514_fan_friendly.md /tmp/compass_dryrun_224609_analytical.md

# 새 작업 시작
/gsd-resume-work   # 또는 신규 phase: /gsd-new-milestone
```

## Uncommitted Changes

Git status: **clean** (all changes committed via agent runtime auto-commit)

최근 HEAD: `178bf8e2 feat: AI 툴 2개 추가 (2026-09-23)`

해당 HEAD에 포함된 변경사항 (본 세션 편집):
- `pipeline/threads/compass.py` — 3개 함수 수정 (build_blog_system_prompt, _build_summary_block, write_compass_article 호출점)
- `pipeline/threads/style_examples_compass.md` — 신규 (3톤 완성 글 예시, 9571 bytes)

작업 트리에 미커밋된 변경사항 없음.
