# Continue Here — Session Handoff

**Created:** 2026-07-07 09:30
**Project:** aikorea24.kr
**Status:** Phase 15 Complete — Hook fix applied, launchd active, pipeline verified

---

## Current Position

**Phase 15 (Vectorize + D1 fix + Writer fallback + Hook fix) — LIVE**
- Vectorize index `aikorea24-dedup` created (1536d, cosine)
- 283 posted articles migrated to Vectorize
- D1 save loss bug fixed (link UNIQUE constraint silent drop)
- Writer fallback chain fixed (DeepSeek → GPT-4o-mini)
- JSON parsing 4-stage recovery restored
- **Hook 검증 수정: `cards[0]` 전체 → 첫 줄만 검사 (ThreadForge 정렬)**
- **launchd `kr.aikorea24.threads-publisher` 재활성화 — 2시간 간격 자동 발행 중**
- Pipeline dry-run 3회 + 실제 발행 1회 성공

---

## Completed This Session

1. **D1 save loss debugging & fix**
   - Root cause: `link TEXT UNIQUE NOT NULL` — `get_existing()` only checked title hashes (7d), not links (all time)
   - `INSERT OR IGNORE` silently dropped 60-80% of articles
   - Fix: `get_existing()` returns both title hashes AND links (all time)
   - `save_to_d1()` parses wrangler `changes` field for actual insert count
   - Before: 245 collected → 3 actual saved (corrected from 180 "logged")

2. **News collector Vectorize auto-indexing**
   - `index_new_to_vectorize()` added to end of `main()` in `news_collector.py`
   - Queries D1 for today's saved articles → embeds → upserts to Vectorize

3. **Writer fallback fix (모든 모델 응답 실패)**
   - Removed `response_format={"type": "json_object"}` — DeepSeek `deepseek-v4-flash` unstable with this param
   - Changed to sequential fallback: try DeepSeek → if None, try GPT-4o-mini
   - Removed ThreadPoolExecutor (no longer needed with single model + fallback)

4. **JSON parsing 4-stage recovery**
   - Stage 1: direct `json.loads()`
   - Stage 2: extract from ```json``` code block
   - Stage 3: brace matching `{...}` extraction
   - Stage 4: delimiter fallback (`---카드 1---`, `카드 1:` formats)

5. **Vectorize index creation & migration**
   - Index `aikorea24-dedup` did not exist → created via API (1536d, cosine)
   - Migration: 283/329 articles indexed (46 empty-article skips)

6. **Hook 검증 수정 (ThreadForge 정렬)**
   - `validate_card_structure()`: `cards[0]` 전체 → `cards[0].split('\n')[0]` 첫 줄만 검사
   - Threshold 10 유지, 대상 범위만 축소 → 첫 카드 3개 stanza(11문장) 자연 허용
   - 08:33 실행: 발행 성공 ✅ / 08:59 실행: 검증 통과 (DNS 장애로 발행만 실패)

7. **Threads publisher launchd 재활성화**
   - `launchctl load ~/Library/LaunchAgents/kr.aikorea24.threads-publisher.plist`
   - 2시간 간격 자동 실행 시작

---

## Remaining Work

1. **Vectorize migrate remaining articles**
   - 46 articles with empty title/description failed embed — from failed_articles tracking

2. **Sports project (`~/Projects2/sportsnew/`)**
   - Replace RSS feeds, keywords, entity tiers
   - Update branding (site name, URLs, descriptions)
   - Create new Cloudflare D1 DB + Vectorize index

3. **failed_crawls.json cleanup**
   - Article IDs accumulated during today's failed runs
   - TTL 24h should auto-clear; verify tomorrow

---

## Decisions

| Decision | Rationale |
|----------|-----------|
| D1 `get_existing()` queries ALL links (not 7d) | Prevents `INSERT OR IGNORE` silent drops on UNIQUE constraint |
| `response_format` removed from writer | DeepSeek `deepseek-v4-flash` inconsistently enforces JSON output; falls back to plain text |
| Writer: sequential fallback (not ThreadPoolExecutor) | Only 1 model + 1 fallback — parallel race adds complexity without benefit |
| JSON parsing: 4 stages (not JSON-only) | Phase 15 removed delimiter fallback too aggressively; DeepSeek non-JSON output needs recovery path |
| Vectorize auto-index after collect | API cost is negligible (~$0.00002/article); real-time dedup is worth it |
| Hook 검증: 첫 줄만 | ThreadForge `role: hook` 명시적 분리 방식과 정렬; 첫 카드 내부 stanza 자연 허용 |

---

## Blockers

None. Pipeline verified. launchd active. Ready for production.

---

## Uncommitted Changes

```
 M api_test/__pycache__/...
 M pipeline/threads/__pycache__/...
 M scripts/thread_topics/__pycache__/...
 M scripts/threads/__pycache__/...
 M scripts/threads/logs/launchd.log
 M scripts/threads/posted.json
 M scripts/tools_collector.log
 M tests/__pycache__/...
?? pipeline/infra/__pycache__/vectorize_client.cpython-314.pyc
?? scripts/threads/__pycache__/migrate_to_vectorize.cpython-314.pyc
```

Only runtime artifacts (logs, caches, posted.json). No code changes pending.

---

## Resume Instructions

```bash
# Check current state
cat .planning/STATE.md | head -20

# Run test suite
.venv/bin/pytest tests/ -x -q

# Run dry-run
.venv/bin/python3 scripts/ scripts/threads/main_v3.py --dry-run

# Run production (after verifying dry-run)
.venv/bin/python3 scripts/threads/main_v3.py

# Check launchd status
launchctl list | grep aikorea24
```

---

## Key Files

| File | Purpose |
|------|---------|
| `pipeline/threads/writer.py` | JSON 4-stage parsing, DeepSeek→GPT-4o-mini fallback, hook 검증용 첫 줄 추출 |
| `pipeline/threads/validator.py` | validate_card_structure() hook_first_line 검증 |
| `api_test/news_collector.py` | News collection, D1 save with link dedup, Vectorize auto-index |
| `scripts/threads/main_v3.py` | Main entry point, Vectorize indexing |
| `pipeline/infra/vectorize_client.py` | Vectorize REST API + OpenAI embeddings |
| `scripts/threads/migrate_to_vectorize.py` | Existing article migration (283/329 done) |