# .continue-here.md

> **Handoff generated:** 2026-07-12 (KST)
> **Session:** Sisyphus (aikorea24) — post-Phase 25 + Option B seed refactor
> **Branch:** `main`
> **Latest commit:** `bb74e9d` (Phase 25: 커뮤니티 레슨 순차 해금, deployed)
> **Uncommitted:** Option B seed-script refactor (2 files) — verified, not committed

---

## Current Position

| Metric | Value |
|--------|-------|
| **Phase** | Post-Phase 25 (커뮤니티 레슨 순차 해금 — committed `bb74e9d`, deployed) |
| **Status** | 🟡 **Paused mid-content-review** — Option B seed refactor done+verified, uncommitted |
| **Last Activity** | 2026-07-12 |
| **Git Branch** | `main` — 2 source files modified (uncommitted), runtime logs also dirty |
| **Next milestone** | 크론 활성화(Phase 22 plist) — 콘텐츠 검수 완료 후 |

---

## Completed This Session

### 1. 강의 편집 권한 메커니즘 확인
- `twinssn@gmail.com`은 `users.role = 'owner'` (마이그레이션 `scripts/migrations/20260710_add_admin_role.sql`: `UPDATE users SET role='owner' WHERE email='twinssn@gmail.com'`).
- 커뮤니티 편집 게이트: `src/pages/community/[id].astro`(편집 버튼 `isAdmin` 표시) + `src/pages/api/community/[id]/update.ts`(`isOwner` → 403) + `src/lib/auth.ts`(`isOwner = role==='owner'`).
- **결론**: twinssn@gmail.com 로그인 시 모든 커뮤니티 글(강의 레슨 포함) 본문을 마크다운으로 직접 수정 가능. 수정은 `posts.content` 갱신 → 잠금 해제된 수강생에게 반영.

### 2. 재시드 클로버 위험 분석 + Option B 결정
- 기존 `--update` 모드가 `UPDATE posts SET content=...` + `UPDATE course_lessons SET teaser_html=...`로 기존 레슨 본문을 seed 값으로 **무조건 덮어씀** → 커뮤니티 손질 소실.
- 사용자 결정: **Option B = DB(커뮤니티 편집)를 강의 본문 유일 정본으로**. 시드 스크립트는 부트스트랩/구조 보강 전용.

### 3. Option B 구현 (두 시드 스크립트)
- `scripts/seed_course_7day_starter.py`, `scripts/seed_course_7day_infra.py`의 `build_sql(update_mode=True)`에서 본문/티저 갱신 UPDATE 블록 **제거**.
- 유지: `INSERT OR IGNORE INTO posts` / `INSERT OR IGNORE INTO course_lessons` (신규 레슨·매핑 보강) + `UPDATE courses SET title,description` (강좌 메타 동기화).
- 주석/argparse help를 "기존 본문 갱신 안 함"으로 갱신.

### 4. 검증 (위임 실행, DB 미접촉 `--dry`)
- PASS. 두 스크립트 exit 0, `UPDATE posts SET`=0, `UPDATE course_lessons SET teaser_html`=0, 구조 INSERT 보강 유지(8/7개), `UPDATE courses SET`=1, 이중 `else:` 없음.

---

## Remaining Work

1. **커밋/배포**: Option B seed 변경 2파일 미커밋 → WIP 커밋 권장 (이 핸드오프와 함께).
2. **콘텐츠 검수**: 사용자가 커뮤니티에서 전 레슨 본문 수정 (twinssn@gmail.com owner). `--update` 재시드해도 보존됨.
3. **크론 활성화 (Phase 22 plist)**: 모든 콘텐츠 준비 후 마지막에. 현재 OFF → 프로덕션에서 전 레슨 잠금(`days_sent=0`).
4. **별도 미해결 이슈** (이번 세션 범위 밖): day 0 고아 레슨(`send-daily`가 `days_sent+1`부터 발송), 랜딩 페이지 커리큘럼≠시드 불일치.
5. (선택) `CHANGES.md`에 Option B 변경 항목 추가 (프로젝트 컨벤션).

---

## Key Decisions (this session)

- **Option B 채택**: 강의 본문 정본 = DB(커뮤니티 편집). 시드 `--update`는 구조 보강만(본문/티저 덮어쓰기 제거).
- **해금 기준 = `days_sent`** (Phase 25 확정, 크론 OFF 수용). auto-enroll 없음(강좌 폼/뉴스레터 폼 분리 유지).
- **시드 스크립트 잔존 이유**: (1) 최초 부트스트랩 필수, (2) 구조 소유권(`courses` row, `course_lessons` day_number 매핑 — 커뮤니티 폼이 못 함). 즉 "불필요"가 아니라 "정본이 둘"이 문제였음.

---

## Blockers

| Blocker | Impact | Resolution |
|---------|--------|------------|
| 크론 OFF (사용자 결정, 콘텐츠 검수 중) | 프로덕션에서 레슨 전체 잠금 | 검수 완료 후 Phase 22 plist 설치 |
| 콘텐츠 검수 대기 (사용자 주도) | 레슨 본문 미확정 | 커뮤니티에서 twinssn@gmail.com이 직접 수정 |

---

## Uncommitted Changes

실제 코드 변경 (보존 필요):

| File | Change |
|------|--------|
| `scripts/seed_course_7day_starter.py` | Option B: `--update`에서 본문/티저 UPDATE 블록 제거, 주석/help 갱신 |
| `scripts/seed_course_7day_infra.py` | 동일 |

무시할 런타임 아티팩트 (커밋 제외 — Phase 25 때도 제외함):

| File | Note |
|------|------|
| `scripts/blog_draft_error.log` | 로그 |
| `scripts/threads/logs/launchd.log` | 로그 |
| `scripts/threads/posted.json` | 런타임 상태 |
| `scripts/tools_collector.log` | 로그 |

---

## Resume Instructions

```bash
# 1. 컨텍스트 복원
cd /Users/twinssn/Projects/aikorea24
cat .continue-here.md

# 2. 미커밋 변경 확인
git status
git diff scripts/seed_course_7day_starter.py scripts/seed_course_7day_infra.py

# 3. 시드 스크립트 검증 (위임 실행 권장 — AGENTS.md: python3 직접 실행 금지)
#    subagent/opencode로 실행:
#    python3 scripts/seed_course_7day_starter.py --dry --update
#    python3 scripts/seed_course_7day_infra.py --dry --update
#    → UPDATE posts SET / UPDATE course_lessons SET teaser_html 가 0개인지 확인

# 4. 강의 본문 수정 (콘텐츠 검수)
#    - twinssn@gmail.com 으로 로그인 → /community/{id} → 편집 버튼 → 마크다운 수정
#    - 재시드(--update)해도 보존됨 (Option B)

# 5. 검수 완료 후
#    - 크론 활성화: Phase 22 plist 설치 (scripts/install_course_sender.sh or launchd)
#    - Option B 변경 커밋: git add scripts/seed_course_7day_*.py && git commit -m "refactor(seed): --update 구조 보강만, 본문 덮어쓰기 제거 (DB 정본)"
```
