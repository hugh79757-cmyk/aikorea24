# .continue-here.md — Session Handoff

## Current Position
- **Milestone**: v1.0
- **Phase**: 13 (card-separation-fix) — **COMPLETE** (committed)
- **Next**: Phase 14 (undefined) — E2E dry run with DeepSeek v4-flash to validate card quality

## Completed This Session
### Phase 13 completion + Extended fixes

**Model Routing (DeepSeek v4-flash rollout)**
- `model_router.py`: `deepseek-v4-flash` 추론 모델 적용 (`deepseek-chat`에서 전환)
- `writer.py`: `write_thread()` 모델 `['deepseek']` (속도 8.8s, 내용 2~3배 향상)
- `pitch.py`: 피치 생성 + 재생성 `model_override='deepseek'`
- `humanize_cards`/`fix_cards`: **GPT-4o-mini 유지** (속도 4초)
- 발견: `deepseek-v4-flash`는 추론 모델 → `max_tokens` 충분해야 출력 생성

**Delimiter 안정화**
- `writer.py`: 카드 구분자 `---` → `\n\n\n` (stanza용 `\n\n`과 명확히 분리)
- `parse_cards()`: `\n\n\n` 우선 분할 → `\n\n` fallback
- `_repair_truncated_cards()`: backward pass + MIN_COUNT=4 가드
- hook merge 레거시 코드 제거
- `assemble_final()`: 6개 미만 시 split 패딩 (안전장치)

**Validation 개선**
- `validator.py`: `sentence_enders`에 `\u3002`(중국어 마침표) 추가
- `validator.py`: `FORMAT_CARD_COUNT_TOLERANCE` D (5,7)→(4,7)
- `validator.py`: Hook 문장 종결 과다 5→10
- `writer.py`: 카드 최소 길이 100자 검증 (링크 제외)
- `main_v3.py`: ENG_LEAK_RE cleanup 개선 (MixCase + `은`/`는`)

**Pitch 안정화**
- `pitch.py`: JSON mode 완전 제거 (일반 텍스트만, 1회 호출)

**블로그 배포 복구**
- `scripts/validate_blog_posts.py` 신규 (빌드 전 frontmatter 검증)
- `scripts/deploy.sh`에 validate 통합
- frontmatter `---` 누락 복구 (1개), 에러 메시지 파일 삭제 (1개)

**중복 방지**
- `_remove_duplicate_links()`: parse_cards + assemble_final 이중 방어

## Decisions
| Decision | Rationale |
|----------|-----------|
| `deepseek-v4-flash` write_thread 1순위 | GPT-4o-mini 대비 내용 2~3배 향상 (150자→~400자) |
| humanize/fix_cards GPT 유지 | 속도 우선 (4초, DeepSeek은 40초 예상) |
| `\n\n\n` delimiter 채택 | stanza용 `\n\n`과 카드 구분 명확히 분리 |
| 카드 최소 100자 검증 | 70자짜리 링크 카드 방어 |
| 피치 JSON mode 제거 | GPT/DeepSeek 모두 JSON 불안정, regex fallback 안정적 |

## Remaining Work
1. **E2E 드라이런**: DeepSeek v4-flash 적용 후 전체 흐름 검증 (이번 세션 미실행)
2. **카드 길이**: DeepSeek v4-flash로 개선 예상되나 아직 드라이런 미확인
3. **Hook 문장 종결 과다**: 구조적 문제, `\n\n\n` delimiter로 완화 기대
4. **Blog pipeline**: build 성공 확인, deploy 후 404 링크 처리 (부모들-a-i-남용 파일 삭제로 브리핑 링크 404 가능)

## Blockers
- E2E 드라이런 미실행 (시간 부족)

## Resume Instructions
```bash
cd ~/Projects/aikorea24

# Run E2E dry run with DeepSeek v4-flash
.venv/bin/python3 scripts/threads/main_v3.py --dry-run

# Or run single article explicit test
tail -f scripts/threads/logs/launchd.log
```

## Uncommitted Changes
- `pipeline/posted.json` (runtime data)
- `scripts/threads/posted.json` (runtime data)
- `scripts/threads/logs/launchd.log` (runtime logs)
- `*.pyc` (cache, gitignored)

All source code committed and pushed (`f3c6a40` → `origin/main`). Tests: 287/287 passing.
