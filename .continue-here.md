# .continue-here.md — Session 2026-07-03

## Current Position
**Phase 6: Prompt Leakage & Truncation Fix** — ALL STEPS COMPLETED
- PLAN.md at project root (no `.planning/` directory exists)

## Completed This Session
1. **진단**: 문제 A (30/50자 강제 트렁케이션) + 문제 B (프롬프트 라벨 `상식(A):` 누출) 식별
2. **Step 1**: `LEAKED_PROMPT_PATTERNS` 리스트 + `clean_leaked_prompt()` 헬퍼 추가 (`pipeline/threads/pitch.py`)
3. **Step 2**: SYSTEM_PROMPT 수정 — JSON 배열 스키마 명시, "라벨 출력 금지" 지시 강화
4. **Step 3**: `save_pitch_to_history()` — `clean_leaked_prompt()` 방어막 추가, `[:30]`/`[:50]` 트렁케이션 제거
5. **Step 4**: `posted.json` (2개 파일) 오염 entry cleanup — 총 72개 정리
6. **Step 5**: `is_duplicate_pitch()` 트렁케이션 완화 — `[:15]`→`[:80]`, `[:30]`→`[:120]`
7. **JSON Structured Output 전환**:
   - `model_router.py`: `response_format` 파라미터 + `**kwargs` 전달
   - `parse_pitches_from_text()`: `json.loads()` 직통 → `_parse_pitches_fallback()` regex 보존
   - `SYSTEM_PROMPT`: JSON 배열 스키마 + "json" 키워드
   - 모든 `chat_completion()` 호출에 `response_format={'type': 'json_object'}` 추가
   - crawl prompt에도 동일 적용
   - fallback 스키마 `[:18]`→`[:30]`, `[:100]`→`[:200]` 완화

## Remaining Work
- **Phase 6 is complete**. No remaining work.
- Pre-existing `test_cascade_2pass.py` freshness=0 failure (fixture date 동일 문제)
- 오늘자 블로그 포스트 5개 + 이미지 5개 untracked 상태 (파이프라인 산출물, 미커밋)

## Decisions
- `response_format={'type': 'json_object'}` 사용으로 프롬프트 누출 근본 차단
- 정상 경로(JSON 모드)에서는 hook/narrative 트렁케이션 0%
- `_parse_pitches_fallback()` 보존 (이전 데이터 호환성)
- `clean_leaked_prompt()`는 `LEAKED_PROMPT_PATTERNS` 리스트로 확장 가능

## Blockers
- None

## Uncommitted Changes
- **수정**: `pexels_used_ids.json`, `posted.json`(x2), `pitch.py`, `model_router.py`, `test_pitch.py`
- **미추적**: `PLAN.md`, `public/images/`(5), `src/content/blog/`(5)
- `__pycache__/` + `scripts/tools_collector.log` 변화 있음 (gitignore 대상)

## Resume Instructions
```bash
# Session resume — just run tests to confirm all good:
python3 -m pytest tests/test_pitch.py -v

# Full suite:
python3 -m pytest --tb=short

# Check posted.json for any remaining label leaks:
python3 -c "
import json, re
pat = re.compile(r'상식\s*[\(（]\s*A\s*[\)）]|실제\s*[\(（]\s*B\s*[\)）]')
for f in ['pipeline/posted.json', 'scripts/threads/posted.json']:
    d = json.load(open(f))
    n = sum(1 for h in d.get('pitch_history',[]) if pat.search(h.get('narrative','') or ''))
    print(f'{f}: {n} leaks remaining')
"
```
