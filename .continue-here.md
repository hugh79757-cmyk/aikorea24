# Continue Here — 2026-09-06 야간 세션 종료 시점

## 컨텍스트: 이메일 미발송 사건 원인 + D1 쿼터 최적화

### 사건 요약
9/2~9/6 아침 이메일 미발송. 원인: D1 free tier 일일 row read 한도(500만, 계정 전체 공유) 소진
→ 06:00 파이프라인 "D1 조회: 0건" → 브리핑 스킵 → 이메일 스킵 연쇄.
저녁 20:00 실행은 성공 (쿼터 UTC 자정 리셋 후라서). 9/6 07:04 재시도 성공 (브리핑 2026-09-06-1, 메일 delivered 확인).

### 오늘 완료
1. threadsp PENDING_TTL 15s→65s (commit 28d13f3, 배포 a5f1718e) — HITL 폴 30s vs 캐시 TTL 15s → 0% 히트였음. 156만→36만 rows/일
2. news_collector.py pub_date ISO 정규화 (수집+저장 2곳) — commit 0e2cbc4
3. 재시도 기능 커밋 (run_pipeline_with_notify.py FAILURE_MARKERS) — commit 0e2cbc4
4. d1_client.py d1_query 실패 warning 로깅 — commit 0e2cbc4
5. news.astro 14일 윈도우 (commit 3aa360b, Pages 배포, 라이브 200 확인) — 44만→6만 rows/일
6. pub_date RFC→ISO 백필 6,000/9,293건 (3,039건 잔여)

### 백필이 뭐였지?
news.pub_date가 ISO(14,563)/RFC 2822(9,040)/기타(254+) 혼재. db_reader.py가 SQL CASE로 RFC 파싱
→ 인덱스 불가 → 풀스캔 23,863행 × 87회/일 = 119만 rows/일. 데이터를 ISO로 통일하면 CASE 삭제 +
인덱스 탑재 가능. 수집 단계 이미 고침(신규 유입 ISO만). 백필은 기존 데이터 변환.

### 현재 상태 (중요)
- 오늘 쿼터 소진 (백필 UPDATE 실행 중) → D1 전면 차단, 오전 07:00 로컬(UTC 자정) 리셋
- HITL threadsp 페이지 "목록 로딩 실패: D1_ERROR ... row read limit" 떠도 정상 — 리셋 후 자동 복구
- 07:10 예약 스크립트 실행 대기 중: PID 94661 (nohup sleep 32700 → d1_maintenance.sh)
  - 로그: /tmp/d1_maintenance_20260907.log
  - 내용: aikeep24lite-db export 백업 → posts/articles 전체 DELETE (사용자 승인, 수동 발행 전용화) → 백필 잔여 3,039건 → 검증
  - 스크립트: /var/folders/6r/kjl8wkw53t1bnr1dypqtccj80000gn/T/opencode/d1_maintenance.sh
  - 백필 스크립트: /var/folders/6r/kjl8wkw53t1bnr1dypqtccj80000gn/T/opencode/backfill_pubdate.py

### 내일 오전 할 일 (순서대로)
1. `cat /tmp/d1_maintenance_20260907.log` — 예약 스크립트 결과 확인 (07:10 이후)
   - 실패 시: 스크립트 수동 재실행 (chmod +x 되어 있음)
2. 백필 완료 확인: `wrangler d1 execute aikorea24-db --remote --json --command "SELECT COUNT(*) as c FROM news WHERE pub_date NOT LIKE '____-__-__%' AND pub_date != ''"` → 254 내외(기타형식)면 성공. 3천대면 백필 재실행
3. `scripts/threads/db_reader.py` CASE 쿼리 제거 (L341~400 sql2/sql3의 CASE 블록 → `pub_date >= date('now','-7 days')` 단순화)
4. 인덱스 생성: `CREATE INDEX IF NOT EXISTS idx_news_pubdate ON news(pub_date)` — 이때 d1_reader의 source_filter도 확인 (source IN (...)이면 (source, pub_date) 복합 고려)
5. threads 파이프라인 1회 수동 실행으로 풀스캔 제거 검증 (rows_read 확인)
6. CHANGES.md 갱신 + 커밋

### 주의사항
- wrangler 명령 앞 `env -u CLOUDFLARE_API_TOKEN` 필수 (전역 규칙)
- 9/2~9/5 silent-0 (에러 없이 0건)은 쿼터 에러 형식(exit 1+stderr)과 달라 별도 원인 가능성.
  d1_query 로깅 신규 추가돼서 재발 시 잡힐 것. 재발 없으면 그냥 넘어가도 됨
- threadsp는 이미 수동 전용 (crons=[], /api/trigger 410). 자동 크롤 없음
- aikeep24lite purge 후 HITL 목록은 빈 상태로 시작 — 수동 발행 시 새 article만 생김

### git 상태
- aikorea24: main, 오늘 커밋 0e2cbc4 / 3aa360b / 2415632 (docs)
- threadsp: main, 오늘 커밋 28d13f3 (배포 완료, push 미수행 — 원격 5 커밋 뒤짐)
- 미커밋 남은 것: briefing_dedup.json (6244줄 — 데이터 캐시, 커밋 안 함), tmp_test/* (테스트 잔재)
