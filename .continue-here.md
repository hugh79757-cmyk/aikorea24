# Continue Here — aikorea24.kr Handoff

**Timestamp**: 2026-07-25 14:35 KST  
**Session**: Phase 28 (28-01/02/03) 실행 완료 — 썸네일 DeepSeek API 장애 대응 + 블로그 발행 개선  
**Branch**: main (origin/main과 동기화됨)

---

## Current Position

| Item | Status |
|------|--------|
| **Active Phase** | Phase 28: 썸네일 DeepSeek API 장애 대응 + 블로그 발행 개선 |
| **Completed Plans** | 28-01, 28-02, 28-03 (3/4 plans) |
| **Deferred** | 28-04: 모니터링/알림 (사용자 요청으로 폐기) |
| **ROADMAP** | Phase 28 추가됨 (`.planning/ROADMAP.md`) |
| **Phase Dir** | `.planning/phases/28-thumbnail-deepseek-fix/` (3개 플랜 문서) |

---

## Completed This Session

### 1. Phase 28 추가 및 플랜 작성
- ROADMAP.md에 Phase 28 추가 (썸네일 DeepSeek API 장애 대응 + 블로그 발행 개선)
- 4개 플랜 문서 생성: 28-01~28-04 (28-04는 추후 폐기 결정)

### 2. Plan 28-01: auto_thumbnail.py 전면 재작성 ✅
**파일**: `scripts/auto_thumbnail.py` (316→351줄)

| 버그 | 수정 내용 |
|------|-----------|
| DeepSeek 실패 시 고정 fallback "abstract technology" | **3회 재시도 + 랜덤 풀 선택** (`random.choice(DEEPSEEK_POOL)`) |
| Pexels 1페이지만 검색 → 미사용 사진 발견 확률 낮음 | **페이지네이션 3페이지** (`max_pages=3`, 최대 45개 후보) |
| 대체 쿼리에 원본 키워드 포함 → 동일 결과 반복 | **원본 키워드 제외** (`[q for q in DEEPSEEK_POOL if q != keyword][:5]`) |
| 최종 폴백: `photos[0]` 재사용 → 5개 포스트 동일 썸네일 | **Placeholder 복사** (`_use_default_thumbnail()`) |
| 로깅 부족 | **구조화 로그**: `fallback_reason`, `chosen_id`, `page` 등 기록 |

### 3. Plan 28-02: 중복 검증 게이트 (blog_draft_generator.py) ✅
**파일**: `scripts/blog_draft_generator.py` (537→631줄)

- 썸네일 생성 완료 후, 텔레그램 알림 전 배치 중복 검증 추가 (라인 446-508)
- MD5 해시 비교로 동일 썸네일 감지 시 → 해당 포스트 재시도 (최대 2회)
- 재검증 후 결과 로깅 + 텔레그램 알림에 중복 현황 포함

### 4. Plan 28-03: 이미지 품질 검증 + 발행 전 체크리스트 ✅
**파일**: `scripts/auto_thumbnail.py` + `scripts/blog_draft_generator.py`

- `validate_thumbnail_quality(filepath, min_size_kb=15)` 함수 추가:
  - 파일 존재, 크기 ≥ 15KB, 800×800 해상도, WEBP 포맷, PIL verify 통과
- 품질 미달 시 자동 재생성 (최대 2회) → placeholder 폴백
- 발행 전 품질 체크리스트 출력 (라인 520+):
  ```
  === 발행 전 품질 체크리스트 ===
  2026-07-25-001: 45KB, 800x800, WebP ✅
  2026-07-25-002: 12KB, 800x800, WebP ❌ (파일 크기 미달)
  전체: N개 검증, M개 통과, K개 이슈
  ```

### 5. 테스트 검증
- **275 passed, 2 failed** (pre-existing, 무관)
  - `test_crawler.py::TestFetchArticleBody::test_successful_crawl` (BeautifulSoup 미설치)
  - `test_writer.py::TestBuildSystemPromptD::test_contains_required_keywords` (프롬프트 변경)

---

## Remaining Work

| Priority | Task | Status | Notes |
|----------|------|--------|-------|
| 🔴 High | 7/23~7/25 블로그 19건 커밋 + 배포 | 미완료 | `src/content/blog/` 19개 파일 untracked |
| 🟡 Medium | Phase 22: 발송 시스템 활성화 | 미완료 | launchd plist + day 0 hook + 3개 강좌 오픈 |
| 🟡 Medium | Phase 23: SNS 자동화 (Instagram Carousel + Shorts) | Planned | 7개 플랜 대기 |
| 🟢 Low | Phase 28-04 모니터링/알림 | 폐기 | 사용자 판단: "모니터링해도 나아지지 않을 것" |

---

## Decisions

1. **Phase 28-04 폐기**: 사용자 지시 — "모니터링을 한다고 나아질 거 같지 않다. 미실행분 실행한다."
2. **최소 파일 크기 15KB**: 품질 검증 임계값으로 설정 (Pexels 평균 50~120KB 대비 여유)
3. **페이지네이션 3페이지**: Pexels API rate limit 고려해 보수적 설정
4. **재시도 2회 제한**: 무한 루프 방지 + placeholder 최종 폴백 보장

---

## Bugs & Issues Discovered

### 1. 7/25 동일 썸네일 5개 발생 (치명적)
- **증상**: 2026-07-25 002~006번 포스트 5개가 MD5 `8c93b87916afb3bc1e227bdda928b569` 동일 썸네일 사용
- **원인**: 
  1. DeepSeek API 5회 연속 실패 → 고정 fallback "abstract technology"
  2. `DEEPSEEK_POOL[:3]`에 원본 키워드 포함 → 대체 쿼리도 동일 결과
  3. 최종 폴백 `photos[0]` 재사용 (b49a09b 커밋에서 도입)
- **해결책**: Plan 28-01에서 4개 버그 동시 수정 (재시도+랜덤, 페이지네이션, 원본제외, placeholder폴백)
- **미해결 여부**: ✅ 해결됨 (다음 발행부터 적용)

### 2. 기존 테스트 2개 실패 (Pre-existing)
- **증상**: 크롤러 BeautifulSoup 미설치, Writer 프롬프트 키워드 변경
- **원인**: 환경 의존성 + 프롬프트 리팩토링 (본 세션 변경과 무관)
- **해결책**: 별도 환경 설정 세션에서 처리 필요
- **미해결 여부**: ⏳ 다음 세션에서 환경 설정 시 해결

---

## Environment Notes

| Item | Value |
|------|-------|
| **OS** | macOS (Darwin) |
| **Python** | 3.14.5 (kaggle-env) |
| **Node** | npm/wrangler v4.110.0 (OAuth profile) |
| **Deploy** | Cloudflare Pages (`wrangler pages deploy dist --project-name aikorea24 --branch main`) |
| **Auth** | wrangler OAuth profile `hugh79757` (CLOUDFLARE_API_TOKEN env var 해제 필요) |
| **Scheduler** | launchd (`kr.aikorea24.tools-collector`, `kr.aikorea24.blog-draft`) |
| **Key Paths** | `PROJECT_DIR=/Users/twinssn/Projects/aikorea24`, `PUBLIC_IMAGES_DIR=/public/images` |

---

## Resume Instructions

```bash
# 1. 프로젝트 디렉토리 이동
cd /Users/twinssn/Projects/aikorea24

# 2. 최신 상태 확인
git status --short
git log --oneline -3

# 3. 미커밋 블로그 포스트 배포 (우선순위 1)
git add src/content/blog/2026-07-23-* src/content/blog/2026-07-24-* src/content/blog/2026-07-25-*
git commit -m "blog: 7/23~7/25 블로그 19건 추가"
npm run build
env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler pages deploy dist --project-name aikorea24 --branch main --commit-dirty=true

# 4. Phase 22 발송 시스템 활성화 (다음 작업)
./scripts/install_course_sender.sh
# enroll.ts day 0 hook 추가
# 7day-infra/7day-agent 등록 오픈

# 5. 테스트 재실행
python3 -m pytest tests/ -v --tb=short
```

---

## Uncommitted Changes

### Modified (3)
- `.planning/ROADMAP.md` — Phase 28 추가
- `scripts/auto_thumbnail.py` — Plan 28-01, 28-03 핵심 로직
- `scripts/blog_draft_generator.py` — Plan 28-02, 28-03 통합
- `scripts/briefing_dedup.json` — 브리핑 중복 캐시 업데이트

### Untracked Directories (2)
- `.planning/phases/28-thumbnail-deepseek-fix/` — Phase 28 플랜 문서 3개
- `scripts/thread_topics/topics/20260724/`, `20260725/` — Threads 주제 캐시

### Untracked Blog Posts (19 files)
- `src/content/blog/2026-07-23-007~012` (6개)
- `src/content/blog/2026-07-24-001~012` (12개)
- `src/content/blog/2026-07-25-001~006` (6개)

> **총 19개 블로그 포스트** — 배포 대기 중 (7/23 6건 + 7/24 12건 + 7/25 6건 중 001만 고유 썸네일, 002~006은 중복 썸네일 상태)

---

## Quick Status Check

```bash
# 사이트 헬스 체크
curl -s -o /dev/null -w "%{http_code}" https://aikorea24.kr
# → 200 OK

# 썸네일 중복 확인 (7/25)
for f in public/images/2026-07-25-00{2,3,4,5,6}-*/thumbnail.webp; do md5 -q "$f"; done | uniq -c
# → 5 8c93b87916afb3bc1e227bdda928b569 (동일)
```

---

**다음 세션 권장 순서**: 
1. 미커밋 19개 블로그 배포 → 
2. Phase 22 발송 시스템 활성화 → 
3. Phase 23 SNS 자동화 착수