# Continue Here — 2026-09-11 파이프라인 재건 세션 종료 시점

## 상태: pipeline-runner 재건 완료 + 오늘 브리핑/블로그 발행 성공

### 완료된 것 [검증됨]

#### 1. 브리핑 초안 생성 실패 원인 진단 (아침 세션)
- 9/11 08:15 blog-draft "오늘 발행된 브리핑 없음" = 파이프라인 9/10 20:00 미실행이 직접 원인
- 원래 plist(kr.aikorea24.pipeline-runner)는 7/15부터 .disabled — 문서화 안 된 리네임
- 9/10 06:00~20:00까지 매일 06:00/20:00 실행시킨 **대체 트리거 정체는 끝내 미확정** [검증불가]
  - 배제 완료: crontab/at/periodic, tmux/screen/nohup, 5000 scheduler, hermes, cloudflared,
    dashboard 전 계열, TV-show, finnews/mde2/improve-loop, subagent-watchdog, analytics-watchdog,
    셸 스크립트 전역 탐색, unified log(실행 흔적 0건), sleep(9/10 "Entering Sleep" 0건)
  - 9/10 오전 BC 정비 세션(BC-SCHEDULES-DISABLED-2026-09-10.md 08:10)과 사망 시간 겹침 — 그 세션에서 같이 죽었을 가능성만 추정

#### 2. pipeline-runner 재건
- `kr.aikorea24.pipeline-runner.plist` 복원(.disabled→.plist) + bootstrap → 06:00/20:00 정기 실행复活
- plist 무결: 인라인 시크릿 0건, with_notify 호출, 로그 리다이렉트 정상
- 11:07 kickstart 테스트 → 실행 헤더 확인 (pipeline_runner.log line 499514)

#### 3. 타임아웃 버그 수정 [PRODUCTION CODE] (scripts/run_pipeline_with_notify.py, 미커밋)
- `timeout=600` → `3600`: 근거 = 9/10 실측 Step1만 6분39초, D1 재시도 겹치면 10분 초과 (11:17 런 타임아웃 재현)
- `wait_and_retry()`에 `subprocess.TimeoutExpired` try/except 추가: 타임아웃 시 텔레그램 보고 + lock 정리 + 정상 종료 (9/9 23:13 크래시 재발 방지)
- py_compile 통과. 다음 정기 실행 20:00부터 새 코드로 동작

#### 4. 오전 놓친 브리핑 보충 재시도 → 성공
- 구 retry(54586, 구 코드) kill → lock 정리 → 새 retry 스폰(61298, 11:44)
- 12:44 재실행 → **13:14 파이프라인 완료**: 소요 1773초(~30분), 선정 뉴스 6건, 에러 0
- **브리핑 id=284 저장 (date=2026-09-11-1)** — D1 조회로 확인
- 블로그 6개 생성 (2026-09-11-001~006) + 딥링크 연결 + CF Pages 배포 완료 (aikorea24.kr)

### launchd 정지 조치 (사용자 지시, 9/11)
- `kr.aikorea24.outline-generator`: bootout+disable+리네임 — 스크립트 6/26 커밋 fd9dcb6에서 삭제되어 6/27부터 매일 실패
- `kr.aikorea24.keyword-updater`: 동일 — keyword_updater.py가 pipeline 모듈 import 실패 (sys.path에 루트 없음)
- 부작용: threads 키워드 자동갱신 중단 — threads 파이프라인 소재 소진 시 수동 갱신 필요
- `kr.aikorea24.kicker7-publisher`: 9/5부터 disabled 유지 (9/5 triage 조치, 마지막 발행 9/5)

### 남은 작업
1. **with_notify 수정 커밋**: scripts/run_pipeline_with_notify.py (timeout 3600 + TimeoutExpired 처리) — 미커밋 상태
2. **plist 시크릿 스크럽 승인 대기**: 7개 파일 인라인 시크릿 노출
   - outline-generator(.disabled/.bak.telegram/.bak_20260823_131536), keyword-updater(동일 3종), news-unified(.plist/.bak)
   - OPENAI_API_KEY(sk-proj-QyyO...), CLOUDFLARE_API_TOKEN(cfut_fmdy...), CLOUDFLARE_ACCOUNT_ID 등
   - pipeline-runner.plist는 노출 없음
3. 오늘 20:00 정기 파이프라인 실행 확인 (재건 후 첫 정기 실행)
4. 미커밋 정리: scripts/briefing_dedup.json(데이터), 블로그 6개(untracked 발행물 — 커밋 여부는 관례 따라 결정)
5. keyword-updater 부작용: threads 키워드 소진 시 keywords.json 수동 갱신

### Bugs & Issues Discovered (이번 세션)
1. **파이프라인 미실행 (9/10 20:00~9/11 06:00)**
   - 증상: 9/11 아침 blog-draft 공회전 "오늘 발행된 브리핑 없음"
   - 원인: pipeline-runner .disabled(7/15) 이후 대체 트리거가 9/10 사망 (정체 미확정)
   - 해결: plist 복원 재건 — 06:00/20:00 정기 실행 복구
   - 미해결: 대체 트리거 정체 (문서 기록 전무, 재추적 불가 소진)
2. **retry 자식 TimeoutExpired 크래시**
   - 증상: 9/9 23:13 재시도 자식 크래시 (pipeline_retry.log mtime 9/10 00:23)
   - 원인: wait_and_retry 미처리 예외 + timeout=600이 실측 7분+ 보다 짧음
   - 해결: timeout 3600 + try/except 추가 [검증됨: py_compile + 12:44 retry 성공으로 새 코드 동작 확인]
   - 미해결: 없음 (20:00 정기 실행에서 재확인 권장)
3. **outline-generator/keyword-updater 매일 실패**
   - 증상: launchctl exit 2/1, 에러로그 매일 축적
   - 원인: 스크립트 삭제(fd9dcb6) / pipeline 모듈 import 실패
   - 해결: 사용자 지시로 둘 다 정지 (bootout+disable+리네임)
   - 미해결: 없음 (시크릿 노출 파일 정리 대기)
4. **9/9 23:03 비정규 파이프라인 실행**
   - 증상: pipeline_runner.log에 비정규 시간 헤더
   - 원인: 미확정 (with_notify 헤더는 자체 프린트 — 누가 실행했는지 로그상 구분 불가)
   - 미해결: 재발 시 zsh_history/실행 컨텍스트 추적 권장

### Environment Notes
- 머신 로컬 타임 +0700(베트남), 로그 일부 KST(+0900). 로컬 06:00 = KST 08:00
- wrangler 명령 앞 `env -u CLOUDFLARE_API_TOKEN` 필수 (CLOUDFLARE_API_TOKEN이 OAuth 프로필 충돌)
- D1: `env -u ... wrangler d1 execute aikorea24-db --remote --json --command "..."` (GraphQL 토큰과 별개)
- 파이프라인 실측: Step1 뉴스선정 ~7분, 전체 완료 ~30분 (1773초) → timeout 3600이 적정
- retry 함정: schedule_retry 전에 acquire_retry_lock 직접 호출하면 lock 점유로 스폰 안 됨 → lock 지우고 schedule_retry만 호출
- pipeline_retry.log의 재시도 헤더는 자식 stdout이 부모 캡처로 안 써짐 — D1 브리핑 조회로 성공 판정이 정확

### Resume Instructions
```bash
# 1. 20:00 정기 실행 확인 (로컬시간)
launchctl list | grep pipeline-runner
tail -30 /Users/twinssn/Projects/aikorea24/scripts/pipeline_runner.log

# 2. with_notify 수정 커밋 (사용자 확인 후)
cd /Users/twinssn/Projects/aikorea24
git add scripts/run_pipeline_with_notify.py
git commit -m "fix: run_pipeline_with_notify timeout 600→3600 + TimeoutExpired 처리 (retry 크래시 방지)"

# 3. 시크릿 스크럽 (승인 시): 7개 plist에서 인라인 키 제거 → env_boot.sh 경유로 변경
# 4. 브리핑 확인 (D1)
cd /Users/twinssn/Projects/aikorea24 && env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler d1 execute aikorea24-db --remote --json --command "SELECT id, date, created_at FROM briefings ORDER BY id DESC LIMIT 2"
```

### Uncommitted Changes
- `scripts/run_pipeline_with_notify.py` (수정 — 타임아웃 버그 픽스, 커밋 필요)
- `scripts/briefing_dedup.json` (파이프라인 갱신 데이터)
- `src/content/blog/2026-09-11-001~006-*.md` 6개 (신규 발행 블로그)
