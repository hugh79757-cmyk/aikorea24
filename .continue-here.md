# Continue Here — 2026-07-14 Session

## Current Position
Ad-hoc fixes across blog pipeline, dark mode CSS, thread-topic-finder, and deployment config.

## Completed This Session
- **blog-draft 파이프라인 재구축**: 브리핑 기사 직접 조회 → 6개 전부 블로그 생성 + deep_dive_url 연결
- **dark mode CSS 변수 복구**: `src/layouts/Layout.astro`에 `import '../styles/global.css'` 추가, `astro.config.mjs`에 `applyBaseStyles: false`
- **thread-topic-finder 복구**: plist 경로 수정 + wrangler CLI 전환 → 5개 글감 생성 확인
- **D1 쿼리 전환**: blog-draft, thread-topic-finder → REST API에서 wrangler CLI로 전환 (OAuth profile)
- **deep_dive_url 404 수정**: slug lowercase 처리 (Astro 정규화와 일치)
- **auto_thumbnail 비활성화**: Pexels 썸네일 slug 불일치로 사용 중단, TECH.md 업데이트
- **Triage 문서**: auto-thumbnail-deactivation, blog-draft-threads-fix 추가

## Decisions
- 심층글(auto_deep_article) 비활성화 유지 — blog-draft가 대체
- auto_thumbnail(Pexels) 비활성화 — slug 불일치 + 저작권 대비 효과 미미
- D1 쿼리는 wrangler CLI 통일 (token 만료 문제 해결)
- Threads는 계속 2시간 간격 자동 발행 (publisher 별도)

## Blockers
- `scripts/run_pipeline_with_notify.py`가 plist XML로 덮어써졌다가 복구됨 — 내용 확인 필요

## Resume Instructions
```bash
cd /Users/twinssn/Projects/aikorea24
git diff --stat
# Check run_pipeline_with_notify.py integrity first
# Then commit if needed
```
