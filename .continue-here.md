# .continue-here.md — Session Handoff

## Current Position
- **Milestone**: v1.0
- **Phase**: 13 (card-separation-fix) — **COMPLETE**
- **Sessions complete**: All 13 pipeline phases complete

## Phase 13 Changes
### 13-01: Persistent Failed Article Tracking
- `scripts/threads/failed_articles.py` — new module (load/save/is_failed/clear)
- `scripts/threads/main_v3.py` — integration (startup load, write failure save, exclude_ids)
- Status: **Complete** — article 38290 infinite retry loop resolved

### 13-02: Validation Fixes
- `pipeline/threads/validator.py` — `sentence_enders` added `\u3002` (Chinese period `。`)
- `pipeline/threads/writer.py` — `_repair_truncated_cards()` enders added `\u3002`
- `pipeline/threads/writer.py` — `_remove_duplicate_links()` new + `parse_cards()` integration
- Status: **Complete** — MiMo `。` false positives solved, duplicate links removed

### 13-03: Repair Logic Strengthening
- `pipeline/threads/writer.py` — `_repair_truncated_cards()` backward pass (last card incomplete → merge into prev)
- `tests/test_writer.py` — `TestRepairTruncatedCards` (6 new tests)
- Status: **Complete** — card truncation repair covers all positions

## Unresolved
- **Hook 문장 종결 과다** (YouTube article case): `\n\n` split too late → hook contains multiple sentences. Structural limitation of `\n\n` approach. Requires separate phase.
- **E2E dry run**: Aborted due to time. Unit tests: 287/287 pass. Pipeline-level verification deferred.

## Key Decisions
- `\n\n` split 방식 유지, `_repair_truncated_cards()` 강화로 보완
- `---` 방식 복원 거부 (이전 세션에서 카드 수 불일치로 많은 실패)
- Chinese period `。` → sentence_enders에 추가 (MiMo v2.5 특성)

## Next
All pipeline refactoring phases complete (13/13). Options:
- Phase 14: Hook 문장 종결 과다 (구조적 문제)
- Phase 15: E2E 드라이런 배치 실행 (nightly, headless)
