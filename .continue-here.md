## Session Handoff — 2026-07-02 (세션 시작 후 계속)

### Phase Position

| Phase | Status | Last Activity |
|-------|--------|---------------|
| Milestone v1.0 (5개 Phase) | ✅ Complete | Phase 5-04: auto_email_sender template 1 복원 |
| Tools 문서화 | ✅ Complete | AGENTS.md + TECH.md Section 10 업데이트 |

### Completed Work

1. **tools_collector.py import 순서 버그 수정**
   - `from pipeline.infra.logger`가 `sys.path.insert`보다 먼저 실행되어 launchd 환경에서 `ModuleNotFoundError` 발생
   - `_PROJECT_DIR = os.path.dirname(__file__)` 기반 path를 pipeline import 전에 먼저 삽입하도록 재배열
   - launchd `kr.aikorea24.tools-collector` (매일 06:00) LastExitStatus=256 원인 해결

2. **Tools Collection Pipeline 문서화**
   - `AGENTS.md`: Tools Collection Pipeline 참조 블록 추가 (향후 세션 LLM이 인지 가능)
   - `docs/TECH.md` Section 10 신규: 처리 흐름, 실행 주기, 5개 수집 소스, 메타데이터 생성, im-not-ai 3단계 검증, plist 설정, 중복 방지, 함수 위치

3. **TECH.md Section 1** 전체 파이프라인 요약에 별도 파이프라인(Tools/Briefing) 표기 추가

### Decisions Made

| Decision | Rationale |
|----------|-----------|
| tools_collector.py `__file__` 기반 PROJECT_DIR 사용 | launchd 환경에서 `project_root()` import보다 먼저 path 확보 필요 |
| Tools 수집 파이프라인을 TECH.md Section 10에 별도 문서화 | Threads/Briefing과 독립된 시스템이므로 별도 섹션 |

### 가시적 Issues

| Issue | Detail |
|-------|--------|
| git 작업트리 복잡 | uncommitted 수정 20+ / 삭제 12 / untracked 60+ (파이프라인 산출물) |
| Threads container timeout | `graph.threads.net` 30s timeout 간헐 발생 (재시도로 복구) |
| DeepSeek writer 카드 수 불일치 | 7개 생성 → 검증 실패 → fallback (2회 중 2회 실패 후 성공) |

### Resume Instructions

```bash
# 다음 세션 시작 시
git log --oneline -5                     # 최근 커밋 확인
cat .continue-here.md                    # 이 파일 읽기
cat AGENTS.md                            # 구조/규칙 확인 (tools 파이프라인 참조 포함)
cat docs/TECH.md | head -10              # 섹션 구조 확인
```

### Next Actions (제안)

1. 작업트리 정리 (uncommitted 산출물 커밋 또는 스테이징)
2. Threads publisher container timeout 30→45s 검토
3. DeepSeek writer 카드 수 안정화 (writer 프롬프트 튜닝)
