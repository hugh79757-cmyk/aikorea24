# Handoff — 2026-09-02 세션 (Phase 38 완료)

**작성**: 2026-09-02 23:00 KST
**마지막 커밋**: 8d40c0e (Phase 38 문서)

## Current Position

- **Phase 38 — Threads 자가개선 (Self-Improvement) 루프**: ✅ **완료** (3/3 plans, 커밋 227bbcc + 8d40c0e)
- 프로젝트 전체: 38/38 phases, 64/64 plans, **100%** (STATE.md 갱신 완료)
- 다음 활성 phase: 없음 — 신규 phase 필요 시 `/gsd-new-milestone` 또는 ad-hoc quick task

## Completed This Session (2026-09-01/02 걸친 세션)

### Phase 38 구현 (커밋 227bbcc)
- **38-01 측정**: `scripts/threads/performance_log.py` 신규 — `record_publish()` (root_id/posted_at/format/article_id/title/source/topic_tags/metrics:null 스키마, atomic save). main_v3.py 발행 성공 블록 +16줄 append-only 연결 (try/except, 발행 흐름 무영향)
- **38-02 수집/분석**: `collect_insights(days=2)` — `GET /v1.0/{root_id}/insights?metric=views,likes,replies,reposts,quotes` (urllib, publisher.load_env 토큰). **net_replies 모델 정정**: insights `replies`는 자기 카드 체인+🔗 링크 답글 포함 → root 직접 답글 중 `username != 'aikorea24'` 직접 카운트 (`_external_reply_count()`), edge 실패 시 `replies-1` fallback. `analyze(30일, min_posts=30)` — insights_report.json (by_format/by_topic/by_slot/by_source + top_topics 3, 미만 시 생성 없음). `insights_collector.py` wrapper. launchd `kr.aikorea24.threads-insights` 일 06:10 등록 완료
- **38-03 주입**: `pipeline/threads/pitch.py` `_top_topics_hint()` — report 존재+top_topics 있을 때만 "📌 최근 30일 반응 상위 토픽 (참고용, 강제 아님): ..." 주입 3곳 (get_pitches 본/fallback, _regenerate_pitch_from_crawl). report 없으면 `''` — 기존 동작 유지

### 검증 결과
- test_performance_log.py 5/5 (스키마/지속성/net_replies/error-계속/문턱 29↔30/by_topic)
- 라이브 수집: 09-01 발행 3건 views 311/194/77 — 프로브 실측 일치, net_replies 0/0/0 (전부 자기 체인)
- 회귀 0: 전체 스uite 수정 전/후 동일 473 passed / 16 failed (16 = 기존 존재, stash 비교 확인)
- pitch 테스트 43 passed. leak 방어 오탐 0. report 삭제 → 주입 자동 무력화 확인

### 기존 세션 작업 (참조: b2/b3/b4 압축 블록)
- D1 REST API 500/7500 장애 진단·복구 (폴백 커밋 0ceb32c, 17:05 자체 복구)
- main_v3 telegram 알림 추가 (커밋 84850ac) — 기사 로드 5회 소진 시 알림
- TAAFT 스크래퍼 추가 (커밋 6d9e1bd)
- Tools 페이지 카테고리 필터 19개 MD 수정 (배포 완료, **커밋은 이번 WIP에 포함**)

## Remaining Work

1. **Phase 38 관찰 포인트** (수동 확인, 자동화 완료 상태):
   - 09-03 00:00 발행 로그에 `📊 성과 로그 기록 완료` 라인 있는지
   - 09-03 06:10 `scripts/threads/logs/insights_collector.log` 첫 launchd 자동 실행
   - ~09-13: 30 posts 축적 → 첫 insights_report.json + 피치 주입 시작 (현재 3/30)
2. **미해결 minor**: tools 페이지 `data-review={0}` 하드코딩 → 인기순 정렬 무효 (필요 시 수정)
3. **잠재 갭**: threads-token-refresh launchd (일 00:30) — 토큰 만료 2026-10-17 전 갱신 관찰

## Decisions (이번 세션)

1. **net_replies 산출 모델 정정**: PLAN의 "replies-1 차감" 폐기 → root 직접 답글 외부 카운트. 근거: 라이브 검증 — 18:00 포스트 replies=5 전부 자기 답글 (링크1+카드체인4), 외부 0
2. **수집 시각 06:10** (PLAN의 06:00에서 +10분): threads-publisher(06:00)/tools-collector(06:00)와 분리
3. **주입은 user 프롬프트 말미 append** — 시스템 프롬프트 변경 0, 배치당 1회 산출
4. 부트스트랩 보호: min_posts=30 미만 시 분석/주입 스킵 (graceful degradation)

## Bugs & Issues Discovered

1. **insights replies 지표 왜곡**
   - 증상: replies=5인데 root replies edge엔 2건만, 전부 자기 답글
   - 원인: `thread_replies` metric이 스레드 전체 답글(자기 카드 체인 포함) 집계
   - 해결: `_external_reply_count()` root 직접 답글 username 기반 외부 카운트로 교체 (해결 완료)
   - 미해결: 없음
2. **기존 테스트 16 실패** (본 세션 발견 아님, 기준선)
   - 증상: tests/ 스uite 16 failed (예: test_failed_articles retention, test_write_thread_validation hook entity)
   - 원인: 미조사 — Phase 38 수정 전후 동일하므로 회귀 아님
   - 해결: 미적용
   - 미해결: 예 — 별도 triage 대상. 다음 세션에 `pytest tests/ -q` 실패 목록으로 분류 권장

## Environment Notes

- launchd 3종: threads-publisher (2h 간격 짝수시), threads-insights (06:10 — **신규**), threads-token-refresh (00:30, 재로드 후 미실행 — 잠재 갭)
- Threads 토큰: EnvConfig 경유만 유효 (만료 2026-10-17). `.env` 수동 파싱 → code 190 재발
- D1: REST API 09-01 장애는 복구됨. db_reader.py에 HTTP 폴백 내장 (aikorea24.kr/api/articles/pool, trailing slash 필수)
- wrangler: 항상 `env -u CLOUDFLARE_API_TOKEN` (에이전트 세션 토큰 무효)
- 쓰레드 발행: 12슬롯/일 2h 간격 (08-28 핸드오프의 "6회/일"은 구버전 — 현재 12슬롯 정상)

## Resume Instructions

```bash
# 상태 복원
cat .continue-here.md && cat .planning/STATE.md | head -40
git log --oneline -5

# Phase 38 관찰 (09-03 아침)
tail -20 scripts/threads/logs/2026-09-03.log        # 📊 성과 로그 기록 완료 확인
tail -20 scripts/threads/logs/insights_collector.log  # launchd 첫 실행 확인
.venv/bin/python3 scripts/threads/test_performance_log.py  # 5/5 재확인

# 새 작업 시작
/gsd-resume-work   # 또는 신규 phase: /gsd-new-milestone
```

## Uncommitted Changes (pause 시점)

이 WIP 커밋에 포함 (workflow step 3):
- `src/content/tools/*.md` 19개 — 카테고리 필터 수정 (배포 완료분, 라이브 검증됨)
- `scripts/threads/threads-publisher.plist.template` (+6줄), `scripts/briefing_dedup.json` (런타임)

제외 (런타임 산출물 — 커밋 불필요): `scripts/scripts/threads/logs/drafts/**` (경로 중복 버그로 생성된 미스), `functions/` (빈 디렉토리 추정), blog MD 초안들, 새 tool MD 11개 (다음 tools-collector 자동 push 예정이나 수동 커밋해도 무방)
