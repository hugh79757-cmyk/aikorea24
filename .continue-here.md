# Continue Here — 2026-07-15 23:30 KST

## Current Position
- **Status**: ✅ 모든 작업 완료, 6 commits
- **Phase**: Ad-hoc (파이프라인 복구 + Threads validation 완화)

## Completed This Session
1. **fix(threads): validator/prompt 완화** — writer.py 카드5 강제 제거, validator body min 50→30자, 문장 완성 논블로킹
2. **fix(pipeline): blog-draft 직접 호출** — launchd 20:15 미실행 문제: pipeline 성공 시 blog_draft_generator.py를 subprocess로 직접 실행
3. **fix(blog): description 서론 프리픽스 제거** — `blog_draft_generator.py` description 150→250자 + `서론:` 프리픽스 자동 제거
4. **feat(blog): 6 new posts (2026-07-15)** — 002~007 블로그 포스트
5. **docs: triage INDEX + CHANGES.md 정리** — INDEX/CHANGES 업데이트, old triage 삭제
6. **fix(env): entry points EnvConfig 로딩 추가** — `pipeline/__main__.py` + `blog_draft_generator.py`에 `.env` 로딩

## Decisions
- **Threads validator 완화**: DeepSeek가 자연스럽게 쓰는 문장을 validator가 막지 않도록 — sentence enders non-blocking, body card 30자로 완화
- **blog-draft 직접 호출**: launchd `StartCalendarInterval` sleep skip에 대한 근본 해결 — pipeline-runner 완료 직후 직접 호출, launchd는 backup으로 유지
- **EnvConfig 패턴 통일**: 모든 진입점(`pipeline/__main__.py`, `scripts/` 아래 모든 스크립트)에 `EnvConfig().load_to_environ()` 첫 import로 배치

## Blockers
- None.

## Bugs & Issues Discovered
1. **저녁 blog-draft launchd 20:15 skip**
   - 증상: 저녁 6개 블로그 포스트 미발행 (06:15는 정상, 20:15만 누락)
   - 원인: macOS Sleep 상태에서 launchd `StartCalendarInterval` skip
   - 해결책: `run_pipeline_with_notify.py`가 pipeline 성공 직후 `blog_draft_generator.py` 직접 호출
   - 미해결: 아니요 — 해결됨

2. **Threads 발행 실패 (5회 시도 모두 실패)**
   - 증상: DeepSeek/validator false positive로 발행 차단
   - 원인: Card 5 강제 open question + sentence enders 검증 오탐 + body card 50자 hard limit
   - 해결책: validator 완화 (body 30자, sentence enders non-blocking), writer Card 5 constraint 제거
   - 미해결: 아니요 — 해결됨

3. **pipeline/__main__.py .env 미로딩**
   - 증상: `python -m pipeline` 실행 시 env var 없이 d1_client import → D1 auth 실패
   - 원인: 다른 진입점들은 모두 `EnvConfig().load_to_environ()`을 가지고 있었으나 `__main__.py`는 누락
   - 해결책: import 직전 `EnvConfig().load_to_environ()` 추가
   - 미해결: 아니요 — 해결됨

4. **blog_draft_generator.py .env 미로딩**
   - 증상: launchd 환경에서 실행 시 DeepSeek/DB 연결 실패
   - 원인: `ENV_PATH = ...` 변수만 선언하고 실제 로딩 코드 누락
   - 해결책: `EnvConfig().load_to_environ()` 추가
   - 미해결: 아니요 — 해결됨

## Resume Instructions
```bash
cd /Users/twinssn/Projects/aikorea24
git log --oneline -6
# 최근 커밋: fdfae4c (env 로딩), 7a04f95 (docs), b18f2ad (blog 6편)...
```

## Uncommitted Changes
- None (all 6 commits done, working tree clean).

## Git Log (unpushed)
```
fdfae4c fix(env): pipeline __main__ + blog-draft에 EnvConfig 로딩 추가
7a04f95 docs: triage INDEX + CHANGES.md 업데이트 + old triage 정리
b18f2ad feat(blog): 6 new posts (2026-07-15)
e76fb45 fix(blog): description 서론 프리픽스 제거 + 250자 확장
4124a7b fix(pipeline): blog-draft 직접 호출 — launchd 20:15 누락 방지
65bd65e fix(threads): validator/prompt 완화 — 카드 길이 30자 + 문장 완성 논블로킹
fdd14ad docs: continue-here 텔레그램 2단계 메시지 + git log 최신화
6cf2ed0 docs: triage telegram 2단계 메시지 추가 + INDEX 업데이트
93f7b5c feat(notify): 텔레그램 2단계 메시지 구조화
...
```
