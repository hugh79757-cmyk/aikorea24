# .continue-here.md — 작업 상태 기록

작성일: 2026-08-13 (목) 13:10 KST

---

## 현재 상태

**작업 유형**: 블로그 발행 파이프라인 LLM 체인 전환 + 과거 상태 원복

**목표**: 모든 LLM 호출을 무료 LLM 폴백 체인(`model_router.chat_completion(model_override=None)`)으로 통일하고, 8월 10일 정상 발행 시점(`6e16127`)의 코드 구조로 복원.

**진행률**: 핵심 작업 완료. 일부 pre-existing 문제(D1 저장 bug, 기존 영어 DB 레코드)는 별도 후속 작업 필요.

---

## 완료 작업

### 1. 코드 원복 (6e16127 기준)
- `scripts/blog_draft_generator.py` → 6e16127 상태로 복원 (단순 프롬프트, 단순 기사 조립: title+source+description)
- `scripts/auto_briefing.py` → 6e16127 상태로 복원
- `api_test/news_collector.py` → 6e16127 상태로 복원
- `scripts/auto_thumbnail.py` → 6e16127 상태로 복원

### 2. LLM 무료 체인 전환 (하드코딩 직접 호출 → model_router)
- **`auto_briefing.py`**: MiMo 직접 호출(requests.post) 제거 → `model_router.chat_completion(model_override=None)`
- **`news_collector.py`**: GPT-4o-mini 직접 호출(openai.OpenAI) 제거 → `model_router.chat_completion(model_override=None)` + sys.path 수정
- **`auto_thumbnail.py`**: DeepSeek 직접 호출(OpenAI+deepseek.com) 제거 → `model_router.chat_completion(model_override=None)` + OpenAI import 제거
- **`blog_draft_generator.py`**: 6e16127 상태에서 이미 `model_router.chat_completion()` 사용. model_router가 무료 체인 버전이므로 자동 무료 체인 사용. 의도적 "무료 체인만 사용" 상태로 유지 (model_override 미지정 → None)
- **`model_router.py`**: 무료 LLM 폴백 체인 유지. GPT-4o-mini 제거됨. (커밋 5b57349에 포함)

### 3. 테스트 결과 (모두 성공)
- **news_collector.py translate_to_korean**: Gemini-3.1-flash-lite 무료 체인 정상 번역 ✅
- **news_collector.py batch_translate**: 202건 번역 성공 ✅
- **auto_briefing.py**: Gemini 무료 체인으로 브리핑 코멘트 생성 ✅
- **blog_draft_generator.py**: Gemini 무료 체인으로 블로그 글 생성 (영문 원문→한국어 제목, 2226자) ✅
- **deep_dive_url 연결**: save_draft → update_deep_dive_url → 브리핑-블로그 연결 성공 ✅
- **auto_thumbnail.py**: Gemini 무료 체인으로 키워드 추출, Pexels 썸네일 생성 ✅
- **블로그 검증**: 검증 통과, 썸네일 품질 체크리스트 통과 ✅
- **배포**: Cloudflare Pages 배포 성공 ✅
- **이메일 발송**: auto_email_sender.py 이메일 발송 성공 (HTTP 201) ✅

### 4. 커밋
- `5b57349 feat: 6e16127 상태로 복원 + LLM 무료 체인 전환` (5 files: blog_draft_generator.py, auto_briefing.py, news_collector.py, auto_thumbnail.py, model_router.py)

---

## 남은 작업 / 미해결 문제

### A. news_collector.py save_to_d1 함수 D1 저장 실패 (pre-existing bug)
- **증상**: news_collector.py 실행 시 "번역 완료: 202건" 이후 "[저장] D1 저장 중..."에서 "배치 N 실패" 출력, "신규: 0건 | 중복 스킵: 0건", "완료! 총 0건 저장"
- **원인**: 6e16127 코드의 save_to_d1이 wrangler 반환코드만 체크. INSERT OR IGNORE에서 중복 발생 시 wrangler가 non-zero exit code 반환 → save_to_d1이 실패 처리. 실제로는 일부만 저장 성공했을 수 있으나 코드 로직이 전체 실패로 처리.
- **확인 테스트**: 수동 INSERT는 정상 동작 (`wrangler d1 execute --file`). 뉴스_collector.py의 save_to_d1 내부 wrangler 호출 결과와 차이.
- **해결책**: save_to_d1 함수를 wrangler stdout JSON 파싱 기반으로 성공 판단하도록 수정. 반환코드 의존 제거.
- **상태**: 미해결. LLM 체인 변경과 무관한 pre-existing bug.

### B. 기존 영어 DB title 레코드 (8/12~13 등)
- **증상**: D1 news 테이블의 일부 레코드에서 `title` = 영어, `original_title` = 영어 (예: "The Pixel Buds Pro 2 just got better at blocking noise...").
- **원인**: ① `news_collector.py`가 자동 실행되지 않음 (cron/launchd 미설정, 수동 실행만 가능). ② 과거 번역 시 `OPENAI_KEY` 없음 → `batch_translate()`가 "OPENAI_API_KEY 없음 - 번역 건너뜀"으로 번역 스킵. ③ 결과: 번역 안 된 영어 title이 DB에 저장됨. ④ 이미 INSERT된 레코드는 `save_to_d1()`의 INSERT OR IGNORE로 갱신 안 됨.
- **확인**: news_collector.py의 batch_translate는 현재 무료 체인(model_router)으로 정상 동작. 그러나 `news_collector.py` 자체가 수동 실행만 가능. 자동 실행 파이프라인 미복원.
- **해결책 (선택지)**:
  1. `news_collector.py`의 batch_translate로 기존 영어 title을 번역 후 UPDATE하는 별도 스크립트 작성 (무료 체인 사용)
  2. `news_collector.py` 자동 실행 파이프라인(cron/launchd) 복원 → 이후 신규 수집 뉴스는 자동 번역
- **상태**: 미해결.

### C. 블로그_draft_generator.py의 "초안 큐" 개념 미구현
- 28-05에서 "검수 실패 시 초안 큐 보관" 개념이 있었으나, 6e16127 원복으로 검수 게이트 자체가 제거됨.
- 현재 상태: 검수 게이트 없음 → 블로그 글 정상 생성. "초안 큐"는 관심 대상 아님.
- **상태**: 해당 없음 (검수 게이트 제거됨).

---

## 결정 사항

1. **LLM 호출 100% model_router 경유로 통일**: auto_briefing, news_collector, auto_thumbnail, blog_draft_generator 모두 `model_router.chat_completion(model_override=None)` 사용. 하드코딩 직접 호출 완전 제거.
2. **model_router 무료 체인 유지**: GPT-4o-mini 제거 상태 유지. "무료 체인만 사용" 의도적 설계.
3. **코드 구조는 6e16127 기준으로 복원**: 블로그_draft_generator의 프롬프트는 단순 구조(검수 게이트 없음). 28-05의 복잡한 프롬프트/검수 게이트는 제거됨.
4. **D1 저장 bug은 LLM 체인 변경과 별개 이슈로 분류**: 번역 파이프라인 정상 동작 확인 후, 저장 bug은 별도 처리한다.

---

## 작업 로그

**세션 시작**:
- 목표: "다음 발행부터 모두 정상 작동. 그치? 이미 영어로 저장된 것은 그냥 놔둔다."
- 6e16127 상태 파악: blog_draft_generator.py(단순 프롬프트), auto_briefing.py(MiMo 직접 호출), news_collector.py(GPT-4o-mini 직접 호출), auto_thumbnail.py(DeepSeek 직접 호출)
- 6e16127 상태로 원복 (git checkout 6e16127)
- LLM 체인 전환: 모든 하드코딩 직접 호출을 model_router.chat_completion(model_override=None)으로 교체
- 테스트: 번역 202건 성공, 브리핑 생성, 블로그 글 생성, deep_dive_url 연결, 썸네일, 배포, 이메일 발송 모두 성공
- 커밋: 5b57349

---

## 환경/특이사항

- wrangler D1 execute 시 `--remote` 플래그 사용. `-u CLOUDFLARE_API_TOKEN`으로 환경변수 해제하고 `/opt/homebrew/bin/wrangler` 직접 호출 (auth profile: hugh79757)
- news_collector.py 실행 시 stdout이 버퍼링되어 실시간 로그 안 보임. `python3 -u` 옵션 필요.
- 가상환경: `#!/Users/twinssn/Projects/aikorea24/.venv/bin/python3` (news_collector.py shebang)
- sys.path: 각 스크립트마다 경로 설정 있음. news_collector.py의 경우 PROJECT_DIR 미포함으로 인해 import 실패했던 이슈 있었음 → sys.path.insert(0, PROJECT_DIR) 추가로 수정.

---

## 재개 시 참고

1. 저장 bug fix 필요 시: `api_test/news_collector.py`의 `save_to_d1()` 함수 확인. wrangler stdout JSON 파싱 방식으로 변경.
2. 기존 영어 DB title 처리 필요 시: `news_collector.py`의 `batch_translate()`로 번역 후 UPDATE하는 스크립트 작성, 또는 `news_collector.py` 자동 실행 파이프라인 복원.
3. 전체 파이프라인 재테스트 시: `python3 -u api_test/news_collector.py` → `python3 -u scripts/auto_briefing.py` → `python3 -u scripts/blog_draft_generator.py` → `python3 -u scripts/auto_email_sender.py` 순서로 실행. (news_collector.py는 D1 저장 bug으로 인해 저장 단계 실패할 수 있음 — 로그 확인)
4. git 상태: 이 세션에서 수정한 파일들 외에 이전 세션에서 수정한 파일들도 uncommitted 상태로 남아 있음. 필요 시 정리.

---

## Git 상태 스냅샷 (작업 종료 시점)

```
M scripts/blog_draft_generator.py
M scripts/auto_briefing.py
M api_test/news_collector.py
M scripts/auto_thumbnail.py
M scripts/threads/v3/model_router.py
M src/components/home/BriefingSection.astro
M src/pages/api/briefing/send-email.ts
M src/pages/briefing/[date].astro
M .planning/triage/INDEX.md
M pipeline/threads/pitch_evaluator.py
M pipeline/threads/writer.py
M scripts/briefing_dedup.json
M scripts/threads/ARCHITECTURE.md
M scripts/threads/publisher.py
?? .planning/phases/phase-28-blog-thumbnail-publish-improve/
?? .planning/triage/20260809--vectorize-threads-json.md
?? .planning/triage/20260812--home-briefing-email-english-description-removed.md
?? .planning/triage/20260812--threads-rhythm-linebreak-fix.md
?? scripts/thread_topics/topics/20260802~20260810/
?? src/content/blog/2026-08-01-* ~ src/content/blog/2026-08-13-001-*
```

- 커밋됨: 5b57349
- 미커밋: 위 목록

---

**다음 세션에서의 첫 액션 추천**: D1 저장 bug fix (save_to_d1 함수) 또는 기존 영어 DB title 레코드 번역 UPDATE 스크립트 작성. 둘 중 우선순위는 강제성 높은 저장 bug fix부터.
