# Continue Here — 2026-07-20 세션

## Current Position

| Phase | Status |
|-------|--------|
| Phase 27 | ✅ 완료 (validation logging) |
| Triage: pipeline-path-bug | ✅ 수정 + 기록 완료 |

**Milestone**: `v2.0-complete` — all 26 phases / 49 plans complete (100%)

---

## Completed This Session

### 1. Pipeline 경로 버그 발견 및 수정
- `run_pipeline_with_notify.py` — `SCRIPTS_DIR`이 `scripts/scripts/`로 중복되어 `run_pipeline.py` + `blog_draft_generator.py` 실행 불가
- **원인**: 7/15 Telegram 중앙화 리팩터링(commit `4124a7b`)에서 하드코딩 경로를 동적 경로로 변경할 때 `__file__`이 이미 `scripts/` 디렉토리임을 간과
- **수정**: `SCRIPTS_DIR = os.path.join(_PROJECT_DIR, 'scripts')`로 정정
- **영향**: 7/19, 7/20 블로그 0건 / 브리핑 미발행. Threads는 별도 launchd로 정상.

### 2. 대규모 리팩터 일괄 커밋
- **Telegram 중앙화**: 6개 파일에 분산된 `send_telegram()` → `pipeline/infra/telegram.py` (신규)
- **retry.py 삭제**: `@retry` 데코레이터 레거시 제거 (d1_query/pexels 불필요 중복 호출)
- **slugify 중복 제거**: `tools_collector.py` → `pipeline.instagram.utils` 공유 함수 사용
- **d1_query 중복 제거**: `auto_news_selector.py` 자체 구현 → `d1_client.d1_query`로 대체
- `run_pipeline_with_notify.py`: 하드코딩 `PROJECT_DIR` → 동적 경로 (with bug fix)

### 3. 콘텐츠 배포
- 12개 블로그 포스트 (2026-07-18) 커밋 + Cloudflare Pages 배포 완료
- `briefing_dedup.json` 7/18 데이터 업데이트
- AI 툴 7개 추가
- Thread topics 3일치 (7/18, 7/19, 7/20)
- **Blockers 해소**: STATE.md의 "2026-07-16 블로그 6건 미배포" — 수동 배포로 모두 해결

### 4. triage 기록
- `.planning/triage/20260720--pipeline-path-bug.md` — 경로 버그 전말 기록
- INDEX.md 업데이트

---

## Remaining Work

- **없음** — 이번 세션 목표 모두 달성. 내일부터 파이프라인 정상화 예정.

---

## Decisions

- `run_pipeline_with_notify.py`의 `SCRIPTS_DIR`은 `_PROJECT_DIR`(루트) 기준으로 계산 (`os.path.join(_PROJECT_DIR, 'scripts')`)
- Triage 항목은 항상 INDEX.md 첫 줄에 추가 (최신순)

---

## Blockers

- **없음** — 7/16 블로그 미배포 해소, 경로 버그 수정 완료

---

## Bugs & Issues Discovered

| 증상 | 원인 | 해결책 | 상태 |
|------|------|--------|------|
| launchd pipeline 실행 시 `scripts/scripts/run_pipeline.py: No such file or directory` | Telegram 중앙화 리팩터 중 `SCRIPTS_DIR` 계산 실수 | `os.path.join(_PROJECT_DIR, 'scripts')`로 정정 | ✅ 해결 |
| 7/19, 7/20 블로그 발행 0건 | 상동 — pipeline 전체가 `FileNotFoundError`로 실패 | 수정 + 12개 블로그 수동 배포 완료 | ✅ 해결 |

---

## Environment Notes

- `run_pipeline_with_notify.py`는 launchd `kr.aikorea24.pipeline-runner`로 실행됨
- Threads 파이프라인은 별도 launchd `kr.aikorea24.threads` (06:32 KST) — 영향 없었음
- `wrangler pages deploy`는 `env -u CLOUDFLARE_API_TOKEN` + `/opt/homebrew/bin/wrangler`로 auth profile 사용
- Python 3.14 사용 중

---

## Resume Instructions

1. `git log --oneline -3` 확인
2. `scripts/run_pipeline_with_notify.py` 경로 검증: `python3 -c "import os; print(os.path.join(os.path.dirname(os.path.dirname(os.path.abspath('scripts/run_pipeline_with_notify.py'))), 'scripts'))"`
3. launchd 로그 확인: `log show --predicate 'subsystem == "kr.aikorea24.pipeline-runner"' --last 1h`
4. 내일 아침 파이프라인 정상 실행 여부 확인

---

## Uncommitted Changes

- 없음 — 모두 커밋/푸시 완료
