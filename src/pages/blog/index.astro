---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const categories = [...new Set(posts.map(p => p.data.category))];
---
<Layout title="블로그">
  <section class="bg-gradient-to-r from-primary to-secondary py-12 px-4">
    <div class="max-w-5xl mx-auto text-center text-white">
      <h1 class="text-3xl md:text-4xl font-bold mb-2">블로그</h1>
      <p class="opacity-90">AI 활용 팁, 바이브코딩 가이드, 최신 트렌드</p>
    </div>
  </section>

  <section class="max-w-5xl mx-auto px-4 py-8">
    <div class="flex gap-2 mb-8 flex-wrap">
      <a href="/blog" class="px-4 py-2 bg-primary text-white text-sm rounded-full">전체</a>
      {categories.map(cat => (
        <a href={`/blog/category/${cat}`} class="px-4 py-2 bg-gray-100 text-gray-700 text-sm rounded-full hover:bg-gray-200">{cat}</a>
      ))}
    </div>

    {posts.length === 0 ? (
      <div class="text-center py-20">
        <p class="text-6xl mb-4">📝</p>
        <p class="text-gray-500 text-lg">아직 작성된 글이 없습니다</p>
        <p class="text-gray-400 text-sm mt-2">첫 번째 글을 작성해보세요!</p>
      </div>
    ) : (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {posts.map(post => (
          <a href={`/blog/${post.id}`} class="group block bg-white border border-gray-200 rounded-xl overflow-hidden hover:shadow-lg transition">
            <div class="bg-gradient-to-br from-primary/10 to-secondary/10 h-40 flex items-center justify-center">
              <span class="text-5xl">{
                post.data.category === 'AI 도구' ? '🛠️' :
                post.data.category === 'AI 강좌' ? '📚' :
                post.data.category === 'AI 뉴스' ? '📰' :
                post.data.category === 'AI 지원사업' ? '💰' :
                post.data.category === 'AI 체험' ? '🧪' : '✍️'
              }</span>
            </div>
            <div class="p-5">
              <div class="flex items-center gap-2 mb-2">
                <span class="text-xs bg-blue-100 text-primary px-2 py-1 rounded">{post.data.category}</span>
                <span class="text-xs text-gray-400">{post.data.date.toLocaleDateString('ko-KR')}</span>
              </div>
              <h2 class="font-bold text-lg group-hover:text-primary transition">{post.data.title}</h2>
              <p class="text-sm text-gray-500 mt-2 line-clamp-2">{post.data.description}</p>
              <div class="flex gap-1 mt-3 flex-wrap">
                {post.data.tags.map(tag => (
                  <span class="text-xs text-gray-400">#{tag}</span>
                ))}
              </div>
            </div>
          </a>
        ))}
      </div>
    )}
  </section>
</Layout>
