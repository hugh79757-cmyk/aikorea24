---
interface Props { items: any[] }
const { items } = Astro.props;
---
<section class="relative z-10 px-4 pb-20">
  <div class="max-w-6xl mx-auto">
    <!-- 헤더 -->
    <div class="flex items-center gap-3 mb-8">
      <div class="w-8 h-8 bg-orange-500/20 rounded-lg flex items-center justify-center text-sm">🚀</div>
      <h2 class="text-xl font-bold">모두의 창업 프로젝트</h2>
      <span class="text-xs bg-orange-500/20 text-orange-400 dark:text-orange-300 px-2 py-0.5 rounded-full border border-orange-500/20">2026 신규</span>
      <div class="flex-1 h-px bg-gradient-to-r from-white/10 to-transparent ml-4"></div>
    </div>

    <!-- 소개 카드 -->
    <div class="bg-gradient-to-br from-orange-50 to-amber-50 dark:from-orange-900/20 dark:to-amber-900/10 backdrop-blur-md border border-orange-200 dark:border-orange-500/20 rounded-2xl p-6 mb-6">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
        <div>
          <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-2">아이디어 하나로, 최대 10억원 지원</h3>
          <p class="text-sm text-gray-600 dark:text-gray-400 leading-relaxed">
            중소벤처기업부가 전국 5,000명의 창업 인재를 발굴합니다.<br class="hidden md:block" />
            서류 대신 아이디어로 평가 · AI 솔루션 활용 지원 · 단계별 사업화 자금 연계
          </p>
        </div>
        <div class="flex-shrink-0 flex flex-col sm:flex-row gap-2">
          <a href="https://www.k-startup.go.kr/" target="_blank" rel="noopener"
             class="inline-flex items-center gap-2 bg-orange-500 hover:bg-orange-600 text-white font-semibold px-5 py-2.5 rounded-xl transition-colors text-sm">
            신청 포털 →
          </a>
          <a href="https://www.mss.go.kr/site/smba/ex/bbs/View.do?cbIdx=86&bcIdx=1065215" target="_blank" rel="noopener"
             class="inline-flex items-center gap-2 bg-white dark:bg-white/10 hover:bg-gray-100 dark:hover:bg-white/20 text-orange-600 dark:text-orange-300 font-semibold px-5 py-2.5 rounded-xl border border-orange-300 dark:border-orange-500/30 transition-colors text-sm">
            상세 안내 →
          </a>
        </div>
      </div>
    </div>

    <!-- 관련 뉴스 -->
    {items.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        {items.map((item: any) => (
          <a href={item.link} target="_blank" rel="noopener" class="group bg-gray-100 dark:bg-white/[0.05] backdrop-blur-md border border-gray-200 dark:border-white/[0.08] rounded-2xl p-6 hover:bg-white/[0.1] hover:border-white/[0.2] hover:-translate-y-1 hover:shadow-[0_8px_32px_rgba(0,0,0,0.3)] transition-all duration-500">
            <div class="flex justify-between items-center mb-4">
              <span class="text-[10px] font-semibold bg-orange-500/20 text-orange-700 dark:text-orange-300 px-3 py-1 rounded-full border border-orange-500/20">모두의 창업</span>
              <span class="text-xs text-gray-500">{item.pub_date}</span>
            </div>
            <h3 class="font-bold text-gray-900 dark:text-white mb-2 group-hover:text-orange-600 dark:group-hover:text-orange-300 transition-colors line-clamp-2">{item.title}</h3>
            <p class="text-sm text-gray-500 line-clamp-2">{item.description}</p>
          </a>
        ))}
      </div>
    ) : (
      <p class="text-sm text-gray-500 text-center py-4">관련 뉴스를 수집 중입니다...</p>
    )}
  </div>
</section>
