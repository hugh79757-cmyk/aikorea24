# Phase 13-02: Validation Fixes — Chinese Period + Duplicate Link

**Goal**: Fix two concrete validation failures identified during Phase 13 E2E dry run.

**Wave**: 2

**Dependencies**: 13-01 complete

**Gap Closure**: false

---

## Decision Record
- **Date**: 2026-07-05
- **Context**: 3회 연속 드라이런 검증 실패 (YouTube Hook 과다, Schneider Card 3 절단, AI Campus Card 4 `。`)
- **Analysis**: 
  - MiMo v2.5는 중국어 모델로 한국어 출력 시 중국어 마침표 `。`(U+3002) 사용 → `sentence_enders`에 `。` 누락으로 검증 실패
  - `\n\n` split 후 중복 링크(동일 URL 2회) 발생 → 링크 카드가 2개 생성되는 경우 존재
- **Decision**: `\n\n` 방식 유지 + 검증/파싱 로직 강화 (미흡한 부분만 보강)
- **Reference**: 이 결정은 `docs/TECH.md` §11.1 (검증 체인)에 기록됨

---

## Tasks

### 1. Add Chinese period to sentence enders in validate_card_structure()
- File: `pipeline/threads/validator.py`
- Line 276: `sentence_enders` 리스트에 `。`(U+3002) 추가
- Also test: `.`과 `。`가 동시에 있을 경우 정상 인식되는지 확인
- Existing test: `tests/test_validator.py`에서 `test_card_structure_incomplete_sentence` 검증 통과 확인

### 2. Add Chinese period to sentence enders in _repair_truncated_cards()
- File: `pipeline/threads/writer.py`
- Line 470: `any(prev.endswith(e) for e in [...])` 리스트에 `。` 추가
- 일관성 유지: validator.py의 `sentence_enders`와 동일한 ender 세트 사용

### 3. Add duplicate link removal in parse_cards()
- File: `pipeline/threads/writer.py`
- After `_repair_truncated_cards()` call (line 487-488), add dedup logic:
  - 마지막 2개 카드가 둘 다 `🔗`로 시작하고 동일 URL이면 하나만 유지
  - 서로 다른 URL이면 둘 다 유지 (형식 D는 1개 링크지만 tolerance가 5-7)

### 4. Regression test sweep
- Run full test suite: `.venv/bin/pytest -x`
- Ensure all existing tests pass
- If any new failures, fix immediately

---

## Success Criteria

1. `。`가 포함된 카드가 `validate_card_structure()`에서 "문장 미완성"으로 실패하지 않음
2. `。`로 끝나는 카드가 `_repair_truncated_cards()`에서 잘 병합되지 않음 (올바르게 분리 유지)
3. 중복 링크(동일 URL 2회)가 `parse_cards()`에서 자동 제거됨
4. 모든 기존 테스트 통과
5. 새 테스트 추가 (validate_card_structure에 `。` 케이스)

---

## Files to Modify

- `pipeline/threads/validator.py` — sentence_enders에 `。` 추가
- `pipeline/threads/writer.py` — sentence enders에 `。` 추가 + 중복 링크 제거 로직
- `tests/test_validator.py` — `。` 케이스 테스트 추가 (선택)

---

**Note**: This plan addresses the concrete validation failures observed in dry run attempts #1-3.
