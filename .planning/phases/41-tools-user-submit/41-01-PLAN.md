---
phase: 41-tools-user-submit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-tools-user-submit/41-01-REPORT.md
autonomous: false
requirements:
  - REQ-41-00 (Phase 0 read-only verification, senior gate)
user_setup: []
estimate:
  tokens: 25000
  raw_tokens: 15000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "D1 users 실제 스키마가 확정되어 Phase 1 user_id 타입 결정 근거가 문서화됨"
    - "vote/reviews D1 테이블 실체가 확인되어 submit API 설계 블로커가 없음"
    - "로그인 감지 3종 패턴 중 submit.astro/API가 따를 패턴이 지정됨"
  artifacts:
    - .planning/phases/41-tools-user-submit/41-01-REPORT.md (verification report)
  key_links:
    - REPORT.md user_id 타입 결정 → 41-02 sql/001_tool_submissions.sql
    - REPORT.md 로그인 패턴 지정 → 41-02 submit.astro + submit.ts
---

<objective>
Phase 0 precondition: read-only verification, no code changes. Confirm RESEARCH.md claims against live repo + D1 remote, resolve open questions, produce gate report for senior review. Junior executes in order. Senior gate after.
Purpose: Phase 1 backbone built on verified facts, not assumptions.
Output: 41-01-REPORT.md + senior go/no-go.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-tools-user-submit/CONTEXT.md
@.planning/phases/41-tools-user-submit/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D1 schema confirm (read-only)</name>
  <files>No repo files modified. Output: report section 1.</files>
  <action>Run read-only remote dump: wrangler d1 execute aikorea24-db --remote --command="SELECT sql FROM sqlite_master WHERE name IN ('users','posts','tool_votes','tool_submissions');". Compare against RESEARCH §1.1 (schema.sql users base, persona_migration kakao cols, 20260710_add_admin_role role col) and §3.1 (tool_votes/posts-extension DDL absent in repo). Confirm or correct: users columns full list, posts has tool_id/rating, tool_votes exists + columns, tool_submissions absent. Also check membership trio (membership, membership_expires, purchased_posts) presence — record only, not blocker per RESEARCH §1.1D. Do NOT create or alter anything.</action>
  <verify><automated>wrangler d1 execute aikorea24-db --remote --command="SELECT name FROM sqlite_master WHERE type='table' AND name IN ('users','posts','tool_votes','tool_submissions');"</automated></verify>
  <done>Report section lists actual remote columns per table + match/mismatch vs RESEARCH §1.1/§3.1, plus user_id type verdict (expect INTEGER per RESEARCH §1.3).</done>
</task>

<task type="auto">
  <name>Task 2: File reads (read-only)</name>
  <files>No repo files modified. Output: report section 2.</files>
  <action>Read and record exact current state (line numbers + key snippets): src/pages/tools/[id].astro (prerender, getStaticPaths, render path, TOOL_ID/TOOL_INDEX wiring, section-hide patterns), src/pages/tools/index.astro (8 categories verbatim, card classes, PAGE_SIZE, submit link), src/pages/tools/submit.astro (prerender, form action, category source), src/pages/api/tools/vote.ts + reviews.ts (DB access line, auth lines, query list), src/config/tasks.ts (key list for form options, confirm 73 count), src/env.d.ts (DB/R2 bindings), src/lib/auth.ts (signSession/verifySession/getSessionUser signatures), src/middleware.ts (sessionSecret injection), api/auth/callback/google.ts + kakao.ts (INSERT/SELECT columns), src/content.config.ts (tools schema fields), wrangler.toml (DB binding). Flag any drift vs RESEARCH §2/§4/§5.</action>
  <verify><automated>ls src/pages/tools/ src/pages/api/tools/ && grep -c prerender src/pages/tools/submit.astro src/pages/tools/index.astro src/pages/tools/\[id\].astro</automated></verify>
  <done>Report section confirms each file state + drifts (if none: "no drift vs RESEARCH §N").</done>
</task>

<task type="auto">
  <name>Task 3: Login patterns + write gate report</name>
  <files>.planning/phases/41-tools-user-submit/41-01-REPORT.md</files>
  <action>Confirm 3 login patterns per RESEARCH §4: A server .astro guard (community/write.astro → /auth/consent), B server API 401 JSON (api/posts/index.ts + vote.ts), C client cookie check ([id].astro → /api/auth/login). Lock decision: submit.astro follows A, submit API follows B, 401 shows message only (no redirect). Write 41-01-REPORT.md in CONTEXT report format (생성/수정 파일, 동작확인, 미해결이슈, 다음메모) plus: user_id type verdict, vote/reviews table verdict, file-drift list, locked pattern choices, go/no-go for 41-02. No code changes anywhere.</action>
  <verify><automated>test -f .planning/phases/41-tools-user-submit/41-01-REPORT.md && grep -c '다음메모' .planning/phases/41-tools-user-submit/41-01-REPORT.md</automated></verify>
  <done>REPORT.md exists with all 4 report sections + verdicts. Zero git-tracked code diffs (git status --porcelain shows only REPORT.md).</done>
</task>

</tasks>

<verification>
Report covers users/posts/tool_votes/tool_submissions remote state, all §2 files, 3 login patterns. No code modified.
</verification>

<success_criteria>
Senior can approve 41-02 from REPORT.md alone. Rollback: n/a (read-only; delete REPORT.md to redo).
</success_criteria>

<output>
Create `.planning/phases/41-tools-user-submit/41-01-SUMMARY.md` when done
</output>
