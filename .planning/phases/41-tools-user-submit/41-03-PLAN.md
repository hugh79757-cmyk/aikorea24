---
phase: 41-tools-user-submit
plan: 03
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - src/pages/tools/submit.astro
  - src/pages/tools/index.astro
  - src/pages/tools/[id].astro
autonomous: true
requirements:
  - REQ-41-04 (completion screen + cafe CTA)
  - REQ-41-05 (index.astro D1 merge + MAKER badge)
  - REQ-41-06 ([id].astro D1 fallback)
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "제출 직후 완료화면에 카드 미리보기 + MAKER + 카페 복사/바로가기 + /tools/{slug} 링크"
    - "목록에 D1 도구가 즉시 MAKER 배지와 함께 최신순 상단 노출"
    - "D1 slug 상세 접근 시 md 우선, 없으면 D1 렌더 (없는 섹션 숨김)"
  artifacts:
    - src/pages/tools/submit.astro (completion view)
    - src/pages/tools/index.astro (D1 merge)
    - src/pages/tools/[id].astro (D1 fallback)
  key_links:
    - submit success → completion → /tools/{slug} detail
    - index.astro D1 query → merged list render
    - [id].astro slug → md else D1 lookup
---

<objective>
Phase 2 MVP: completion screen + list merge + detail fallback. Done = 제출→목록 MAKER 노출→상세 접근.
Purpose: user-submitted tool visible immediately with MAKER identity + cafe funnel.
Output: 3 files modified, astro build passes, no Phase 3 work.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-tools-user-submit/CONTEXT.md
@.planning/phases/41-tools-user-submit/RESEARCH.md
@.planning/phases/41-tools-user-submit/41-02-SUMMARY.md
@src/pages/tools/index.astro
@src/pages/tools/[id].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Completion screen in submit.astro</name>
  <files>src/pages/tools/submit.astro</files>
  <action>On fetch POST 201 ({slug}), swap form for completion view (client-side, same page): card preview reusing index card classes (gradient bar, name, description, category, price) + MAKER badge (exact classes defined in Task 2 — copy verbatim, single source: define badge markup once here and mirror in index/[id]). Cafe template copy button (clipboard: tool name + url + one-line intro + 'https://cafe.naver.com/gptdohye 에서 더 많은 AI 활용법을 확인하세요'), cafe shortcut link https://cafe.naver.com/gptdohye (target _blank, locked CONTEXT URL), '내 도구 보기' link → /tools/{slug}. All errors already handled in 41-02. Keep Tailwind/dark/responsive. No new routes, no /my/tools (Phase 3).</action>
  <verify><automated>npm run build 2>&1 | tail -5; grep -c MAKER src/pages/tools/submit.astro</automated></verify>
  <done>After 201: preview + MAKER + copy button (clipboard works) + cafe link + /tools/{slug} link visible.</done>
</task>

<task type="auto">
  <name>Task 2: index.astro D1 merge + MAKER badge</name>
  <files>src/pages/tools/index.astro</files>
  <action>Remove `export const prerender = true` from index.astro (full SSR locked — D1 query needs it; perf note in report). Query D1 tool_submissions (all rows, status 무관 즉시표시 locked) via Astro.locals runtime DB with try/catch → fail-soft empty array 200 attitude (RESEARCH §2.3). Merge with md collection: D1 first (created_at DESC, top newest), then md order. Map D1 rows to card shape (accent/difficulty/price fallbacks reuse existing maps). MAKER badge exact classes (define here, mirror in Task 1/3): purple mirror of NEW badge (RESEARCH §2.2): class="text-xs font-bold px-2 py-0.5 bg-purple-100 text-purple-700 rounded-full border border-purple-200 dark:bg-purple-500/15 dark:text-purple-400 dark:border-purple-500/20" text "MAKER". Extend data-* filter attrs for D1 cards (data-cat/name/desc). PAGE_SIZE/pagination must include D1 rows. Markdown files untouched; card Tailwind patterns kept.</action>
  <verify><automated>npm run build 2>&1 | tail -5; grep -c MAKER src/pages/tools/index.astro; curl -s localhost:4321/tools/ | grep -o MAKER | head -1</automated></verify>
  <done>D1 test row appears at top of list with MAKER badge, filters/pagination include it, DB failure still renders md list.</done>
</task>

<task type="auto">
  <name>Task 3: [id].astro D1 fallback</name>
  <files>src/pages/tools/[id].astro</files>
  <action>Remove `export const prerender = true` from [id].astro (full SSR — locked; getStaticPaths kept harmlessly for pre-render list but all routes render server-side so unknown D1 slugs resolve at runtime). If Astro.params.id not in md collection, query D1 tool_submissions by slug; miss → 404. D1 render: title/name, MAKER badge (Task 2 classes verbatim), description, detail_markdown else description as body (md Content path stays md-only; D1 body = simple prose render, no new deps — escape HTML, preserve line breaks), meta rows (category/price/url/koreanSupport/difficulty), tasks chips filtered by ALL_TASKS (RESEARCH §2.1:68 unknown slugs dropped). Hide missing sections with existing conditional patterns (RESEARCH §2.1:153/174/187 — {x && (...)}). Reviews section hidden for D1 rows (reviews needs INTEGER tool_id, RESEARCH §7 — vote button works with slug TEXT key per §7). useCases/tags/tasks empty → section omitted.</action>
  <verify><automated>npm run build 2>&1 | tail -5; curl -s -o /dev/null -w "md:%{http_code}\n" localhost:4321/tools/chatgpt-work/; curl -s -w "\nd1:%{http_code}\n" localhost:4321/tools/$TEST_D1_SLUG/ | grep -o MAKER; curl -s -o /dev/null -w "unknown:%{http_code}\n" localhost:4321/tools/no-such-tool-xyz/</automated></verify>
  <done>/tools/{d1-slug} renders with MAKER + available sections only; md pages byte-identical behavior; unknown slug 404s.</done>
</task>

</tasks>

<verification>
npm run build passes. E2E local: submit → completion → list top MAKER → detail renders. md tools untouched (git diff --stat src/content/tools/ empty).
</verification>

<success_criteria>
Done = 제출→목록 MAKER 노출→상세 접근. Report in CONTEXT format. Rollback: git revert 3 files (D1 rows kept; DELETE FROM tool_submissions WHERE slug test-only if needed). No Phase 3 (R2/my/PUT/DELETE) started.
</success_criteria>

<output>
Create `.planning/phases/41-tools-user-submit/41-03-SUMMARY.md` when done
</output>
