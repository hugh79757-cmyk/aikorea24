---
phase: 41-tools-user-submit
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - sql/001_tool_submissions.sql
  - src/pages/api/tools/submit.ts
  - src/pages/tools/submit.astro
autonomous: true
requirements:
  - REQ-41-01 (tool_submissions DDL + D1 apply)
  - REQ-41-02 (POST /api/tools/submit)
  - REQ-41-03 (submit.astro SSR 2-step form)
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "로그인 유저가 2스텝 폼 제출 시 D1 tool_submissions에 1행 저장됨"
    - "비로그인 제출 시 401 + 사용자 친화 메시지"
    - "md/D1 slug 중복 시 숫자 접미사로 충돌 회피"
    - "마크다운 도구·자동수집 파이프라인 untouched"
  artifacts:
    - sql/001_tool_submissions.sql
    - src/pages/api/tools/submit.ts
    - src/pages/tools/submit.astro (SSR, prerender 제거)
  key_links:
    - submit.astro fetch POST → /api/tools/submit → D1 INSERT
    - submit.ts slug check → getCollection('tools') + D1 slug
---

<objective>
Phase 1 backbone: DDL + submit API + SSR form. Done = form→D1 저장.
Purpose: user registration path works end-to-end; list/detail (41-03) build on it.
Output: 3 files created/modified, applied to D1 remote, astro build passes.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-tools-user-submit/CONTEXT.md
@.planning/phases/41-tools-user-submit/RESEARCH.md
@.planning/phases/41-tools-user-submit/41-01-REPORT.md
@src/pages/api/tools/vote.ts
@src/pages/api/tools/reviews.ts
@src/config/tasks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: sql/001_tool_submissions.sql + D1 apply</name>
  <files>sql/001_tool_submissions.sql</files>
  <action>Create table tool_submissions per 41-01-REPORT user_id verdict (expect user_id INTEGER NOT NULL REFERENCES users(id) per RESEARCH §1.3; if report says otherwise follow report). Columns: id INTEGER PK AUTOINCREMENT, user_id (report type), slug TEXT UNIQUE NOT NULL, name TEXT NOT NULL, url TEXT NOT NULL, category TEXT NOT NULL, description TEXT NOT NULL (200자 capped in API), price TEXT, korean_support INTEGER DEFAULT 0, difficulty TEXT, use_cases TEXT, tags TEXT (JSON array string), tasks TEXT (JSON array string, max 5 enforced in API), detail_markdown TEXT, status TEXT DEFAULT 'published' (1차 즉시게시, 승인 없음), created_at/updated_at TEXT DEFAULT (datetime('now')). CREATE INDEX idx_tool_submissions_slug, idx_tool_submissions_user. Filename sql/001_tool_submissions.sql (no collision per RESEARCH §6). Apply: wrangler d1 execute aikorea24-db --local --file=sql/001_tool_submissions.sql then --remote. Do NOT touch posts/tool_votes/users DDL.</action>
  <verify><automated>wrangler d1 execute aikorea24-db --local --command="SELECT sql FROM sqlite_master WHERE name='tool_submissions';"; wrangler d1 execute aikorea24-db --remote --command="SELECT sql FROM sqlite_master WHERE name='tool_submissions';"</automated></verify>
  <done>Table exists local + remote, slug unique, user_id type matches report verdict.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/tools/submit</name>
  <files>src/pages/api/tools/submit.ts</files>
  <action>New file following vote.ts pattern (RESEARCH §2.3) + posts/index.ts auth (RESEARCH §4B): DB via (locals as any).runtime?.env?.DB, missing → 500 'DB 없음'. Auth: cookies session missing → 401 '로그인이 필요합니다.', verifySession(session, (locals as any).sessionSecret) fail → 401 '유효하지 않은 세션입니다.'. Then users SELECT id by email → no row → 401. Validation 400s: required name/url/category/description missing; description cap 200자 (over → 400 '설명은 200자 이내로 입력해주세요', no silent truncate); URL check via new URL() + http(s) only; category must be one of 7 fixed + 기타 (RESEARCH §5.2: index 8 minus 전체 plus 기타); tasks max 5 + keys must exist in TASKS (RESEARCH §2.5, unknown keys → 400). Slug: name lowercase kebab (non-alnum→hyphen, trim hyphens; empty/non-latin result → tool-<epoch> per RESEARCH §5.1), duplicate check against getCollection('tools') slugs AND D1 slug (RESEARCH §7) with numeric suffix loop (-2, -3...). D1 INSERT all fields + user_id. Return 201 {ok:true, slug}. All errors JSON {error} with user-friendly Korean messages. Try/catch → 500 friendly. No R2, no PUT/DELETE (Phase 3 deferred).</action>
  <verify><automated>npm run build 2>&1 | tail -5; curl -s -o /dev/null -w "no-cookie:%{http_code}\n" -X POST localhost:4321/api/tools/submit; curl -s -w "\nbad-field:%{http_code}\n" -X POST localhost:4321/api/tools/submit -H 'Content-Type: application/json' -d '{"name":""}' --cookie "session=$TEST_SESSION"</automated></verify>
  <done>401 on no session, 400 on bad fields, 201 INSERT on valid POST (local D1 test via curl).</done>
</task>

<task type="auto">
  <name>Task 3: submit.astro SSR 2-step form</name>
  <files>src/pages/tools/submit.astro</files>
  <action>Remove export const prerender (SSR required per RESEARCH §7). Server guard pattern A (RESEARCH §4A, community/write.astro): verifySession via Astro.cookies + Astro.locals.sessionSecret, no user → Astro.redirect('/auth/consent'). Keep existing Tailwind classes/dark mode/responsive. 2-step client form with fetch POST (not native action): Step1 required 5 — 이름, URL, 카테고리(select: index 8 중 전체 제외 7 + 기타 fixed per RESEARCH §5.2), 설명(200자 counter), 가격. Step2 optional 7 — 한국어지원(checkbox), 난이도, 활용사례, 태그, 연관작업(tasks.ts keys only, max 5 checkboxes), 상세설명(detail_markdown textarea), featured 아님(omit). Back/next nav, per-step validation, 401 → '로그인이 필요합니다' message only (no redirect per RESEARCH §4C decision), other errors show API message. Success → minimal completion-state hook only (show slug + link); full completion view is owned by 41-03 Task 1 — do not build redirect page here. Markdown tools untouched.</action>
  <verify><automated>npm run build 2>&1 | tail -5</automated></verify>
  <done>Build passes, logged-out visit redirects /auth/consent, logged-in sees 2-step form, valid submit returns 201 + D1 row, errors show Korean messages.</done>
</task>

</tasks>

<verification>
npm run build passes. Local curl: 401 no-cookie, 400 bad fields, 201 valid → D1 row. No .md tools touched (git status shows only 3 files).
</verification>

<success_criteria>
Done = form→D1 저장. Report in CONTEXT format (생성/수정 파일, 동작확인, 미해결이슈, 다음메모). Rollback: DROP TABLE tool_submissions (local + remote) + git revert 3 files.
</success_criteria>

<output>
Create `.planning/phases/41-tools-user-submit/41-02-SUMMARY.md` when done
</output>