---
phase: 03-landing-zone-orchestrator
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/threads/threads-publisher.plist.template
  - scripts/install_launchd.sh
  - scripts/deploy.sh
autonomous: true
requirements: [POR-02, POR-03, POR-04]
must_haves:
  truths:
    - "threads-publisher.plist.template contains string.Template variables ($PROJECT_DIR, $VENV_PYTHON, $LOG_DIR) — no hardcoded paths or secrets"
    - "install_launchd.sh generates a real plist from the template using Python string.Template"
    - "install_launchd.sh computes PROJECT_DIR from its own location (dirname $0)"
    - "install_launchd.sh unloads old launchd agent and loads the new one"
    - "deploy.sh resolves paths relative to its location (dirname $0) and sources only project .env"
  artifacts:
    - path: "scripts/threads/threads-publisher.plist.template"
      provides: "Plist template with string.Template variables"
      contains: "\\$PROJECT_DIR|\\$VENV_PYTHON|\\$LOG_DIR"
    - path: "scripts/install_launchd.sh"
      provides: "Install script that generates plist from template and loads it"
      min_lines: 30
    - path: "scripts/deploy.sh"
      provides: "Portable deploy script using dirname-based path resolution"
      contains: "SCRIPT_DIR.*dirname.*BASH_SOURCE|PROJECT_DIR.*dirname.*SCRIPT_DIR"
  key_links:
    - from: "scripts/install_launchd.sh"
      to: "scripts/threads/threads-publisher.plist.template"
      via: "reads template, substitutes variables, writes plist"
    - from: "scripts/deploy.sh"
      to: "PROJECT_DIR/.env"
      via: "sources only project .env (no cross-project references)"
---

<objective>
Make the pipeline clone-and-run portable: template-ize the launchd plist (zero hardcoded paths/secrets), create install_launchd.sh that generates plist with computed paths, and fix deploy.sh to resolve paths relatively.

Purpose: Eliminate the last hardcoded paths (POR-02, POR-03) and cross-project dependencies (POR-04) so the pipeline can be cloned to any machine and the plist can be installed with a single command.
Output: `scripts/threads/threads-publisher.plist.template`, `scripts/install_launchd.sh`, `scripts/deploy.sh` (modified)
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-landing-zone-orchestrator/03-RESEARCH.md

<interfaces>
Current threads-publisher.plist (hardcoded paths — exact strings to template-ize):
```
ProgramArguments:
  /Users/twinssn/Projects/aikorea24/.venv/bin/python3
  /Users/twinssn/Projects/aikorea24/scripts/threads/main_v3.py
  --once
WorkingDirectory: /Users/twinssn/Projects/aikorea24
StandardOutPath: /Users/twinssn/Projects/aikorea24/scripts/threads/logs/launchd.log
StandardErrorPath: /Users/twinssn/Projects/aikorea24/scripts/threads/logs/launchd_error.log
```
All of these become string.Template variables: $PROJECT_DIR, $VENV_PYTHON, $LOG_DIR

Current deploy.sh (scripts/deploy.sh):
- Uses `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` and `PROJECT_DIR="$(dirname "$SCRIPT_DIR")"` — already relative
- Sources only PROJECT_DIR/.env — no api_test/.env.sh reference (confirmed via reading)
- The file already resolves paths relatively. The main change is to ensure the .env sourcing uses PROJECT_DIR explicitly and that there's no fallback to other .env sources.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plist template with string.Template variables</name>
  <files>
    scripts/threads/threads-publisher.plist.template
  </files>
  <read_first>
    scripts/threads/threads-publisher.plist
  </read_first>
  <action>
    Create `scripts/threads/threads-publisher.plist.template` — an exact copy of the current plist with these substitutions:

    - `/Users/twinssn/Projects/aikorea24/.venv/bin/python3` → `${VENV_PYTHON}`
    - `/Users/twinssn/Projects/aikorea24/scripts/threads/main_v3.py` → `${SCRIPT_PATH}`
    - `/Users/twinssn/Projects/aikorea24` (WorkingDirectory) → `${PROJECT_DIR}`
    - `/Users/twinssn/Projects/aikorea24/scripts/threads/logs/launchd.log` → `${LOG_DIR}/launchd.log`
    - `/Users/twinssn/Projects/aikorea24/scripts/threads/logs/launchd_error.log` → `${LOG_DIR}/launchd_error.log`

    Keep the StartInterval at 7200 (2 hours), KeepAlive=false, RunAtLoad=false.

    Use `string.Template` syntax (`$VAR` or `${VAR}`) — not Jinja2 or f-string. Per research (D-12: stdlib only, use `string.Template`).

    The template must be valid XML after substitution. Path values are plain ASCII file paths, so no XML escaping is needed for $VAR values.

    Add a comment line at the top:
    `<!-- GENERATED from threads-publisher.plist.template. Do not edit directly. -->`
  </action>
  <acceptance_criteria>
    - Template file contains `${VENV_PYTHON}`, `${SCRIPT_PATH}`, `${PROJECT_DIR}`, `${LOG_DIR}` variables
    - Template contains NO hardcoded `/Users/twinssn/` paths
    - Template contains NO API keys or secrets
    - `python3 -c "from string import Template; t = open('scripts/threads/threads-publisher.plist.template').read(); r = Template(t).safe_substitute(VENV_PYTHON='/path/python3', PROJECT_DIR='/path', SCRIPT_PATH='/path/main.py', LOG_DIR='/path/logs'); print(r)"` produces valid-looking plist XML with substituted paths
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from string import Template; t = open('scripts/threads/threads-publisher.plist.template').read(); r = Template(t).safe_substitute(VENV_PYTHON='/test/p', PROJECT_DIR='/test', SCRIPT_PATH='/test/main.py', LOG_DIR='/test/logs'); assert '${' not in r; assert '/test' in r; print('Template substitution OK')"</automated>
  </verify>
  <done>plist template created with string.Template variables, zero hardcoded paths</done>
</task>

<task type="auto">
  <name>Task 2: Create install_launchd.sh that generates plist from template</name>
  <files>
    scripts/install_launchd.sh
  </files>
  <read_first>
    scripts/threads/threads-publisher.plist.template
  </read_first>
  <action>
    Create `scripts/install_launchd.sh`:
    - Shebang: `#!/bin/bash`
    - `set -e` (fail on first error)

    - Compute paths using `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` and `PROJECT_DIR="$(dirname "$SCRIPT_DIR")"`. scripts/ dir contains install_launchd.sh; PROJECT_DIR is one level up.

    - Template path: `TEMPLATE="$SCRIPT_DIR/threads/threads-publisher.plist.template"`
    - Output path: `PLIST_DEST="$HOME/Library/LaunchAgents/kr.aikorea24.threads-publisher.plist"`
    - Plist label: `PLIST_NAME="kr.aikorea24.threads-publisher"`

    - Validate template exists; exit with error if missing

    - Generate plist using inline Python `python3 -c` command that: imports Template from string, opens template file at $TEMPLATE path, calls Template().safe_substitute() with VENV_PYTHON='$PROJECT_DIR/.venv/bin/python3', PROJECT_DIR='$PROJECT_DIR', SCRIPT_PATH='$PROJECT_DIR/pipeline/__main__.py', LOG_DIR='$PROJECT_DIR/pipeline/logs', writes result to $PLIST_DEST, prints confirmation.

    - Unload existing agent: `launchctl unload "$PLIST_DEST" 2>/dev/null || true` (ignore error if not loaded)

    - Load new agent: `launchctl load "$PLIST_DEST"` then `echo "Launchd agent loaded: $PLIST_NAME"`

    - Note: `\` before `$` in the Python string prevents bash variable expansion. Use `\` for all Python-level `$` variable references.
    - The SCRIPT_PATH now points to `$PROJECT_DIR/pipeline/__main__.py` instead of `main_v3.py` — this is the new orchestrator entry point.
    - Korean comments inline explaining each section.
  </action>
  <acceptance_criteria>
    - install_launchd.sh exists with #!/bin/bash and set -e
    - Script computes PROJECT_DIR using dirname of BASH_SOURCE
    - Script uses Python string.Template to substitute variables
    - Script calls launchctl unload + load
    - `bash -n scripts/install_launchd.sh` passes syntax check
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/install_launchd.sh && echo "Syntax OK"</automated>
  </verify>
  <done>install_launchd.sh created — generates plist from template, loads into launchd</done>
</task>

<task type="auto">
  <name>Task 3: Fix deploy.sh portability</name>
  <files>
    scripts/deploy.sh
  </files>
  <read_first>
    scripts/deploy.sh
  </read_first>
  <action>
    Read the current `scripts/deploy.sh` and verify it already has portable path resolution:
    - Line 26: `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` — correct
    - Line 27: `PROJECT_DIR="$(dirname "$SCRIPT_DIR")"` — correct
    - Line 28-33: Sources only `"$PROJECT_DIR/.env"` — correct, no cross-project reference

    The script already satisfies POR-04. The only change needed:

    - Add a comment above the .env loading section clarifying that only PROJECT_DIR/.env is sourced (no api_test/.env.sh or ~/.env.common fallback within deploy.sh itself — those are handled by the pipeline scripts at runtime).
    - Ensure the error message is in Korean for consistency.
    - Change the English comments on lines 5-7 to Korean for project consistency per D-13.

    No functional changes needed. This task documents and confirms the existing portability.
  </action>
  <acceptance_criteria>
    - deploy.sh sources only "$PROJECT_DIR/.env" — no api_test/.env.sh reference
    - deploy.sh resolves PROJECT_DIR from SCRIPT_DIR using dirname
    - deploy.sh has Korean comments
    - `bash -n scripts/deploy.sh` passes syntax check
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/deploy.sh && echo "Syntax OK"</automated>
  </verify>
  <done>deploy.sh confirmed portable — resolves paths relative to its location, sources only PROJECT_DIR/.env</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell script → filesystem | install_launchd.sh writes plist to ~/Library/LaunchAgents/ |
| Shell script → launchctl | install_launchd.sh calls launchctl to load/unload agent |
| string.Template → plist | Template substitution; variables are computed paths (not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Plist path injection | mitigate | install_launchd.sh computes paths from its own location using `dirname $0`. No user input enters the paths. Template substitution uses safe_substitute (doesn't throw on missing vars). |
| T-03-08 | Tampering | Shell injection | mitigate | No user input flows into any shell command. Python template substitution escapes nothing because paths are ASCII and computed. |
| T-03-09 | Information Disclosure | Secrets in plist | mitigate | Template contains ZERO secret references — all env vars delegate to .env (Phase 1 already removed secrets from plist). Verified: template has no API_KEY, TOKEN, SECRET, or PASSWORD patterns. |
| T-03-SC | Tampering | pip/npm installs | mitigate | No new packages. stdlib only. |
</threat_model>

<verification>
- `bash -n scripts/install_launchd.sh` — syntax OK
- `bash -n scripts/deploy.sh` — syntax OK
- `grep -c "/Users/twinssn/" scripts/threads/threads-publisher.plist.template` returns 0 (no hardcoded paths)
- `grep -c '\$PROJECT_DIR\|\$VENV_PYTHON\|\$LOG_DIR\|\$SCRIPT_PATH' scripts/threads/threads-publisher.plist.template` returns 4 (all variables present)
- Template substitution produces valid XML output
</verification>

<success_criteria>
- Plist template exists with string.Template variables — zero hardcoded paths or secrets (POR-02)
- install_launchd.sh generates plist from template and loads into launchd (POR-03)
- deploy.sh resolves paths relative to its location, sources only PROJECT_DIR/.env (POR-04)
</success_criteria>

<output>
Create `.planning/phases/03-landing-zone-orchestrator/03-03-SUMMARY.md` when done
</output>
