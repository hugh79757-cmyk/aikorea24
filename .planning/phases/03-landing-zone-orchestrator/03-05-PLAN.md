---
phase: 03-landing-zone-orchestrator
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_characterization_validate_final_cards.py
  - tests/test_characterization_pure_functions.py
autonomous: true
requirements: [TST-01]
must_haves:
  truths:
    - "validate_final_cards() behavior is captured in snapshot tests — output matches expected for known inputs"
    - "Pure functions from main_v3.py and writer_v3.py have characterization tests that pass before any Phase 4 changes"
    - "Tests run via pytest and pass in CI environment"
    - "Tests do not require D1, network, or API access — fully hermetic"
  artifacts:
    - path: "tests/test_characterization_validate_final_cards.py"
      provides: "Characterization tests for validate_final_cards() pure function"
      min_lines: 60
      contains: "def test_valid|def test_invalid"
    - path: "tests/test_characterization_pure_functions.py"
      provides: "Characterization tests for other pure functions"
      min_lines: 40
      contains: "def test_"
  key_links:
    - from: "tests/test_characterization_validate_final_cards.py"
      to: "scripts/threads/main_v3.py"
      via: "imports validate_final_cards for snapshot testing"
    - from: "tests/test_characterization_pure_functions.py"
      to: "scripts/threads/v3/writer_v3.py"
      via: "imports pure functions for characterization"
---

<objective>
Write characterization tests for pure functions before Phase 4 monolith splitting. These tests capture current behavior as snapshots — they verify that refactoring in Phase 4 preserves existing behavior.

Purpose: TST-01 — The Strangler Fig pattern requires that before we split monoliths, we know what the current functions do. Characterization tests ("golden tests") assert the current output for known inputs. When Phase 4 refactors, these tests ensure behavior is preserved.

Output: `tests/test_characterization_validate_final_cards.py`, `tests/test_characterization_pure_functions.py`
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-landing-zone-orchestrator/03-RESEARCH.md

<interfaces>
Scripts needed for sys.path:
- tests/ directory needs sys.path.insert(0, project_root/scripts) to import from scripts/
- tests/ directory needs sys.path.insert(0, project_root/scripts/threads) to import from threads/

Existing conftest.py at tests/conftest.py provides:
- monkeypatch_d1, monkeypatch_openai, monkeypatch_deepseek, monkeypatch_http fixtures
- sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "scripts"))

Pure functions identified for characterization:
1. `validate_final_cards(cards: list[str]) -> tuple[bool, list[str]]` in scripts/threads/main_v3.py
   - Pure: no side effects, no API calls, deterministic output for given input
   - Validates card format, length, instruction patterns, source links, sentence completion, content similarity, Korean-English spacing
   - Easy to snapshot: provide sample card lists, capture (valid, issues) tuple

2. `load_style_examples()` in scripts/threads/v3/writer_v3.py
   - Pure: reads from file system but can be mocked with fixture file
   - Returns string content or empty string on failure

3. `extract_keywords(title, original_title, description)` in scripts/threads/dedup.py
   - Pure: deterministic string processing with stopword filtering
   - Returns keyword set or list

4. `validate_thread(content)` in scripts/threads/validator.py
   - Pure: regex-based validation, no side effects
   - Returns (bool, list) tuple

For this plan, focus on #1 (validate_final_cards) — the most critical pure function for Phase 4 — and #4 (validate_thread) as secondary.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Characterization test for validate_final_cards()</name>
  <files>
    tests/test_characterization_validate_final_cards.py
  </files>
  <read_first>
    scripts/threads/main_v3.py
    tests/conftest.py
  </read_first>
  <behavior>
    Test 1 — valid cards: Provide 5 well-formed, short test cards that should pass validation. Assert returns (True, []).
    Test 2 — instruction pattern detection: Cards containing lines starting with known INSTRUCTION_PATTERNS prefixes should fail with "프롬프트 명령어 포함"
    Test 3 — empty card detection: A card that is empty (blank string) should fail with "빈 카드"
    Test 4 — length violation: A card exceeding 500 characters should fail with "500자 초과"
    Test 5 — missing source link: Last card without http/https link should fail with "출처 링크 없음"
    Test 6 — incomplete sentence: Cards ending without punctuation (. ! ?) should fail with "미완결 문장"
    Test 7 — duplicate content: Two cards with >85% word overlap should fail with "내용 유사"
    Test 8 — Korean-English spacing: Cards with Korean+English no-space concatenation (3+ letter English) should fail with "한글+영어 붙어쓰기"
  </behavior>
  <action>
    Create `tests/test_characterization_validate_final_cards.py`:

    - Import: pytest, sys, os
    - Set up sys.path: `import sys` and `from pathlib import Path`, then `PROJECT_ROOT = Path(__file__).resolve().parent.parent`, `sys.path.insert(0, str(PROJECT_ROOT / "scripts"))`, `sys.path.insert(0, str(PROJECT_ROOT / "scripts/threads"))`

    - Import validate_final_cards: `from main_v3 import validate_final_cards` (import by filename since scripts/threads/ is on sys.path and is not a proper package; the import will find main_v3.py as a top-level module)

    - Import INSTRUCTION_PATTERNS for test fixtures: `from v3.writer_v3 import INSTRUCTION_PATTERNS` (handle ImportError gracefully — INSTRUCTION_PATTERNS is defined inside writer_v3.py. If import fails, hardcode the necessary patterns from the source code)

    - Test 1: `test_valid_cards` — Create 5 cards with valid content (short, no issues). Each card 3-4 lines, ending with . or ! or ?, last card contains http link.
      Assert result == (True, [])

    - Test 2: `test_instruction_pattern_detected` — One card starting with INSTRUCTION_PATTERNS[0].
      Assert result == (False, issues) where issues[0] contains "프롬프트 명령어 포함"

    - Test 3: `test_empty_card` — One empty card in the list.
      Assert result == (False, issues) where "빈 카드" in str(issues)

    - Test 4: `test_length_violation` — One card 510 characters long.
      Assert result == (False, issues) where "500자 초과" in str(issues)

    - Test 5: `test_missing_source_link` — Last card has no http or 🔗.
      Assert result == (False, issues) where "출처 링크 없음" in str(issues)

    - Test 6: `test_incomplete_sentence` — A card ending without terminal punctuation.
      Assert result == (False, issues) where "미완결 문장" in str(issues)

    - Test 7: `test_duplicate_content` — Two cards with >85% word overlap (same words, different order).
      Assert result == (False, issues) where "내용 유사" in str(issues)

    - Test 8: `test_korean_english_spacing` — Card with run-together Korean+English (e.g., "개발자는OpenAI API를").
      Assert result == (False, issues) where "한글+영어 붙어쓰기" in str(issues)

    - No conftest needed — tests are fully hermetic (no D1, no network, no OpenAI).
    - All comments in Korean per D-13.
    - Test card content: use realistic but short Korean AI news content that makes the test case obvious.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_characterization_validate_final_cards.py -v 2>&1</automated>
  </verify>
  <done>validate_final_cards characterization tests pass — 8 test cases covering all validation rules</done>
</task>

<task type="tdd">
  <name>Task 2: Characterization tests for other pure functions</name>
  <files>
    tests/test_characterization_pure_functions.py
  </files>
  <read_first>
    scripts/threads/validator.py
    scripts/threads/main_v3.py
    tests/conftest.py
  </read_first>
  <behavior>
    Test 1 — validate_thread valid: A well-formed 8-card thread should pass.
    Test 2 — validate_thread invalid card count: 5 cards instead of 8 should fail.
    Test 3 — validate_thread label detection: Cards with label prefix ("(훅)", "(데이터)", etc.) should fail.
    Test 4 — validate_final_cards empty input: Empty list should return (False, issues).
    Test 5 — validate_final_cards single card: Single short valid card should fail (no source link in last card).
  </behavior>
  <action>
    Create `tests/test_characterization_pure_functions.py`:

    - Same sys.path setup as Task 1.
    - Import validate_thread from scripts/threads/validator.py: use same sys.path pattern as Task 1 (`PROJECT_ROOT = Path(__file__).resolve().parent.parent`, insert scripts/ and scripts/threads/ on sys.path), then `from validator import validate_thread` and `from main_v3 import validate_final_cards`

    - Test 1: `test_validate_thread_valid` — Create 8 cards separated by '---'. Each card 3-4 lines. No label prefixes. Assert returns (True, [])

    - Test 2: `test_validate_thread_invalid_card_count` — 5 cards instead of 8. Assert returns (False, _) where "카드 수" in str(failures)

    - Test 3: `test_validate_thread_label_detected` — One card with "(훅)" prefix at start. Assert returns (False, _) where "레이블" in str(failures)

    - Test 4: `test_validate_final_cards_empty` — Empty list. Assert returns (False, issues) where len(issues) > 0

    - Test 5: `test_validate_final_cards_no_source` — Single valid short card without link. Assert returns (False, _) where "출처 링크" in str(issues)

    - Each test function: `def test_<name>()` with no fixtures needed.
    - All comments in Korean.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_characterization_pure_functions.py -v 2>&1</automated>
  </verify>
  <done>Characterization tests for validate_thread and edge cases pass — 5 test cases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → pure functions | No external systems accessed |
| Test fixtures | Generated inline; no D1, network, or OpenAI calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | Test output | accept | Test output may contain sample function outputs but no secrets (test data is fabricated). |
| T-03-SC | Tampering | pip installs | mitigate | No new packages. pytest already installed. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_characterization_validate_final_cards.py -v --tb=short` — all tests pass
- `python3 -m pytest tests/test_characterization_pure_functions.py -v --tb=short` — all tests pass
- Tests do not connect to D1, network, or external APIs (confirmed by running in isolation)
</verification>

<success_criteria>
- 13+ characterization tests exist for pure functions (validate_final_cards, validate_thread)
- All tests pass with `python3 -m pytest tests/test_characterization_*.py -v`
- Tests are fully hermetic — no D1, network, or API dependencies (TST-01)
- Tests serve as regression gate for Phase 4 monolith splitting
</success_criteria>

<output>
Create `.planning/phases/03-landing-zone-orchestrator/03-05-SUMMARY.md` when done
</output>
