---
phase: 03-landing-zone-orchestrator
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - pipeline/steps/__init__.py
  - pipeline/steps/step_run_threads.py
  - pipeline/threads/__init__.py
autonomous: true
requirements: [DIR-01, DIR-02, DIR-03, DIR-05, THR-02]
must_haves:
  truths:
    - "pipeline/steps/ directory exists with step wrappers callable by orchestrator"
    - "pipeline/threads/ directory exists as container for future Threads modules"
    - "Step wrapper calls main_v3.py --once via subprocess (Strangler Fig)"
    - "Old scripts/threads/* files remain unchanged (Strangler Fig transition)"
    - "Each step wrapper can run standalone via `python -m pipeline.steps.step_run_threads`"
  artifacts:
    - path: "pipeline/steps/step_run_threads.py"
      provides: "Single step calling main_v3.py --once via subprocess"
      min_lines: 40
      contains: "subprocess.run|class StepRunThreads|def run|venv_python.*main_v3"
    - path: "pipeline/steps/__init__.py"
      provides: "Package init, exports StepRunThreads"
      contains: "StepRunThreads"
    - path: "pipeline/threads/__init__.py"
      provides: "Threads package marker"
      exists: true
  key_links:
    - from: "pipeline/steps/step_run_threads.py"
      to: "scripts/threads/main_v3.py"
      via: "subprocess.run([venv_python, main_v3_script, '--once'])"
    - from: "pipeline/steps/step_run_threads.py"
      to: "pipeline.infra.config.project_root"
      via: "import for PROJECT_DIR path resolution"
---

<objective>
Create the directory structure for pipeline steps and threads modules. Create a single Strangler Fig step wrapper (`step_run_threads.py`) that calls the existing `main_v3.py --once` via subprocess. Old files stay untouched.

Purpose: Establish the structural layout (DIR-01, DIR-02) with thin wrappers (DIR-05) so the orchestrator from Plan 1 can control the existing pipeline. The step wrapper preserves Strangler Fig — old scripts remain the actual implementation, the wrapper only delegates via subprocess.
Output: `pipeline/steps/__init__.py`, `pipeline/steps/step_run_threads.py`, `pipeline/threads/__init__.py`
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-landing-zone-orchestrator/03-RESEARCH.md

<interfaces>
From pipeline/infra/config.py:
```python
def project_root() -> Path
```

From scripts/threads/main_v3.py:
- Entry point: `python3 scripts/threads/main_v3.py --once` runs full pipeline cycle
- Args: --dry-run (dry run), --once (single execution), --daemon (to be removed in Plan 4)
- The script already imports from pipeline.infra.* modules (Phase 2 wiring complete)
- Shebang is hardcoded (`#!/Users/twinssn/Projects/aikorea24/.venv/bin/python3`) — step wrapper uses project_root() to resolve venv python
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pipeline/steps/ directory with StepRunThreads wrapper</name>
  <files>
    pipeline/steps/__init__.py
    pipeline/steps/step_run_threads.py
  </files>
  <read_first>
    pipeline/infra/config.py
    scripts/threads/main_v3.py
    pipeline/orchestrator.py
  </read_first>
  <action>
    A. Create `pipeline/steps/__init__.py`:
    - Import and re-export: `from pipeline.steps.step_run_threads import StepRunThreads`
    - `__all__ = ["StepRunThreads"]`
    - Korean comment header: "# pipeline/steps/ — pipeline step wrappers (Strangler Fig)"

    B. Create `pipeline/steps/step_run_threads.py`:

    - Imports: subprocess, sys, time, Path from pathlib
    - Import project_root from pipeline.infra.config
    - Compute PROJECT_DIR = project_root()
    - Compute VENV_PYTHON = str(PROJECT_DIR / ".venv" / "bin" / "python3")
    - Compute OLD_SCRIPT = str(PROJECT_DIR / "scripts" / "threads" / "main_v3.py")

    - Define `class StepRunThreads`:
      - `name: str = "run_threads"` (class-level attribute matching PipelineStep protocol)
      - `def run(self) -> int`: 
        - Call `subprocess.run([VENV_PYTHON, OLD_SCRIPT, "--once"], capture_output=True, text=True, timeout=600, cwd=str(PROJECT_DIR))`
        - Print stdout to stdout, stderr to stderr
        - Return result.returncode (0 = success)
        - On subprocess.TimeoutExpired: print error to stderr, return 1
        - On Exception: print error to stderr, return 1

    - Add `if __name__ == "__main__":` block:
      - Import sys
      - Step = StepRunThreads()
      - print(f"Running step: {step.name}")
      - exit_code = step.run()
      - print(f"Step completed with exit code: {exit_code}")
      - sys.exit(exit_code)

    - All comments in Korean per D-13.
    - Follow the Strangler Fig wrapper pattern from 03-RESEARCH.md lines 297-320 (Pattern 2). The wrapper calls the old script via subprocess — NOT by importing Python modules from it. This prevents circular import issues and keeps old files as black boxes.
  </action>
  <acceptance_criteria>
    - `python3 -c "from pipeline.steps import StepRunThreads; s = StepRunThreads(); print(s.name)"` outputs "run_threads"
    - `python3 -c "from pipeline.steps.step_run_threads import StepRunThreads; from pipeline.orchestrator import PipelineStep; assert isinstance(StepRunThreads(), PipelineStep)"` passes (structural subtyping via Protocol)
    - Step class has name="run_threads" and run() returning int
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from pipeline.steps.step_run_threads import StepRunThreads; from pipeline.orchestrator import PipelineStep; s = StepRunThreads(); assert s.name == 'run_threads'; assert isinstance(s, PipelineStep); print('Step conforms to PipelineStep protocol')"</automated>
  </verify>
  <done>pipeline/steps/ created with StepRunThreads conforming to PipelineStep protocol</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline/threads/ directory with __init__.py</name>
  <files>
    pipeline/threads/__init__.py
  </files>
  <read_first>
    pipeline/infra/__init__.py
  </read_first>
  <action>
    Create `pipeline/threads/__init__.py`:
    - Korean comment header: "# pipeline/threads/ — Threads pipeline modules (Phase 4 will populate)"
    - Empty __init__.py with just the comment. No imports — this directory is a container for Phase 4 monolith splitting.
    - This satisfies DIR-02: "Create pipeline/threads/ with flattened v3/ nesting". The v3/ nesting under scripts/threads/ stays untouched (Strangler Fig). The new pipeline/threads/ is flat.
  </action>
  <acceptance_criteria>
    - `pipeline/threads/__init__.py` exists
    - `python3 -c "import pipeline.threads"` succeeds (empty package)
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import pipeline.threads; print('pipeline.threads package OK')"</automated>
  </verify>
  <done>pipeline/threads/ directory created with skeletal __init__.py</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Strangler Fig — old scripts still runnable</name>
  <files>
    scripts/threads/main_v3.py
  </files>
  <read_first>
    scripts/threads/main_v3.py
  </read_first>
  <what-built>
    pipeline/steps/step_run_threads.py wrapper and pipeline/threads/ directory. No old files were modified.
  </what-built>
  <how-to-verify>
    1. Run `python3 scripts/threads/main_v3.py --dry-run` to confirm old entry point still works
    2. Run `python3 -m pipeline.steps.step_run_threads` to confirm the new wrapper works (this will call main_v3.py --once via subprocess, which triggers --dry-run behavior since no --dry-run flag is passed — actually the step wrapper calls --once, which will do a real run if env vars are set. For dry-run testing, the user should check code or run with environment check.)
    3. Confirm scripts/threads/main_v3.py is unchanged (git diff shows no changes to old files)
  </what-built>
  <how-to-verify>
    Confirm no old files were modified:
    ```bash
    cd /Users/twinssn/Projects/aikorea24
    python3 -m pipeline.steps.step_run_threads --dry-run 2>&1 | head -5
    ```
    (This is a checkpoint because step_run_threads with --once will try to actually contact D1. Verify the wrapper correctly constructs paths and doesn't crash.)
  </how-to-verify>
  <resume-signal>
    Type "approved" to confirm, or describe any issues found
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess.call → main_v3.py | Step wrapper invokes old script via subprocess array (not shell=True) |
| pip installs | No new packages; stdlib only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | subprocess argument injection | mitigate | Step wrapper uses fixed `subprocess.run([VENV_PYTHON, OLD_SCRIPT, "--once"], shell=False)` — list form prevents shell injection. Paths are computed from project_root(), not user input. |
| T-03-06 | Tampering | pip installs | mitigate | No new pip packages. Python 3.14 stdlib only. |
</threat_model>

<verification>
- python3 -c "from pipeline.steps import StepRunThreads" succeeds
- python3 -c "from pipeline.threads import *" succeeds (empty package)
- python3 -c "from pipeline.steps.step_run_threads import StepRunThreads; from pipeline.orchestrator import PipelineStep; assert isinstance(StepRunThreads(), PipelineStep)" passes
- git diff --name-only shows no old files modified
</verification>

<success_criteria>
- `pipeline/steps/` directory exists with StepRunThreads conforming to PipelineStep protocol (DIR-01)
- `pipeline/threads/` directory exists as flat container (DIR-02)
- `pipeline/infra/` already exists from Phase 2 (DIR-03 — verified)
- Old scripts/threads/* files remain unchanged (DIR-05 — Strangler Fig preserved)
- Step wrapper uses pipeline.infra.config.project_root and subprocess (THR-02 — shared infra)
</success_criteria>

<output>
Create `.planning/phases/03-landing-zone-orchestrator/03-02-SUMMARY.md` when done
</output>
