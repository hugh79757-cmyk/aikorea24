---
phase: 42-tools-phase1-adjust
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/001_tool_submissions.sql
  - sql/002_tools_phase1_adjust.sql
  - src/pages/api/tools/submit.ts
autonomous: true
requirements:
  - REQ-42-01 (status published/hidden, D-01)
  - REQ-42-02 (price_model + price_detail, D-02)
  - REQ-42-04 (category_custom, D-04)
  - REQ-42-05 (screenshot_url API validation, D-05)
  - REQ-42-06 (Phase 3 ban, D-06)
user_setup: []
estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "신규 환경에서 001 파일 하나로 최종 스키마가 구성된다 (price 없음, price_model/price_detail/category_custom/screenshot_url 있음)"
    - "기존 local/remote D1의 price 값이 규칙대로 price_model/price_detail에 보존된다 (현재 0행이므로 규칙 정의 + 적용 성공)"
    - "POST /api/tools/submit이 price_model 3택 필수 검증 + price_detail/category_custom/screenshot_url 처리를 수행하고 description 200 캡을 유지한다"
  artifacts:
    - sql/001_tool_submissions.sql (최종 스키마로 갱신)
    - sql/002_tools_phase1_adjust.sql (신규 마이그레이션)
    - src/pages/api/tools/submit.ts (신규 필드 검증 + INSERT 교체)
  key_links:
    - 002 migration local apply → remote apply → PRAGMA verify
    - submit.ts INSERT column list ↔ DDL final schema match
---

<objective>
DDL 정합 + API 검증 — 시니어 결정 D-01/D-02/D-04/D-05/D-06의 백엔드 절반. 001 최종 스키마 갱신, 002 마이그레이션 작성·적용(local+remote), submit.ts 신규 필드 검증·INSERT 교체.
Purpose: 프론트 작업(42-02)이 의존하는 스키마 진실원 확정. D1 0행이므로 데이터 손실 리스크 없음 (RESEARCH live probe).
Output: 001 갱신, 002 신규 + local/remote 적용 증거, submit.ts 수정.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-tools-phase1-adjust/CONTEXT.md
@.planning/phases/42-tools-phase1-adjust/RESEARCH.md
@sql/001_tool_submissions.sql
@src/pages/api/tools/submit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 001 최종 스키마 갱신 + 002 마이그레이션 작성·적용 (D-01/D-02/D-04/D-05 per CONTEXT Step 0)</name>
  <files>sql/001_tool_submissions.sql, sql/002_tools_phase1_adjust.sql</files>
  <action>Update sql/001_tool_submissions.sql CREATE TABLE to final schema: replace `price TEXT,` line with `price_model TEXT,` + `price_detail TEXT,`, add `category_custom TEXT,` and `screenshot_url TEXT,` near category/description lines, keep `status TEXT DEFAULT 'published'` untouched (D-01 already matches, RESEARCH §Current State 1). Create sql/002_tools_phase1_adjust.sql with RESEARCH §ALTER TABLE Migration Path recommended file verbatim: 4x ADD COLUMN (price_model, price_detail, category_custom, screenshot_url) + price backfill UPDATE (무료→price_model 무료 / 유료→유료 / other non-empty→Freemium + price_detail=원값) + DROP COLUMN price. If DROP COLUMN is rejected by D1, leave price column in place and ignore it in code (harmless — all read queries use explicit column lists, RESEARCH §Pitfall). Apply with `env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler d1 execute aikorea24-db --local --file=sql/002_tools_phase1_adjust.sql` then `--remote`, verify both with PRAGMA table_info. Do NOT touch network_schema.sql or persona_migration.sql. Do NOT start any Phase 3 work (D-06).</action>
  <verify><automated>env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler d1 execute aikorea24-db --local --command="PRAGMA table_info(tool_submissions);" | grep -E "price_model|price_detail|category_custom|screenshot_url"; env -u CLOUDFLARE_API_TOKEN /opt/homebrew/bin/wrangler d1 execute aikorea24-db --remote --command="PRAGMA table_info(tool_submissions);" | grep -E "price_model|price_detail|category_custom|screenshot_url"; grep -c "price_model" sql/001_tool_submissions.sql</automated></verify>
  <done>Local + remote PRAGMA both show price_model/price_detail/category_custom/screenshot_url columns; 001 contains price_model and no standalone `price TEXT` line.</done>
</task>

<task type="auto">
  <name>Task 2: submit.ts 신규 필드 검증 + INSERT 교체 (D-02/D-04/D-05 per CONTEXT Step 1)</name>
  <files>src/pages/api/tools/submit.ts</files>
  <action>Extend submit.ts body destructure with price_model, price_detail, category_custom, screenshot_url (drop price). Add validations in existing 400-JSON style: price_model required, must be one of 무료/Freemium/유료 else 400; price_detail optional free text (trim, null if empty); category_custom: when category is 기타, accept trimmed string or null (do NOT force required — senior decision only says input is shown, no required rule); screenshot_url optional — empty/null skips, non-empty must pass the same http(s) URL check pattern as url field (submit.ts:72-79, reuse shape with empty-skip branch). Keep description 200-char 400 (D-03 server side unchanged), ALLOWED_CATEGORIES 8 entries unchanged (D-04 matches), tasks max-5 + TASKS check unchanged, slug md+D1 dedup unchanged. Replace INSERT column list: price → price_model, price_detail, category_custom, screenshot_url with matching bind values in order. Server 400 stays source of truth (client disable is bypassable).</action>
  <verify><automated>npm run build 2>&1 | tail -5; grep -c "price_model" src/pages/api/tools/submit.ts; grep -c "screenshot_url" src/pages/api/tools/submit.ts</automated></verify>
  <done>submit.ts validates price_model 3택 + optional price_detail/category_custom/screenshot_url(URL-only), INSERT writes 4 new columns, build passes.</done>
</task>

</tasks>

<verification>
PRAGMA local+remote shows 4 new columns. npm run build passes. No Phase 3 files created (no R2/my/PUT/DELETE). git diff --stat src/content/tools/ empty.
</verification>

<success_criteria>
DDL truth established (001 final + 002 applied both DBs) and API accepts new fields with 400-shape errors. 42-02 may proceed.
</success_criteria>

<output>
Create `.planning/phases/42-tools-phase1-adjust/42-01-SUMMARY.md` when done
</output>
