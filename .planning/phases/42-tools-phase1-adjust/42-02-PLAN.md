---
phase: 42-tools-phase1-adjust
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - src/pages/tools/submit.astro
  - src/pages/tools/index.astro
  - src/pages/tools/[id].astro
autonomous: true
requirements:
  - REQ-42-01 (index WHERE published, D-01)
  - REQ-42-02 (price_detail || price_model display, D-02)
  - REQ-42-03 (desc counter + disable, D-03)
  - REQ-42-04 (기타→희망카테고리 toggle, D-04)
  - REQ-42-05 (screenshot_url input + CAFE_URL const, D-05)
  - REQ-42-06 (Phase 3 ban, D-06)
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "등록 폼에 price_model 라디오(무료/Freemium/유료) + price_detail 입력 + 기타 선택 시 희망 카테고리 입력 + screenshot_url 입력 + 42/200 카운터(초과 시 빨강 + 제출 비활성화)가 동작한다"
    - "목록이 published 행만 조회하고 카드 가격을 price_detail || price_model으로 표시한다"
    - "상세 D1 렌더가 동일 표시 규칙을 따르고, hidden 직접 URL 접근은 허용 유지되며, 빌드 + E2E curl이 통과한다"
  artifacts:
    - src/pages/tools/submit.astro (폼 조정)
    - src/pages/tools/index.astro (WHERE + 표시 규칙)
    - src/pages/tools/[id].astro (표시 규칙)
  key_links:
    - submit.astro payload keys ↔ submit.ts destructure keys match
    - index/[id] SELECT lists ↔ 002 final schema match
    - completion preview price ↔ price_detail || price_model rule
---

<objective>
폼·목록·상세 프론트 조정 + 빌드/E2E — 시니어 결정 D-01..D-06의 사용자 가시 절반. submit.astro 폼 교체, index WHERE+표시, [id] 표시, npm run build + curl E2E.
Purpose: Phase 41 구현을 6개 locked 결정에 정합. 서버 검증(42-01)이 진실 공급원, 클라이언트는 선방어.
Output: 3 files modified, build green, E2E curl matrix green.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-tools-phase1-adjust/CONTEXT.md
@.planning/phases/42-tools-phase1-adjust/RESEARCH.md
@.planning/phases/42-tools-phase1-adjust/42-01-SUMMARY.md
@src/pages/tools/submit.astro
@src/pages/tools/index.astro
@src/pages/tools/[id].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: submit.astro 폼 조정 (D-02/D-03/D-04/D-05 per CONTEXT Step 2)</name>
  <files>src/pages/tools/submit.astro</files>
  <action>Replace price select (f-price, submit.astro:71-75) with price_model radio group (무료/Freemium/유료, required) + price_detail free-text input (optional, e.g. 월 9,900원부터). Add category_custom text input (희망 카테고리) hidden by default, toggled visible only when f-category is 기타 (D-04). Add screenshot_url url input in Step 2 block (D-05, optional). Upgrade desc counter: existing span#desc-count (:59) + input listener (:185-187) shows `N/200`; when N>200 add red class to counter and disable #btn-submit, re-enable when back within limit (maxlength=200 stays but JS length is source of truth since paste truncates silently — RESEARCH §Current State 3). Keep client validateStep1 200-check. Define `const CAFE_URL = 'https://cafe.naver.com/gptdohye'` once at top of client script and use it in both cafe shortcut href (:155) and dataset.shareText template (:302-303) — replace both hardcoded strings. Update fetch payload: price → price_model + price_detail + category_custom (null unless 기타) + screenshot_url (null if empty). Update completion preview (:293, :301): preview-price = price_detail.trim() || price_model || '—'. Keep SSR guard, MAKER badge classes verbatim, tasks max-5, no /my/tools (D-06).</action>
  <verify><automated>npm run build 2>&1 | tail -5; grep -c "CAFE_URL" src/pages/tools/submit.astro; grep -c "category_custom" src/pages/tools/submit.astro; grep -c "screenshot_url" src/pages/tools/submit.astro; grep -c "price_model" src/pages/tools/submit.astro</automated></verify>
  <done>Form shows radios + price_detail + 기타 toggle + screenshot_url + N/200 counter with red+disable over 200; payload keys match submit.ts; single CAFE_URL const, zero remaining hardcoded cafe URLs in script logic.</done>
</task>

<task type="auto">
  <name>Task 2: index.astro WHERE + 표시 규칙 (D-01/D-02 per CONTEXT Step 3)</name>
  <files>src/pages/tools/index.astro</files>
  <action>In index.astro D1 query (:13-15) add `WHERE status='published'` (D-01) and extend explicit SELECT list with price_model, price_detail (replacing price in list). Replace mapping `price: r.price ?? '—'` (:38) with display rule `price: (r.price_detail && String(r.price_detail).trim()) || r.price_model || '—'` (D-02). Update data-price filter attr (:321) to use the same computed display value so free-filter stays consistent. LOCKED CHOICE on Open Question 1 (flag for verify): leave matchFree `includes('무료')` logic unchanged — Freemium tools are excluded from the 무료-only filter. Rationale: senior decision defines no filter rule, minimal-change principle; senior to confirm in review. Keep fail-soft try/catch, MAKER badge classes verbatim, D1-first merge order, pagination inclusion.</action>
  <verify><automated>npm run build 2>&1 | tail -5; grep -c "WHERE status='published'" src/pages/tools/index.astro; grep -c "price_model" src/pages/tools/index.astro</automated></verify>
  <done>Index queries published-only with price_detail || price_model display; Freemium filter behavior explicitly flagged for senior confirm.</done>
</task>

<task type="auto">
  <name>Task 3: [id].astro 표시 규칙 + 빌드/E2E (D-02 per CONTEXT Step 4)</name>
  <files>src/pages/tools/[id].astro</files>
  <action>In [id].astro D1 fallback query (:28-30) extend SELECT list with price_model, price_detail, category_custom, screenshot_url (replacing price in list). Replace `price: row.price ?? '—'` (:53) with same display rule as index Task 2. Render screenshot_url as a link row in D1 detail when present (recommended placement: near URL meta row; hide section when empty per existing {x && (...)} conditional patterns). LOCKED CHOICE on Open Question 2 (flag for verify): do NOT add status filter to [id] query — hidden rows remain directly accessible by URL. Rationale: CONTEXT Step 4 scopes status filtering to index only. Keep MAKER badge verbatim, body detail_markdown||description, 404 on miss. Then start local dev server (`npm run dev -- --port 4321 &`, kill after matrix), run npm run build + local E2E curl matrix with code asserts (fail gate on mismatch): 401 unauthenticated POST, 400 bad price_model, 400 bad screenshot_url, 400 desc>200, 201 valid submit (price_model=Freemium + price_detail + category_custom + screenshot_url), list shows MAKER row with detail||model price, detail renders, hidden-row direct access returns 200 (documents locked choice), unknown slug 404s.</action>
  <verify><automated>npm run build 2>&1 | tail -5; [ "$(curl -s -o /dev/null -w "%{http_code}" -X POST localhost:4321/api/tools/submit -H 'Content-Type: application/json' -d '{"name":"t"}')" = "401" ]; [ "$(curl -s -o /dev/null -w "%{http_code}" -X POST localhost:4321/api/tools/submit -H 'Content-Type: application/json' -d '{"name":"t","url":"https://x.com","category":"기타","description":"d","price_model":"WRONG"}' --cookie "session=$TEST_SESSION")" = "400" ]; [ "$(curl -s -o /dev/null -w "%{http_code}" localhost:4321/tools/no-such-tool-xyz/)" = "404" ]; curl -s localhost:4321/tools/ | grep -q "MAKER"; curl -s -o /dev/null -w "md:%{http_code}\n" localhost:4321/tools/chatgpt-work/</automated></verify>
  <done>Detail shows price_detail || price_model + optional screenshot link; build green; E2E matrix (401/400x3/201/list/detail/hidden-200/unknown-404) recorded in SUMMARY with both locked open-question choices flagged.</done>
</task>

</tasks>

<verification>
npm run build passes. E2E curl matrix recorded (401/400/201/list/detail/404). Freemium-filter + hidden-direct-access locked choices flagged for senior verify. git diff --stat src/content/tools/ empty, no Phase 3 files.
</verification>

<success_criteria>
All 6 locked decisions visibly implemented: published-only list, model+detail price everywhere, counter+disable, 기타 toggle, screenshot+CAFE_URL, zero Phase 3 work. Both open questions decided, implemented, and flagged.
</success_criteria>

<output>
Create `.planning/phases/42-tools-phase1-adjust/42-02-SUMMARY.md` when done
</output>
