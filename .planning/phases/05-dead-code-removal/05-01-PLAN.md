---
phase: 05-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # backup_*.txt (14 files)
  - backup_20260212_174254.txt
  - backup_20260213_084152.txt
  - backup_20260213_114840.txt
  - backup_20260215_103711.txt
  - backup_20260216_162817.txt
  - backup_20260221_102430.txt
  - backup_20260222_071737.txt
  - backup_20260222_095838.txt
  - backup_20260224_080523.txt
  - backup_20260225_084412.txt
  - backup_20260226_075903.txt
  - backup_20260228_134606.txt
  - backup_20260304_095648.txt
  - backup_20260312_103452.txt
  # .bak* files (80+ files)
  # patch_*.py (2 files)
  - patch_footer.py
  - patch_existing.py
  # test_*.py in root (3 files)
  - test_post.py
  - test_wellness_api.py
  - test_wellness_debug.py
  # test_*.py in scripts/ (3 files)
  - scripts/test_crawl_sources.py
  - scripts/test_email_send.py
  - scripts/threads/v3/test_model.py
  # spotlight_*.sh (4 files)
  - spotlight_exclude.sh
  - spotlight_verify.sh
  - spotlight_check.sh
  - spotlight_check2.sh
  # quick_check.sh
  - quick_check.sh
  # Standalone dead utilities
  - scripts/threads/scorer.py
  - scripts/threads/enricher.py
  - scripts/threads/backfill_meta.py
  - scripts/threads/run_dry.py
  # Shell scripts
  - scripts/threads/run_loop.sh
  # archived/ directory (6 files)
  - scripts/threads/archived/main.py
  - scripts/threads/archived/main_v2.py
  - scripts/threads/archived/scorer_v2.py
  - scripts/threads/archived/writer.py
  - scripts/threads/archived/writer_v2.py
  - scripts/threads/archived/db_reader_v2.py
  # Legacy prompt files
  - scripts/threads/prompts/prompt_00_selector.md
  - scripts/threads/prompts/prompt_rules.md
  - scripts/threads/prompts_legacy/prompt_00_selector.md
  - scripts/threads/prompts_legacy/prompt_rules.md
  # Generated plist (replaced by template)
  - scripts/threads/threads-publisher.plist
  # Extra .bak files not covered by .bak glob
  - scripts/threads/scorer.py.bak
  - scripts/threads/db_reader.py.bak
  - scripts/threads/writer.py.bak
  - scripts/dynamic_seed_generator.py.bak
  - scripts/seeds.json.bak
autonomous: true
requirements:
  - DED-01  # No backup files
  - DED-02  # No abandoned scripts
user_setup: []
must_haves:
  truths:
    - "No backup_*.txt files exist anywhere in the repository"
    - "No .bak, .bak2, .bak3, .bak_* files exist anywhere in the repository (excluding .venv/ and node_modules/)"
    - "No patch_*.py, test_*.py (outside tests/), spotlight_*.sh, quick_check.sh files exist in project root or scripts/"
    - "No standalone dead utilities (scorer.py, enricher.py, backfill_meta.py, run_dry.py) remain in scripts/threads/"
    - "No archived/ directory exists under scripts/threads/"
    - "No legacy prompt files exist under scripts/threads/prompts/ or prompts_legacy/"
    - "No generated plist (threads-publisher.plist) exists under scripts/threads/"
    - "All 173+ existing tests still pass"
  artifacts:
    - path: "<removed>"
      provides: "14 backup_*.txt files removed from project root"
    - path: "<removed>"
      provides: "80+ .bak* files removed across the project"
    - path: "<removed>"
      provides: "3 root-level test_*.py removed (ad-hoc scripts, not pytest tests)"
    - path: "<removed>"
      provides: "3 scripts-level test_*.py removed (test_crawl_sources, test_email_send, test_model)"
    - path: "<removed>"
      provides: "2 patch_*.py files removed"
    - path: "<removed>"
      provides: "4 spotlight_*.sh files + quick_check.sh removed"
    - path: "<removed>"
      provides: "4 standalone dead utilities removed from scripts/threads/"
    - path: "<removed>"
      provides: "scripts/threads/run_loop.sh removed"
    - path: "<removed>"
      provides: "scripts/threads/archived/ directory removed"
    - path: "<removed>"
      provides: "scripts/threads/prompts/ and prompts_legacy/ directories removed"
    - path: "<removed>"
      provides: "scripts/threads/threads-publisher.plist old generated plist removed"
    - path: "tests/"
      provides: "173+ tests still pass after all removals"
  key_links:
    - from: "backup_*.txt"
      to: "<removed>"
      via: "git rm"
    - from: "*.bak*"
      to: "<removed>"
      via: "git rm"
    - from: "patch_*.py, test_*.py, spotlight_*.sh, quick_check.sh"
      to: "<removed>"
      via: "git rm"
---

<objective>
Remove all dead files from the repository: backup_*.txt (14), .bak* files (80+), abandoned scripts (patch_*.py, test_*.py outside tests/, spotlight_*.sh, quick_check.sh), standalone dead utilities, archived/ directory, legacy prompt files, and the old generated plist.

Purpose: DED-01 and DED-02 require a clean repository with no backup files or abandoned scripts. These files have accumulated over months of development and have no remaining callers or consumers.

Output:
- Clean repository state: ~110+ dead files removed
- All 173+ existing tests continue to pass (no test-impacting removals in this plan)
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dead-code-removal/05-RESEARCH.md

**Critical note:** This plan removes ONLY files with no impact on the running pipeline. Files that are imported by pipeline modules or tests are handled in Plan 05-02. The inventory in 05-RESEARCH.md Section "Backup/Abandoned File Inventory" and "Standalone Python utilities" is confirmed accurate — every file listed below has been grep-checked for callers. No file in this plan's removal list is imported by any pipeline module or pytest test file.

**Pattern for deletion:** Use `git rm` for tracked files, `rm` for untracked ones. Each task runs `pytest tests/ -m "unit" --tb=short` after deletions to confirm no regressions.

**No .bak files in .venv/ or node_modules/ are touched.**

**Extra files not in research but found during verification:** api_test/ contains many .bak variants (bak2-19, bak_*) — these are covered by the glob-based removal task that targets ALL *.bak* patterns project-wide.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 14 backup_*.txt files from project root</name>
  <files>backup_20260212_174254.txt, backup_20260213_084152.txt, backup_20260213_114840.txt, backup_20260215_103711.txt, backup_20260216_162817.txt, backup_20260221_102430.txt, backup_20260222_071737.txt, backup_20260222_095838.txt, backup_20260224_080523.txt, backup_20260225_084412.txt, backup_20260226_075903.txt, backup_20260228_134606.txt, backup_20260304_095648.txt, backup_20260312_103452.txt</files>
  <action>
    Remove all 14 timestamped backup files from project root. These are old manual backups from Feb-Mar 2026, safe to delete.

    ```bash
    # Remove all backup_*.txt files from project root
    for f in backup_*.txt; do
      if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
        git rm "$f"
      else
        rm "$f"
      fi
    done
    ```
  </action>
  <verify>
    <automated>
      test ! -f backup_20260212_174254.txt && echo "backup files removed OK"
    </automated>
  </verify>
  <done>No backup_*.txt files exist in project root. `ls backup_*.txt 2>&1` returns "No such file or directory".</done>
</task>

<task type="auto">
  <name>Task 2: Remove all .bak* files project-wide (excluding .venv/ and node_modules/)</name>
  <files>80+ files with .bak, .bak2, .bak3, .bak4, .bak_* extensions across the project</files>
  <action>
    Remove ALL files matching `*.bak*` patterns across the project, excluding .venv/ and node_modules/ directories.

    Use a find-based approach that tracks via git:

    ```bash
    # Find all .bak* files, exclude .venv/ and node_modules/
    find . \( -name '*.bak' -o -name '*.bak[0-9]*' -o -name '*.bak_*' \) \
      -not -path './.git/*' \
      -not -path './node_modules/*' \
      -not -path './.venv/*' \
      -not -path './dist/*' 2>/dev/null | while read f; do
      if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
        git rm "$f"
      else
        rm "$f"
      fi
    done
    ```

    Note: `dist/` contains only one file (`OG-1.webp.bak`) which is in the build output directory — not tracked by git, not relevant to the project. Skip dist/ unless it contains important .bak files. Actually `dist/` is generated output, skip it.

    **IMPORTANT:** Do NOT remove `.env.bak.telegram` (this is an env backup, not a .bak file). Only remove files where the EXTENSION is .bak, .bak2, .bak3, etc.
  </action>
  <verify>
    <automated>
      COUNT=$(find . \( -name '*.bak' -o -name '*.bak[0-9]*' -o -name '*.bak_*' \) -not -path './.git/*' -not -path './node_modules/*' -not -path './.venv/*' -not -path './dist/*' 2>/dev/null | wc -l)
      test "$COUNT" -eq 0 && echo "All .bak* files removed ($COUNT remaining)"
    </automated>
  </verify>
  <done>No .bak, .bak2, .bak3, .bak4, .bak_* files remain in the working tree (excluding .venv/, node_modules/, .git/).</done>
</task>

<task type="auto">
  <name>Task 3: Remove abandoned scripts (patch_*.py, test_*.py outside tests/, spotlight_*.sh, quick_check.sh)</name>
  <files>patch_footer.py, patch_existing.py, test_post.py, test_wellness_api.py, test_wellness_debug.py, scripts/test_crawl_sources.py, scripts/test_email_send.py, scripts/threads/v3/test_model.py, spotlight_exclude.sh, spotlight_verify.sh, spotlight_check.sh, spotlight_check2.sh, quick_check.sh</files>
  <action>
    Remove all abandoned scripts using git rm for tracked files:

    ```bash
    for f in \
      patch_footer.py patch_existing.py \
      test_post.py test_wellness_api.py test_wellness_debug.py \
      scripts/test_crawl_sources.py scripts/test_email_send.py \
      scripts/threads/v3/test_model.py \
      spotlight_exclude.sh spotlight_verify.sh spotlight_check.sh spotlight_check2.sh \
      quick_check.sh; do
      if [ -f "$f" ]; then
        if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
          git rm "$f"
        else
          rm "$f"
        fi
      fi
    done
    ```

    **Note about `test_post.py`, `test_wellness_api.py`, `test_wellness_debug.py`:** These are ad-hoc API test scripts in the project root, not pytest tests. They import `requests` and `dotenv` and make live HTTP calls. Safe to remove — not imported by any pipeline module or pytest test file. Confirmed by grep.
  </action>
  <verify>
    <automated>
      for f in patch_footer.py patch_existing.py test_post.py test_wellness_api.py test_wellness_debug.py scripts/test_crawl_sources.py scripts/test_email_send.py scripts/threads/v3/test_model.py spotlight_exclude.sh spotlight_verify.sh spotlight_check.sh spotlight_check2.sh quick_check.sh; do
        test ! -f "$f" || { echo "FAIL: $f still exists"; exit 1; }
      done
      echo "All abandoned scripts removed OK"
    </automated>
  </verify>
  <done>No patch_*.py, test_*.py (outside tests/), spotlight_*.sh, or quick_check.sh files remain. All 13 files confirmed removed.</done>
</task>

<task type="auto">
  <name>Task 4: Remove standalone dead utilities, shell scripts, archived/, prompts/, generated plist</name>
  <files>scripts/threads/scorer.py, scripts/threads/enricher.py, scripts/threads/backfill_meta.py, scripts/threads/run_dry.py, scripts/threads/run_loop.sh, scripts/threads/archived/main.py, scripts/threads/archived/main_v2.py, scripts/threads/archived/scorer_v2.py, scripts/threads/archived/writer.py, scripts/threads/archived/writer_v2.py, scripts/threads/archived/db_reader_v2.py, scripts/threads/prompts/prompt_00_selector.md, scripts/threads/prompts/prompt_rules.md, scripts/threads/prompts_legacy/prompt_00_selector.md, scripts/threads/prompts_legacy/prompt_rules.md, scripts/threads/threads-publisher.plist</files>
  <action>
    Remove all remaining dead files:

    **Standalone dead utilities (confirmed by grep — none imported by pipeline code):**
    - `scripts/threads/scorer.py` — Legacy scoring, not imported
    - `scripts/threads/enricher.py` — Cluster enrichment, only imported by archived scripts
    - `scripts/threads/backfill_meta.py` — One-time backfill tool
    - `scripts/threads/run_dry.py` — Debug dry-run loop

    **Shell scripts:**
    - `scripts/threads/run_loop.sh` — Debug dry-run loop

    **archived/ directory (6 files):**
    - All v1/v2 files that have been superseded by v3/pipeline modules

    **Legacy prompt files:**
    - `scripts/threads/prompts/` — A/B/C/D selector guide (now D-only) and inlined rules
    - `scripts/threads/prompts_legacy/` — Legacy versions

    **Generated plist:**
    - `scripts/threads/threads-publisher.plist` — Old hard-coded plist; template exists at scripts/threads/.template/threads-publisher.plist

    ```bash
    # Remove standalone utilities
    for f in \
      scripts/threads/scorer.py \
      scripts/threads/enricher.py \
      scripts/threads/backfill_meta.py \
      scripts/threads/run_dry.py \
      scripts/threads/run_loop.sh; do
      if [ -f "$f" ]; then
        if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
          git rm "$f"
        else
          rm "$f"
        fi
      fi
    done

    # Remove archived/ directory
    if [ -d scripts/threads/archived ]; then
      for f in scripts/threads/archived/*; do
        if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
          git rm "$f"
        else
          rm "$f"
        fi
      done
      rmdir scripts/threads/archived 2>/dev/null || true
    fi

    # Remove legacy prompt files
    if [ -d scripts/threads/prompts ]; then
      for f in scripts/threads/prompts/*; do
        if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
          git rm "$f"
        else
          rm "$f"
        fi
      done
      rmdir scripts/threads/prompts 2>/dev/null || true
    fi
    if [ -d scripts/threads/prompts_legacy ]; then
      for f in scripts/threads/prompts_legacy/*; do
        if git ls-files --error-unmatch "$f" >/dev/null 2>&1; then
          git rm "$f"
        else
          rm "$f"
        fi
      done
      rmdir scripts/threads/prompts_legacy 2>/dev/null || true
    fi

    # Remove generated plist
    if [ -f scripts/threads/threads-publisher.plist ]; then
      if git ls-files --error-unmatch scripts/threads/threads-publisher.plist >/dev/null 2>&1; then
        git rm scripts/threads/threads-publisher.plist
      else
        rm scripts/threads/threads-publisher.plist
      fi
    fi
    ```
  </action>
  <verify>
    <automated>
      # Verify all targeted files are removed
      for f in scripts/threads/scorer.py scripts/threads/enricher.py scripts/threads/backfill_meta.py scripts/threads/run_dry.py scripts/threads/run_loop.sh; do
        test ! -f "$f" || { echo "FAIL: $f still exists"; exit 1; }
      done
      test ! -d scripts/threads/archived || { echo "FAIL: archived/ still exists"; exit 1; }
      test ! -d scripts/threads/prompts || { echo "FAIL: prompts/ still exists"; exit 1; }
      test ! -d scripts/threads/prompts_legacy || { echo "FAIL: prompts_legacy/ still exists"; exit 1; }
      test ! -f scripts/threads/threads-publisher.plist || { echo "FAIL: plist still exists"; exit 1; }
      echo "All remaining dead files removed OK"
    </automated>
  </verify>
  <done>All standalone utilities, shell scripts, archived/, prompts/, and generated plist confirmed removed.</done>
</task>

<task type="auto">
  <name>Task 5: Run full test suite to confirm zero regressions</name>
  <files>tests/</files>
  <action>
    Run the full pytest suite to confirm that none of the above removals broke any test:

    ```bash
    pytest tests/ --tb=short
    ```

    The test count should remain at 173 (no tests should have been affected since none of the removed files were imported by pytest test files or pipeline modules).
  </action>
  <verify>
    <automated>pytest tests/ --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>All 173+ tests pass. Test count unchanged from baseline (no tests removed, no tests broken).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → git rm | Deleting tracked files via git rm — ensures persistent removal from version history (Tampering) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | git rm operations | mitigate | Each file checked with `git ls-files --error-unmatch` before removal — avoids deleting files that aren't tracked (accidental removal of untracked work). |
| T-05-02 | Denial of Service | Removing imported files | mitigate | Every file in this plan's removal list verified by grep in 05-RESEARCH.md to have zero callers in pipeline modules or test files. Full test suite run in Task 5 confirms no regressions. |
| T-05-SC | Tampering | File removal | mitigate | No new packages installed. All operations use git built-in commands. |
</threat_model>

<verification>
```bash
# Verify no backup_*.txt remain
ls backup_*.txt 2>&1 || echo "OK"

# Verify no .bak* remain (excluding allowed dirs)
COUNT=$(find . \( -name '*.bak' -o -name '*.bak[0-9]*' -o -name '*.bak_*' \) \
  -not -path './.git/*' -not -path './node_modules/*' -not -path './.venv/*' 2>/dev/null | wc -l)
echo "$COUNT .bak* files remaining (should be 0)"

# Verify no abandoned scripts remain
for p in patch_* test_* quick_check* spotlight_*; do
  test -f "$p" && echo "WARNING: $p still exists" || true
done

# Full regression gate
pytest tests/ --tb=short
```
</verification>

<success_criteria>
1. No `backup_*.txt` files exist in project root (confirmed: `ls backup_*.txt` returns error)
2. No `.bak`, `.bak2`, `.bak3`, `.bak4`, `.bak_*` files exist anywhere (confirmed: find count = 0)
3. No `patch_*.py`, `test_*.py` (outside tests/), `spotlight_*.sh`, `quick_check.sh` exist
4. `scripts/threads/scorer.py`, `enricher.py`, `backfill_meta.py`, `run_dry.py`, `run_loop.sh` all removed
5. `scripts/threads/archived/` directory no longer exists
6. `scripts/threads/prompts/` and `scripts/threads/prompts_legacy/` directories no longer exist
7. `scripts/threads/threads-publisher.plist` no longer exists
8. All 173+ tests pass (regression gate)
</success_criteria>

<output>
Create .planning/phases/05-dead-code-removal/05-01-SUMMARY.md when done
</output>
