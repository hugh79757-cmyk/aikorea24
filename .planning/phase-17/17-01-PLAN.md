---
phase: 17-instagram-carousel-shorts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/instagram/__init__.py
  - pipeline/instagram/models.py
  - pipeline/instagram/content_converter.py
autonomous: true
requirements: [REQ-03, REQ-07]
must_haves:
  truths:
    - "Format D cards can be converted to Carousel slide data"
    - "Format D cards can be converted to Reel scene script with timing"
    - "Converted output preserves all content from original Format D cards"
  artifacts:
    - path: "pipeline/instagram/__init__.py"
      provides: "Package export"
      exports: ["convert_format_d_to_carousel", "convert_format_d_to_reel_script", "InstagramCard", "InstagramSlide", "InstagramReelScene"]
    - path: "pipeline/instagram/models.py"
      provides: "Instagram data models"
      contains: "class InstagramSlide"
    - path: "pipeline/instagram/content_converter.py"
      provides: "Format D → Instagram format conversion"
      min_lines: 120
  key_links:
    - from: "content_converter.py"
      to: "models.py"
      via: "imports InstagramSlide, InstagramReelScene"
      pattern: "from.*models import"
---

<objective>
Format D(펀치 브리핑 5-6카드) → Instagram Carousel 슬라이드 데이터 / Reels 씬 스크립트 변환기.

Purpose: 기존 Threads 발행용 Format D 출력을 재가공하여 Instagram 포맷에 맞는 구조화된 데이터로 변환. 카드 매핑 규칙(Hook→Slide 1, Conflict→Slide 2, Twist→Slide 3, Expansion→Slide 4, CTA→Slide 5, Link→Slide 6~7 + Branding)을 구현.

Output: `pipeline/instagram/models.py` (데이터클래스), `pipeline/instagram/content_converter.py` (변환 함수)
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-17/CONTEXT.md
@.planning/phase-17/RESEARCH.md

<reference>
기존 Format D 출력 구조 (pipeline/threads/writer.py — build_system_prompt_D):
- Card 1: Hook (훅 — 큰 숫자/질문 + 한 줄)
- Card 2: Conflict A (문제 제기 — "~아님, ~함")
- Card 3: Twist (반전/핵심 인사이트)
- Card 4: Expansion (데이터/맥락 확장)
- Card 5: CTA/열린질문
- Card 6: Link (출처 링크)

기존 PipelineStep 패턴:
- pipeline/steps/step_run_threads.py 에서 StepRunThreads 클래스 참조
- name: str 필드 + run() -> int 메서드
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Instagram data models</name>
  <files>
    pipeline/instagram/__init__.py
    pipeline/instagram/models.py
  </files>
  <action>
    Create `pipeline/instagram/models.py` with these dataclasses (stdlib only — no pydantic per project convention):

    1. `class SlideType(str, Enum)` — HOOK, CONFLICT, TWIST, EXPANSION, CTA, LINK, BRANDING, COVER

    2. `@dataclass class InstagramSlide`:
       - `slide_type: SlideType` — 어떤 종류의 슬라이드인지
       - `title: str` — 슬라이드 메인 타이틀 (큰 텍스트)
       - `body: str` — 본문 텍스트 (2-3문장)
       - `highlight_number: str | None` — 숫자 강조 (예: "87%", "#1")
       - `emoji_prefix: str` — 이모지 헤더 (예: "💡", "⚠️", "🎯")
       - `style_hint: str` — 시각적 힌트 (예: "gradient-blue", "dark-card", "stats-focus")
       - `subtitle: str | None` — 보조 텍스트 (출처 등)

    3. `@dataclass class InstagramReelScene`:
       - `scene_index: int` — 씬 순서 (0-based)
       - `text: str` — TTS로 읽을 대본 텍스트 (한국어, 15~30자)
       - `slide_ref: SlideType` — 참조 슬라이드 타입
       - `duration_seconds: float` — 예상 지속 시간 (TTS 속도 기반, 초기값은 2~5초)
       - `transition_type: str | None` — xfade 전환 타입 (wipeleft/circleclip/dissolve/smoothleft 중 하나, None=첫 씬)
       - `animation_style: str` — drawtext 애니메이션 (bounce/fade/slide-up 중 하나)
       - `caption_lines: list[str]` — 자막으로 표시될 라인 (한 줄 15~20자로 분할)

    4. `__init__.py` exports: `InstagramSlide`, `InstagramReelScene`, `SlideType`, `convert_format_d_to_carousel`, `convert_format_d_to_reel_script`

    Import from stdlib: `from dataclasses import dataclass`, `from enum import Enum`, `from typing import Optional`.
    Per project convention: use `str | None` not `Optional[str]` (Python 3.14 style is fine with `from __future__ import annotations` or use `Optional`).
    Keep it simple: use `Optional[str]` for Python 3.14 compatibility without future import.
  </action>
  <verify>
    <automated>python3 -m py_compile pipeline/instagram/models.py && python3 -c "from pipeline.instagram.models import InstagramSlide, InstagramReelScene, SlideType; s = InstagramSlide(slide_type=SlideType.HOOK, title='Test', body='Body', highlight_number='99%', emoji_prefix='💡', style_hint='gradient-blue'); print(f'Slide: {s.slide_type.value} / {s.title}')"</automated>
  </verify>
  <done>Dataclasses defined, importable, SlideType enum works with value access</done>
</task>

<task type="auto">
  <name>Task 2: Implement Format D → Carousel conversion</name>
  <files>
    pipeline/instagram/__init__.py
    pipeline/instagram/content_converter.py
  </files>
  <action>
    Create `pipeline/instagram/content_converter.py` with:

    1. `convert_format_d_to_carousel(cards: list[str]) -> list[InstagramSlide]`:
       - 입력: Format D 카드 문자열 리스트 (6개, writer.py 출력 형식)
       - 매핑 규칙 (per CONTEXT.md §3):
         - `cards[0]` (Hook) → Slide 1: HOOK — highlight_number 추출 (정규식: `\d+[%%,$,#,]*\d*`), title=첫 줄, body=나머지
         - `cards[1]` (Conflict A) → Slide 2: CONFLICT — body=전체, emoji_prefix="⚠️"
         - `cards[2]` (Twist) → Slide 3: TWIST — body=전체, emoji_prefix="💡"
         - `cards[3]` (Expansion) → Slide 4: EXPANSION — body=전체, emoji_prefix="📊"
         - `cards[4]` (CTA/질문) → Slide 5: CTA — body=전체, emoji_prefix="🎯"
         - `cards[5]` (Link) → Slide 6: LINK → Slide 7: BRANDING (6장이면 Slide 6=LINK, 5장이면 Slide 5=LINK+BRANDING 합침)
       - 각 슬라이드에 적절한 style_hint 할당:
         - HOOK: "gradient-purple" (보라색 그라데이션 배경)
         - CONFLICT: "gradient-red" (붉은 계열)
         - TWIST: "gradient-blue" (파란 계열)
         - EXPANSION: "dark-card" (어두운 카드 스타일)
         - CTA: "gradient-green" (초록 계열)
         - LINK: "minimal-dark" (미니멀 다크)
         - BRANDING: "brand-footer" (브랜드 로고 + "AI코리아24" 푸터)

    2. `extract_highlight(text: str) -> str | None`:
       - 정규식 `r'(\d+[%%,$,#,x]*\d*|[#]\d+)'`으로 숫자 강조 추출
       - 매칭 실패 시 None 반환

    3. `split_text_for_slide(text: str, max_chars: int = 80) -> tuple[str, str]`:
       - 텍스트를 title(첫 번째 문장/줄)과 body(나머지)로 분할
       - 첫 번째 `\n` 또는 첫 마침표 기준 분할

    Per D-03 (기존 파이프라인 재사용): Format D의 기존 출력 형식을 변경하지 않고, 컨버터가 후처리로 Instagram 형식으로 변환.

    Import pattern: `from pipeline.instagram.models import InstagramSlide, SlideType`
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.content_converter import convert_format_d_to_carousel, extract_highlight, split_text_for_slide
# Test highlight extraction
assert extract_highlight('87% of developers prefer AI') == '87%', f'Got: {extract_highlight(\"87% of developers prefer AI\")}'
# Test text split
title, body = split_text_for_slide('First line.\nSecond line.')
assert title == 'First line.'
# Test full conversion with mock cards
cards = ['Card 1: 87% of developers', 'Card 2: But there is a problem', 'Card 3: The twist is here', 'Card 4: More context data', 'Card 5: What do you think?', 'Card 6: Source link']
slides = convert_format_d_to_carousel(cards)
assert len(slides) >= 5, f'Expected >=5 slides, got {len(slides)}'
assert slides[0].slide_type.value == 'HOOK'
assert slides[0].highlight_number == '87%'
print(f'All tests passed: {len(slides)} slides created')
"</automated>
  </verify>
  <done>convert_format_d_to_carousel returns correctly typed slides with proper mapping from all 6 card types</done>
</task>

<task type="auto">
  <name>Task 3: Implement Format D → Reels script conversion</name>
  <files>pipeline/instagram/content_converter.py</files>
  <action>
    Add to `content_converter.py`:

    1. `convert_format_d_to_reel_script(cards: list[str]) -> list[InstagramReelScene]`:
       - 입력: Format D 카드 문자열 리스트 (6개)
       - 각 씬의 text: 카드 내용에서 핵심 문장 1~2개 (15~30자, TTS 읽기용)
       - duration_seconds 추정: 한국어 기준 초당 4~5음절 → `len(text) / 4.5` 클램프 2.0~5.0초
       - transition_type: 랜덤 선택 (random 모듈, wipeleft/circleclip/dissolve/smoothleft 중)
         - 첫 씬(인덱스 0)은 None (전환 불필요)
       - animation_style: HOOK="slide-up", CONFLICT/TWIST="bounce", EXPANSION="fade", CTA="bounce", LINK="fade"
       - caption_lines: text를 한국어 음절 기준 15~20자로 분할 (단어 분할 금지, 공백 기준)
         - `_split_korean_caption(text: str, max_width: int = 18) -> list[str]`
         - 공백 위치에서 분할, 각 줄 max_width 초과 금지

    2. `_estimate_tts_duration(text: str) -> float`:
       - 한국어: `len(text) / 4.5` (초당 4.5음절)
       - 클램프: 1.5초 ~ 6.0초
       - 영어 포함 시: 영어 단어는 3음절 가산

    3. `_split_korean_caption(text: str, max_width: int = 18) -> list[str]`:
       - 공백 기준 분할, 각 줄 max_width 초과 금지
       - `textwrap.fill` 사용 금지 (stdlib `textwrap`도 금지 — 줄바꿈 위치가 한국어에 부적합)
       - 직접 구현: 단어 단위로 축적, max_width 초과 시 새 줄

    Per D-04 (기존 파이프라인 재사용): Reels 대본은 Carousel과 동일한 Format D 소스 사용. 별도 LLM 호출 불필요.
  </verify>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.content_converter import convert_format_d_to_reel_script, _split_korean_caption
cards = ['87% of devs prefer AI for coding', 'But there is a hidden problem', 'The twist changes everything', 'Data shows 3x improvement', 'What will you build?', 'Source: AIKorea24']
scenes = convert_format_d_to_reel_script(cards)
assert len(scenes) == 6, f'Expected 6 scenes, got {len(scenes)}'
assert scenes[1].duration_seconds >= 1.5
assert len(scenes[0].caption_lines) > 0
# Test Korean caption splitting
lines = _split_korean_caption('한국어 문장을 자동으로 분할하는 테스트입니다', 15)
assert all(len(l) <= 15 for l in lines), f'Line exceeds max_width: {lines}'
print(f'All tests passed: {len(scenes)} scenes, caption lines: {lines}')
"</automated>
  </verify>
  <done>convert_format_d_to_reel_script returns 6 scenes with proper timing estimates and Korean-aware caption splitting</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content_converter.py ↔ writer.py | Format D 카드 문자열이 컨버터 입력. 잘못된 형식의 카드(빈 리스트, 짧은 문자열)는 빈 슬라이드/씬 생성 위험. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | DoS | convert_format_d_to_carousel | mitigate | 입력 카드 리스트 empty/null 체크 → 빈 리스트 반환. 각 카드 최소 5자 미만 시 건너뜀. |
| T-17-01-02 | Tampering | convert_format_d_to_reel_script | mitigate | scene duration 클램프(1.5~6.0초)로 과도한 타이밍 방지. |
</threat_model>

<verification>
1. `python3 -c "from pipeline.instagram import convert_format_d_to_carousel, convert_format_d_to_reel_script, InstagramSlide, InstagramReelScene"` ✅
2. 테스트 변환 시 5~7개 슬라이드, 6개 씬 출력 확인
3. Empty 카드 리스트 → 빈 리스트 반환 (크래시 없음)
</verification>

<success_criteria>
1. convert_format_d_to_carousel()가 6개 카드 입력 → 6~7개 InstagramSlide 반환
2. convert_format_d_to_reel_script()가 6개 카드 입력 → 6개 InstagramReelScene 반환
3. 각 슬라이드에 적절한 SlideType, highlight_number, style_hint 할당됨
4. 각 씬에 적절한 timing, transition, caption_lines 할당됨
5. 빈 입력 → 빈 리스트 (크래시 없음)
</success_criteria>

<output>
Create `.planning/phases/17-instagram-carousel-shorts/17-01-SUMMARY.md` when done
</output>
