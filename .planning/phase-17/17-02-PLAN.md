---
phase: 17-instagram-carousel-shorts
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - pipeline/instagram/__init__.py
  - pipeline/instagram/html_renderer.py
  - pipeline/instagram/templates/carousel_slide.html
  - pipeline/instagram/templates/reel_cover.html
  - pipeline/instagram/utils.py
autonomous: true
requirements: [REQ-01, REQ-02, REQ-06]
must_haves:
  truths:
    - "Carousel slides can be rendered as HTML and captured as 1080×1350 PNG"
    - "Reel cover can be rendered as HTML and captured as 1080×1920 PNG"
    - "Korean text renders correctly in captured images"
    - "Multiple slides can be batch-processed"
  artifacts:
    - path: "pipeline/instagram/html_renderer.py"
      provides: "HTML rendering + Playwright capture orchestration"
      min_lines: 100
    - path: "pipeline/instagram/templates/carousel_slide.html"
      provides: "Carousel slide HTML template"
      contains: "1080×1350"
    - path: "pipeline/instagram/templates/reel_cover.html"
      provides: "Reel cover HTML template"
      contains: "1080×1920"
  key_links:
    - from: "html_renderer.py"
      to: "templates/*.html"
      via: "Jinja2-like string.Template substitution"
      pattern: "Template|template"
    - from: "html_renderer.py"
      to: "Playwright (html_to_png)"
      via: "subprocess.run"
      pattern: "subprocess"
---

<objective>
HTML 템플릿 → Playwright 스크린샷 캡처 파이프라인. Instagram Carousel 슬라이드(1080×1350, 4:5)와 Reels 커버(1080×1920, 9:16)를 HTML/CSS 템플릿으로 렌더링하고 Playwright로 PNG 캡처.

Purpose: 무료($0) 이미지 생성 — PIL/Pillow 대신 HTML+CSS로 디자인 품질 확보, Playwright로 정확한 픽셀 출력. 모든 슬라이드는 동일한 디자인 시스템(다크 테마, Pretendard 폰트, 그라데이션 배경) 공유.

Output: HTML 템플릿 2개(`carousel_slide.html`, `reel_cover.html`), `html_renderer.py` (렌더링 + 캡처 오케스트레이션)
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-17/17-01-PLAN.md

<interfaces>
From pipeline/instagram/models.py:
```python
class SlideType(str, Enum):
    HOOK, CONFLICT, TWIST, EXPANSION, CTA, LINK, BRANDING, COVER

@dataclass
class InstagramSlide:
    slide_type: SlideType
    title: str
    body: str
    highlight_number: str | None
    emoji_prefix: str
    style_hint: str  # "gradient-purple" | "gradient-red" | "gradient-blue" | "dark-card" | "gradient-green" | "minimal-dark" | "brand-footer"
    subtitle: str | None
```
</interfaces>

Playwright CLI 확인: `which playwright` → `/opt/homebrew/bin/playwright`
Playwright Python: `pip install playwright && python3 -m playwright install chromium`

Per 안티-스코프 축소 규칙: "v1", "간단한 버전" 금지. 전체 디자인 시스템을 한 번에 구현.
디자인 시스템: 다크 테마(#0D1117 배경), Pretendard 폰트(한국어), 그라데이션 배경(슬라이드 타입별), 큰 타이포그래피, 이모지 아이콘.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML/CSS templates with design system</name>
  <files>
    pipeline/instagram/templates/carousel_slide.html
    pipeline/instagram/templates/reel_cover.html
  </files>
  <action>
    Create two HTML templates using string.Template syntax (`$variable` substitution — NOT Jinja2, per project stdlib-only convention):

    **`carousel_slide.html`** (1080×1350px, 4:5):
    - Full dark theme: 배경 `#0D1117`, 기본 텍스트 흰색
    - Pretendard 폰트 로드: `<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v2/dist/web/static/pretendard.css" rel="stylesheet">`
    - CSS 변수로 색상 시스템:
      - `--bg-gradient-hook`: `linear-gradient(135deg, #1a0533 0%, #0D1117 100%)` (보라)
      - `--bg-gradient-conflict`: `linear-gradient(135deg, #330a0a 0%, #0D1117 100%)` (빨강)
      - `--bg-gradient-twist`: `linear-gradient(135deg, #0a1a33 0%, #0D1117 100%)` (파랑)
      - `--bg-gradient-expansion`: `#1a1a2e` (어두운 카드)
      - `--bg-gradient-cta`: `linear-gradient(135deg, #0a3314 0%, #0D1117 100%)` (초록)
      - `--bg-gradient-link`: `#0D1117` (미니멀 다크)
      - `--accent-purple`: `#A855F7`, `--accent-red`: `#EF4444`, `--accent-blue`: `#3B82F6`, `--accent-green`: `#22C55E`
    - Template variables: `${emoji_prefix}`, `${highlight_number}`, `${title}`, `${body}`, `${bg_class}`, `${subtitle}`
    - 레이아웃:
      - 상단 15%: emoji_prefix (64px) + highlight_number (120px, bold)
      - 중앙 55%: title (48px, bold, 2줄) + body (28px, light, 4줄)
      - 하단 15%: subtitle (20px, gray-400)
      - 우측 하단: "AI코리아24" 브랜드 워터마크 (16px, opacity 0.3)
    - 모든 텍스트에 `word-break: keep-all` (한국어 줄바꿈)

    **`reel_cover.html`** (1080×1920px, 9:16):
    - 동일한 디자인 시스템, 9:16 비율에 맞게 조정
    - 상단 40%: highlight_number + emoji
    - 중앙 35%: title + body
    - 하단: "AI코리아24 · AI 뉴스" 브랜딩
    - 추가 Template 변수: `${subtitle_line}`

    **중요**: `string.Template` 사용 시 `$variable` 문법. CSS 내부에 `$`가 들어가면 이스케이프 필요. `$$`로 literal `$` 표현.
    템플릿 내에서는 `$emoji_prefix`, `$highlight_number`, `$title`, `$body`, `$bg_class`, `$subtitle` 만 사용하고 CSS 변수는 하드코딩.
  </action>
  <verify>
    <automated>python3 -c "
from string import Template
import os
for f in ['carousel_slide.html', 'reel_cover.html']:
    path = f'pipeline/instagram/templates/{f}'
    with open(path) as fh:
        t = Template(fh.read())
        result = t.safe_substitute(emoji_prefix='💡', highlight_number='87%', title='Test Title', body='Test body text', bg_class='gradient-purple', subtitle='AIKorea24', subtitle_line='AIKorea24')
        assert '💡' in result
        assert '87%' in result
        assert '1080' in result or '1920' in result
    print(f'{f}: OK ({len(result)} chars)')
"</automated>
  </verify>
  <done>Both HTML templates render correctly with Template substitution, Pretendard CDN linked, all style_hint background classes defined</done>
</task>

<task type="auto">
  <name>Task 2: Build HTML renderer with Playwright capture</name>
  <files>
    pipeline/instagram/html_renderer.py
    pipeline/instagram/utils.py
    pipeline/instagram/__init__.py
  </files>
  <action>
    Create `pipeline/instagram/html_renderer.py`:

    1. `render_carousel_slides(slides: list[InstagramSlide], output_dir: str | Path) -> list[Path]`:
       - 각 slide를 HTML로 렌더링 → PNG 캡처
       - 슬라이드별 template 변수 매핑 (style_hint → bg_class CSS 클래스)
       - 필요한 경우 highlight_number가 없으면 title 첫 단어를 강조 숫자로 사용
       - 반환: 생성된 PNG 파일 경로 리스트

    2. `render_reel_cover(slide: InstagramSlide, output_dir: str | Path) -> Path`:
       - 첫 번째 슬라이드(HOOK)를 Reels 커버로 렌더링
       - 1080×1920 템플릿 사용

    3. `capture_html_to_png(html_path: str | Path, output_path: str | Path, width: int, height: int) -> bool`:
       - Playwright CLI 호출: `playwright screenshot --device-scale-factor=2 "$html_path" "$output_path"`
       - 또는 `npx playwright screenshot` — `which playwright`로 경로 찾아 subprocess.run
       - 실제로는: `python3 -m playwright open --browser chromium --viewport-size={width}x{height} {html_path}` 방식으로 스크린샷
       
       **Playwright 스크린샷 실제 구현:**
       `subprocess.run([
           str(PLAYWRIGHT_PATH), 'screenshot',
           '--device-scale-factor=2',
           '--viewport-size', f'{width}x{height}',
           f'file://{html_path}',
           str(output_path)
       ], check=True, timeout=30)`
       
       PLAYWRIGHT_PATH = `shutil.which('playwright')` 또는 `/opt/homebrew/bin/playwright`
       - 실패 시 명확한 에러 메시지 + `pip install playwright && python3 -m playwright install chromium` 안내
       - timeout=30초, 실패 시 재시도 1회

    4. `_render_slide_to_html(slide: InstagramSlide, template_path: str, output_path: str)`:
       - string.Template로 HTML 렌더링
       - `$bg_class` 매핑:
         - "gradient-purple" → "bg-hook", "gradient-red" → "bg-conflict",
         - "gradient-blue" → "bg-twist", "dark-card" → "bg-expansion",
         - "gradient-green" → "bg-cta", "minimal-dark" → "bg-link",
         - "brand-footer" → "bg-brand"
       - HTML 저장 → capture_html_to_png 호출

    Create `pipeline/instagram/utils.py`:
    - `ensure_output_dir(date_str: str | None = None) -> Path`: output 디렉토리 생성 (기본: 오늘 날짜)
      - `instagram-carousel-output/YYYYMMDD/` 또는 `instagram-reel-output/YYYYMMDD/`
    - `get_playwright_path() -> str`: Playwright 바이너리 경로 탐색 (which playwright → /opt/homebrew/bin/playwright)
    - `cleanup_old_html(output_dir: Path)`: 캡처 후 임시 HTML 파일 자동 삭제

    Update `__init__.py` to export `render_carousel_slides`, `render_reel_cover`.

    Per 프로젝트 규칙: `subprocess.run` 사용, 귀찮아도 `capture_output=True` + `text=True` 로그 기록.
    Per CONTEXT.md §4 (비용 $0): Playwright 로컬 설치, API 불필요.
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.html_renderer import render_carousel_slides, render_reel_cover, capture_html_to_png
from pipeline.instagram.models import InstagramSlide, SlideType
from pipeline.instagram.utils import ensure_output_dir
# Check imports work (no runtime test - requires display)
slides = [InstagramSlide(slide_type=SlideType.HOOK, title='Test', body='Body', highlight_number='99%', emoji_prefix='💡', style_hint='gradient-blue', subtitle=None)]
print(f'render_carousel_slides: {render_carousel_slides}')
print(f'render_reel_cover: {render_reel_cover}')
print(f'Slide type HOOK: {slides[0].slide_type.value}')
" 2>&1 | head -5</automated>
  </verify>
  <done>Tasks 1-2 complete: HTML templates renderable, core functions importable, Playwright path detectable</done>
</task>

<task type="auto">
  <name>Task 3: Wire HTML rendering + batch capture pipeline</name>
  <files>
    pipeline/instagram/html_renderer.py
    pipeline/instagram/utils.py
    pipeline/instagram/__init__.py
  </files>
  <action>
    Complete the renderer with batch processing utilities:

    1. Add `batch_render_carousel(slides: list[InstagramSlide], output_dir: str | Path) -> list[Path]` to `html_renderer.py`:
       - Sequential rendering (슬라이드 순서 유지 중요)
       - 각 슬라이드 → _render_slide_to_html → capture_html_to_png
       - 진행률 로그: `[1/6] Rendering HOOK slide...`
       - 실패한 슬라이드는 건너뛰고 로그 기록 (전체 실패 방지)
       - 반환: 성공적으로 생성된 PNG 경로 리스트 (부분 성공 가능)

    2. Add `render_carousel_cover(slides: list[InstagramSlide], output_dir: str | Path) -> Path`:
       - Carousel용 커버 생성 (첫 번째 슬라이드 + "Swipe →" 화살표 오버레이)
       - 1080×1350, carousel_slide.html에 화살표 CSS 추가

    3. Add to `utils.py` — `create_run_directory(base_name: str = "instagram-carousel-output") -> Path`:
       - `project_root() / base_name / YYYYMMDD/` 생성
       - 이미 존재하면 덮어쓰기 (날짜별 1회 실행)
       - 반환: 생성된 Path 객체

    4. Add to `html_renderer.py` — `render_full_carousel(slides: list[InstagramSlide]) -> dict[str, list[Path] | Path]`:
       - 단일 호출로 전체 Carousel 생성
       - 내부: create_run_directory → render_carousel_cover → batch_render_carousel
       - 반환: `{"cover": Path, "slides": [Path, ...], "output_dir": Path}`

    5. Add to `html_renderer.py` — `render_reel_thumbnail(slides: list[InstagramSlide]) -> Path`:
       - Reels용 썸네일 생성 (첫 번째 슬라이드, reel_cover.html 템플릿)
       - 반환: 생성된 PNG 경로

    Per D-03 (무료 운영): Playwright는 로컬 설치. `npm` 대신 `pip install playwright` 사용.
    `PLAYWRIGHT_PATH`는 `shutil.which("playwright")`로 탐색하며, 설치 안내는 stderr로 출력.
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.html_renderer import batch_render_carousel, render_carousel_cover, render_full_carousel
from pipeline.instagram.utils import create_run_directory
import inspect
for fn in [batch_render_carousel, render_carousel_cover, render_full_carousel]:
    sig = inspect.signature(fn)
    print(f'{fn.__name__}{sig}')
from pipeline.instagram import render_carousel_slides, render_reel_cover, batch_render_carousel, render_full_carousel
print('All exports OK')
" 2>&1</automated>
  </verify>
  <done>render_full_carousel() produces cover + N slides in dated output directory; batch processing handles partial failures gracefully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML 템플릿 → Playwright | 템플릿은 string.Template으로 제어된 변수만 치환 — injection 불가 |
| Playwright capture | 로컬 파일 시스템에 PNG 저장, 외부 네트워크 호출 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | DoS | Playwright subprocess | mitigate | timeout=30초, 실패 시 재시도 1회, 최종 실패 시 건너뛰고 로그 |
| T-17-02-02 | Tampering | HTML template | mitigate | string.Template safe_substitute 사용 — 변수 미존재 시에도 HTML 깨지지 않음 |
</threat_model>

<verification>
1. `python3 -c "from pipeline.instagram import render_carousel_slides, render_reel_cover, batch_render_carousel"` ✅
2. 템플릿 파일 존재: `pipeline/instagram/templates/carousel_slide.html`, `pipeline/instagram/templates/reel_cover.html`
3. `create_run_directory()`로 출력 디렉토리 생성 확인
</verification>

<success_criteria>
1. 모든 HTML 템플릿이 string.Template으로 정상 렌더링
2. render_full_carousel()이 커버 + N개 슬라이드 PNG 경로 반환
3. 캡처 실패 시 건너뛰고 로그 기록 (전체 중단 없음)
4. 출력 디렉토리: `instagram-carousel-output/YYYYMMDD/` 형태
5. `utils.py`의 Playwright 경로 탐색이 `which playwright` 결과 반환
</success_criteria>

<output>
Create `.planning/phases/17-instagram-carousel-shorts/17-02-SUMMARY.md` when done
</output>