---
phase: 17-instagram-carousel-shorts
plan: 04
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - pipeline/instagram/tts_generator.py
  - pipeline/instagram/__init__.py
autonomous: true
requirements: [REQ-02, REQ-06]
must_haves:
  truths:
    - "Korean news text can be converted to natural-sounding TTS audio"
    - "TTS timing information can be used to generate accurate SRT subtitles"
    - "Multiple scenes can have individual TTS segments concatenated into one audio file"
    - "Generated subtitles have Korean-aware line breaking (15-20 chars/line)"
  artifacts:
    - path: "pipeline/instagram/tts_generator.py"
      provides: "edge-tts TTS + SRT subtitle generation"
      min_lines: 150
  key_links:
    - from: "tts_generator.py"
      to: "ReelScene text"
      via: "text → TTS → MP3"
      pattern: "edge-tts|generate_tts"
    - from: "tts_generator.py"
      to: "video_renderer.py"
      via: "MP3 + SRT files as video renderer input"
      pattern: "output_dir"
---

<objective>
edge-tts(MS Edge 무료 TTS)를 사용한 한국어 TTS 생성 + SRT 자막 파일 생성.

Purpose: 무료($0)로 자연스러운 한국어 TTS 오디오와 정확한 타임코드 자막 생성. 
각 Reels 씬별 개별 TTS → 타임코드 계산 → SRT → 최종 단일 MP3+SRT 병합.

Output: `pipeline/instagram/tts_generator.py`
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-17/CONTEXT.md
@.planning/phase-17/RESEARCH.md
@.planning/phase-17/17-01-PLAN.md

<interfaces>
From 17-01 models.py:
```python
@dataclass
class InstagramReelScene:
    scene_index: int
    text: str  # TTS로 읽을 대본 텍스트
    duration_seconds: float  # 예상 지속 시간
    transition_type: str | None
    animation_style: str
    caption_lines: list[str]  # 자막 라인 (15-20자 분할)
```

RESEARCH.md §1: edge-tts — 한국어 음성 `ko-KR-SunHiNeural` (자연스러움 최상, 무제한 무료)
설치 확인: `edge-tts==7.2.8` pip 패키지

edge-tts API:
```python
import edge_tts
tts = edge_tts.SubMaker()
communicate = edge_tts.Communicate(text, voice="ko-KR-SunHiNeural")
with open("output.mp3", "wb") as f:
    async for chunk in communicate.stream():
        if chunk["type"] == "audio":
            f.write(chunk["data"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build edge-tts wrapper for Korean TTS generation</name>
  <files>
    pipeline/instagram/tts_generator.py
    pipeline/instagram/__init__.py
  </files>
  <action>
    Create `pipeline/instagram/tts_generator.py`:

    1. `generate_tts(text: str, voice: str = "ko-KR-SunHiNeural", output_path: str | Path | None = None) -> dict`:
       - edge-tts Communicate로 TTS 생성 (async 함수지만 sync 래퍼로 호출)
       - sync 호출: `asyncio.run(_generate_tts_async(text, voice, output_path))`
       - 반환: `{"audio_path": str, "duration": float, "text": str}`
       - output_path가 None이면 tempfile 생성, 자동 cleanup 방지

    2. `_generate_tts_async(text: str, voice: str, output_path: str | Path) -> dict`:
       - `edge_tts.Communicate(text, voice)` 생성
       - `communicate.stream()`으로 청크 수신 → 파일 저장
       - `communicate.submaker`로 워드타이밍 추출 (선택)
       - 완료 후 파일 지속시간 측정: `_get_audio_duration()`
       - 반환: `{"audio_path": str(output_path), "duration": float, "text": text}`

    3. `_get_audio_duration(audio_path: str) -> float`:
       - `ffprobe -v error -show_entries format=duration -of csv=p=0 {audio_path}`
       - 실패 시 `0.0` 반환
       - subprocess.run timeout=10초

    4. `validate_tts(audio_path: str, expected_text_len: int) -> dict`:
       - 파일 존재 확인
       - 지속시간 > 0 확인
       - 파일 크기 < 5MB 확인 (짧은 TTS)
       - 반환: `{"valid": bool, "duration": float, "size_kb": float, "error": str | None}`

    Per CONTEXT.md §6(위험): edge-tts 실패 시 재시도 1회, 그래도 실패면 명확한 에러 메시지.
    Per AGENTS.md: `subprocess.run` 사용, log 기록.
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.tts_generator import generate_tts, _get_audio_duration, validate_tts
import inspect
# Check all functions importable
for fn in [generate_tts, _get_audio_duration, validate_tts]:
    sig = inspect.signature(fn)
    print(f'{fn.__name__}{sig}')
print('All imports OK')
# Test ffprobe duration
import tempfile, os
with tempfile.NamedTemporaryFile(suffix='.mp3', delete=False) as f:
    f.write(b'FAKE_MP3')
    path = f.name()
dur = _get_audio_duration(path)
os.unlink(path)
print(f'ffprobe on fake mp3: {dur} (expected 0.0)')
" 2>&1</automated>
  </verify>
  <done>generate_tts wrapper importable; _get_audio_duration falls back gracefully; validate_tts checks all quality criteria</done>
</task>

<task type="auto">
  <name>Task 2: Build SRT subtitle generation from TTS timing</name>
  <files>pipeline/instagram/tts_generator.py</files>
  <action>
    Add SRT generation to `tts_generator.py`:

    1. `generate_srt(text: str, scene_duration: float, start_time: float = 0.0, caption_lines: list[str] | None = None) -> str`:
       - 텍스트 + 지속시간 → SRT 형식 문자열 반환
       - 단일 SRT entry 생성:
         ```
         1
         00:00:01,000 --> 00:00:04,000
         자막 텍스트 라인 1
         자막 텍스트 라인 2
         ```
       - start_time을 기준으로 SRT 타임코드 계산
       - caption_lines가 제공되면 해당 라인 사용
       - 없으면 `_split_korean_caption(text, 18)`로 분할

    2. `generate_subtitle_pack(scenes: list[dict], output_dir: str | Path) -> dict`:
       - 여러 Reels 씬 → 단일 SRT 파일 생성
       - 입력 scenes: `[{"text": str, "estimated_duration": float, "caption_lines": list[str]}, ...]`
       - 각 씬의 start_time 누적 계산 (이전 씬 duration 합)
       - 단일 SRT 파일에 모든 씬 순차 기록
       - 반환: `{"srt_path": str, "total_duration": float, "scene_timings": list[{"start": float, "end": float}]}`

    3. `_format_srt_timestamp(seconds: float) -> str`:
       - `seconds` → `"00:00:01,500"` 형식
       - 소수점 이하 3자리 → 밀리초 (`,` 구분자)
       - 클램프: 0.000 ~ 99:59:59,999

    4. `_split_korean_caption(text: str, max_width: int = 18) -> list[str]`:
       - 17-01과 동일한 함수 (중복 방지를 위해 공유 유틸로 뺄 수 있으나, 독립성 위해 복제)
       - 공백 기준 분할, 각 줄 max_width 초과 금지
       - `textwrap` 미사용 (직접 구현)

    5. `generate_subtitle_filter_srt(srt_path: str, style: str = "bounce") -> str`:
       - SRT 파일을 FFmpeg subtitles 필터로 변환
       - 반환: `subtitles={srt_path}:force_style='FontName=AppleSDGothicNeo,FontSize=28,Alignment=2'`
       - drawtext 대신 subtitles 필터 사용 시 (단순 자막)
       - NOTE: 17-03 video_renderer의 drawtext 사용이 기본이므로 이 함수는 fallback

    Per AGENTS.md 3중 방어 원칙: SRT 생성 후 `_validate_srt()`로 타임코드 정확성 검증.
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.tts_generator import generate_srt, _format_srt_timestamp, _split_korean_caption, generate_subtitle_pack
# Test SRT generation
srt = generate_srt('테스트 자막입니다', 2.5, 1.0, ['테스트 자막입니다'])
assert '1' in srt
assert '00:00:01,000' in srt
assert '00:00:03,500' in srt  # 1.0 + 2.5 = 3.5
# Test timestamp format
ts = _format_srt_timestamp(1.5)
assert ts == '00:00:01,500', f'Got: {ts}'
ts = _format_srt_timestamp(75.123)
assert ts == '00:01:15,123', f'Got: {ts}'
# Test subtitle pack
scenes = [
    {'text': 'First scene', 'estimated_duration': 2.0, 'caption_lines': ['First scene']},
    {'text': 'Second scene', 'estimated_duration': 3.0, 'caption_lines': ['Second scene']},
]
import tempfile
with tempfile.TemporaryDirectory() as tmpdir:
    result = generate_subtitle_pack(scenes, tmpdir)
    assert result['total_duration'] == 5.0
    assert len(result['scene_timings']) == 2
    assert abs(result['scene_timings'][0]['start'] - 0.0) < 0.01
    assert abs(result['scene_timings'][1]['start'] - 2.0) < 0.01
    with open(result['srt_path']) as f:
        content = f.read()
    assert 'First scene' in content
print(f'SRT: {srt.strip()[-50:]}')
print(f'Timestamp: {ts}')
print(f'Subtitle pack: {result[\"scene_timings\"]}')
" 2>&1</automated>
  </verify>
  <done>generate_srt produces valid SRT with correct timestamps; generate_subtitle_pack cascades scene timings; caption splitting handles Korean text</done>
</task>

<task type="auto">
  <name>Task 3: Build multi-segment TTS concatenation + timing optimization</name>
  <files>pipeline/instagram/tts_generator.py</files>
  <action>
    Add multi-segment processing to `tts_generator.py`:

    1. `generate_reel_audio(scenes: list[dict], output_dir: str | Path, voice: str = "ko-KR-SunHiNeural") -> dict`:
       - 각 씬별 TTS 생성 → concatenate → 단일 MP3
       - 입력 scenes: `[{"text": str, "scene_index": int}, ...]` (text만 있어도 됨)
       - 처리:
         - 각 scene text → `generate_tts()` 호출
         - 모든 TTS MP3를 `_concatenate_audio()`로 병합
         - 생성된 SRT와 함께 반환
       - 반환: `{
           "audio_path": str,        # 최종 병합 MP3 경로
           "srt_path": str,          # SRT 파일 경로
           "total_duration": float,  # 총 지속 시간
           "scene_audios": [{"index": int, "path": str, "duration": float}, ...]
         }`

    2. `_concatenate_audio(audio_paths: list[str], output_path: str) -> str`:
       - `ffmpeg -i "concat:input1.mp3|input2.mp3|..." -acodec copy {output_path}`
       - 또는 concat demuxer 사용: 각 파일 경로를 temp 파일에 기록 후 `-f concat -safe 0`
       - 반환: output_path

    3. `_validate_srt_timing(srt_path: str, audio_path: str, tolerance: float = 0.5) -> bool`:
       - SRT 마지막 타임코드 vs 오디오 길이 차이 < tolerance
       - SRT 내 타임코드 순서 검증 (각 entry start < end)
       - 반환: boolean

    4. `adjust_reel_timing(scenes: list[InstagramReelScene], audio_duration: float) -> list[InstagramReelScene]`:
       - 오디오 실제 길이에 맞춰 씬별 duration 조정
       - 총 오디오 길이를 씬 비율에 따라 분배
       - 최소 1.5초, 최대 6.0초 클램프 (17-01과 동일)

    5. Export `generate_reel_audio` in `__init__.py`

    Per RESEARCH.md §6(비용): edge-tts 무제한 무료 — API 키 불필요.
    Per 성공 기준: SRT 타임코드 정확도 ±0.5초.
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.tts_generator import (
    generate_reel_audio, _concatenate_audio, _validate_srt_timing, adjust_reel_timing
)
from pipeline.instagram.models import InstagramReelScene
import inspect, tempfile
# Check function signatures
for fn in [generate_reel_audio, _concatenate_audio, _validate_srt_timing, adjust_reel_timing]:
    sig = inspect.signature(fn)
    print(f'{fn.__name__}{sig}')
# Test adjust_reel_timing
scenes = [
    InstagramReelScene(scene_index=0, text='Scene 1', slide_ref=None, duration_seconds=2.0, transition_type=None, animation_style='bounce', caption_lines=['Scene 1']),
    InstagramReelScene(scene_index=1, text='Scene 2 longer text here', slide_ref=None, duration_seconds=3.0, transition_type='wipeleft', animation_style='bounce', caption_lines=['Scene 2']),
]
adjusted = adjust_reel_timing(scenes, 4.0)  # actual audio is 4 sec
total = sum(s.duration_seconds for s in adjusted)
assert abs(total - 4.0) < 0.1, f'Total: {total}'
for s in adjusted:
    assert 1.5 <= s.duration_seconds <= 6.0
print(f'Adjusted durations: {[s.duration_seconds for s in adjusted]}, total: {total:.1f}')
# Test concat (with temp empty files)
with tempfile.TemporaryDirectory() as tmpdir:
    import os
    empty = os.path.join(tmpdir, 'empty.mp3')
    with open(empty, 'wb') as f:
        f.write(b'FAKE')
    result = _concatenate_audio([empty, empty], os.path.join(tmpdir, 'out.mp3'))
    assert result is not None
print('All tests passed')
" 2>&1</automated>
  </verify>
  <done>generate_reel_audio returns audio + SRT paths; adjust_reel_timing correctly redistributes duration; SRT timing validates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge-tts → 네트워크 | MS Edge TTS는 네트워크 API 호출 — HTTPS로 암호화 |
| TTS 출력 파일 | MP3 파일 시스템 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | DoS | edge-tts 네트워크 실패 | mitigate | 재시도 1회, 실패 시 명확한 에러 + 안내 메시지 |
| T-17-04-02 | Tampering | SRT 타임코드 | mitigate | _validate_srt_timing()으로 모든 타임코드 순서 검증 |
| T-17-04-03 | DoS | 오디오 시간 과다 | mitigate | scene duration 1.5~6.0초 클램프, 총 30초 제한 |
</threat_model>

<verification>
1. `python3 -c "from pipeline.instagram.tts_generator import generate_tts, generate_reel_audio, generate_srt, validate_tts"` ✅
2. SRT 타임코드 형식 정확성: `_format_srt_timestamp(1.5) == "00:00:01,500"`
3. `adjust_reel_timing()`이 총 길이를 오디오 길이에 맞춰 재분배
4. `_validate_srt_timing()`이 SRT ↔ 오디오 싱크 검증
5. `_concatenate_audio()`가 다중 MP3 병합 (기능 검증)
</verification>

<success_criteria>
1. generate_tts()가 edge-tts로 한국어 TTS MP3 생성 (동기 래퍼)
2. generate_srt()가 올바른 SRT 타임코드 형식 생성
3. generate_reel_audio()가 다중 씬 → 단일 MP3 + SRT 생성
4. adjust_reel_timing()이 실제 오디오 길이로 씬 duration 재조정
5. SRT 타임코드 정확도 ±0.01초 (내부 테스트)
6. 모든 함수가 FFmpeg/ffprobe 의존성 없이도 안전한 fallback 처리
</success_criteria>

<output>
Create `.planning/phases/17-instagram-carousel-shorts/17-04-SUMMARY.md` when done
</output>
