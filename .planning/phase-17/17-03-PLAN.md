---
phase: 17-instagram-carousel-shorts
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02, 17-04]
files_modified:
  - pipeline/instagram/video_renderer.py
  - pipeline/instagram/__init__.py
autonomous: true
requirements: [REQ-01, REQ-02, REQ-06]
must_haves:
  truths:
    - "Static images can be rendered into cinematic video with Ken Burns zoom/pan"
    - "Multiple scenes are connected with smooth transitions (wipe, circlecrop, dissolve)"
    - "Korean subtitles appear with bounce animation synced to audio"
    - "Output is valid MP4 (H.264, AAC, 30fps) at correct resolution"
  artifacts:
    - path: "pipeline/instagram/video_renderer.py"
      provides: "FFmpeg command builder + video rendering"
      min_lines: 200
  key_links:
    - from: "video_renderer.py"
      to: "FFmpeg"
      via: "subprocess.run with filter_complex"
      pattern: "ffmpeg"
    - from: "video_renderer.py"
      to: "instagram-carousel-output/"
      via: "reads PNG files for zoompan input"
      pattern: "input"
---

<objective>
FFmpeg 기반 비디오 렌더러 — Ken Burns(zoompan) + xfade 전환 + drawtext 자막 애니메이션을 단일 FFmpeg 명령어로 합성.

Purpose: 정적 PNG 슬라이드를 시네마틱 비디오(Reels/Shorts)로 변환. $0 비용 유지(FFmpeg 로컬).
Carousel용 MP4(1080×1350, 4:5)와 Reels용 MP4(1080×1920, 9:16) 모두 지원.

Output: `pipeline/instagram/video_renderer.py`
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phase-17/CONTEXT.md
@.planning/phase-17/RESEARCH.md
@.planning/phase-17/17-01-PLAN.md
@.planning/phase-17/17-02-PLAN.md
@.planning/phase-17/17-04-PLAN.md

<reference>
기존 파이프라인 의존성:
- 17-01: content_converter.py — Format D → InstagramSlide[], InstagramReelScene[]
- 17-02: html_renderer.py — slide → PNG (1080×1350 / 1080×1920)
- 17-04: tts_generator.py — TTS audio (.mp3) + SRT subtitles (.srt)

RESEARCH.md §3 FFmpeg 기법:
- Ken Burns: `zoompan=z='min(zoom+0.0015,1.12)':d=75:s=1080x1920:fps=30`
- xfade 전환: `wipeleft`, `circlecrop`, `dissolve`, `smoothleft`
- 자막 바운스: `drawtext=fontsize='56*(0.7+0.3*sin(2*PI*t/0.5))'`
- HW 가속: macOS `-hwaccel videotoolbox`, Linux `-hwaccel nvenc`

FFmpeg 확인: `/opt/homebrew/bin/ffmpeg`, `/opt/homebrew/bin/ffprobe`
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Ken Burns + xfade transition filter logic</name>
  <files>pipeline/instagram/video_renderer.py</files>
  <action>
    Create `pipeline/instagram/video_renderer.py` with Ken Burns and transition modules:

    1. `build_ken_burns_filter(image_path: str, duration_frames: int = 75, output_size: str = "1080x1920", fps: int = 30) -> str`:
       - FFmpeg zoompan 필터 문자열 반환
       - 기본값: `zoompan=z='min(zoom+0.0015,1.12)':x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)':d={d}:s={s}:fps={fps}`
       - zoom 속도: 0.0015/frame (1.0x → 1.12x, 약 2.5초)
       - 다양한 팬 방향 지원 (랜덤):
         - 기본: 중앙 고정 (x='iw/2-(iw/zoom/2)', y='ih/2-(ih/zoom/2)')
         - `zoompan_left`: x='iw/2-(iw/zoom/2)-50' (좌측偏移)
         - `zoompan_right`: x='iw/2-(iw/zoom/2)+50' (우측偏移)
         - `zoompan_up`: y='ih/2-(ih/zoom/2)-50' (상승)
         - `zoompan_down`: y='ih/2-(ih/zoom/2)+50' (하강)
       - parameter: `pan_direction: str = "center"` (center/left/right/up/down)

    2. `build_xfade_filter(transition: str, duration: float = 0.5, offset: float = 2.0) -> str`:
       - `xfade=transition={t}:duration={d}:offset={o}`
       - transition 목록: `["wipeleft", "circlecrop", "dissolve", "smoothleft"]`
       - 지원 검증: `is_supported_xfade(transition: str) -> bool`
       - 지원 안 되면 `dissolve`로 fallback

    3. `select_random_transitions(count: int) -> list[str]`:
       - count개 전환 랜덤 선택 (첫 인덱스=None, 이후 랜덤)
       - `random.choice(SUPPORTED_TRANSITIONS)` 사용

    4. `build_scene_filter_chain(scenes: list[dict], output_size: str = "1080x1920", fps: int = 30) -> str`:
       - 여러 이미지/텍스트를 FFmpeg filter_complex 문자열로 변환
       - 각 scene: `{"image": str, "duration": float, "transition": str | None, "pan_direction": str}`
       - 반환: 완전한 filter_complex 문자열 (concat으로 연결)

    Per RESEARCH.md: Ken Burns 서브픽셀 흔들림 방지를 위해 zoom 속도 0.0015 유지 (너무 빠르면 흔들림).
    각 transition 지속 시간 0.5초 표준 (RESEARCH.md 표준값).
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.video_renderer import build_ken_burns_filter, build_xfade_filter, select_random_transitions, is_supported_xfade
# Test Ken Burns
kb = build_ken_burns_filter('/tmp/test.png', 75, '1080x1920', 30)
assert 'zoompan' in kb
assert '0.0015' in kb
assert '1080x1920' in kb
# Test xfade
xf = build_xfade_filter('wipeleft', 0.5, 2.0)
assert 'xfade' in xf
assert 'wipeleft' in xf
# Test transition support
assert is_supported_xfade('wipeleft') == True
assert is_supported_xfade('glitch') == False
# Test random selection
trans = select_random_transitions(5)
assert len(trans) == 5
for t in trans:
    assert t is None or is_supported_xfade(t)
print(f'Ken Burns: {kb[:80]}...')
print(f'xfade: {xf}')
print(f'Transitions count: {len([t for t in trans if t])}')
" 2>&1</automated>
  </verify>
  <done>build_ken_burns_filter returns valid zoompan filter string; xfade filter works with all 4 supported transitions; unsupported transitions fallback to dissolve</done>
</task>

<task type="auto">
  <name>Task 2: Build drawtext subtitle animation (bounce + timing)</name>
  <files>pipeline/instagram/video_renderer.py</files>
  <action>
    Add subtitle rendering functions to `video_renderer.py`:

    1. `build_drawtext_filter(text: str, fontfile: str, start_time: float, duration: float, style: str = "bounce", video_width: int = 1080, video_height: int = 1920) -> str`:
       - FFmpeg drawtext 필터 문자열 반환
       - `fontfile`: Pretendard 폰트 경로 (로컬 fallback: `/System/Library/Fonts/AppleSDGothicNeo.ttc` on macOS)
       - `text`: 표시할 텍스트 (SRT 라인)
       - `start_time`, `duration`: 표시 시간 (enable=`between(t,st,st+dur)`)
       - `style`:
         - `"bounce"`: `fontsize='56*(0.7+0.3*sin(2*PI*(t-{st})/0.5))':x=(w-text_w)/2:y=h-text_h-120`
         - `"fade"`: `alpha='if(between(t,{st},{st}+0.2),(t-{st})/0.2,if(between(t,{st}+{dur}-0.2,{st}+{dur}),({st}+{dur}-t)/0.2,1))'`
         - `"slide-up"`: `y='h-text_h-120-50*(1-min(1,(t-{st})/0.3))'`
       - `fontcolor=white`, `borderw=3`, `bordercolor=black@0.8` (가독성)
       - x 중앙 정렬: `x=(w-text_w)/2`
       - y 하단: `y=h-text_h-120` (하단에서 120px 위)
       - `enable='between(t,{start_time},{start_time+duration})'`

    2. `build_subtitle_filter_from_srt(srt_path: str, fontfile: str | None = None, video_height: int = 1920, bounce: bool = True) -> str`:
       - SRT 파일을 파싱하여 drawtext 필터 체인 생성
       - `_parse_srt(srt_path) -> list[dict]`: SRT → `{"index": int, "start": float, "end": float, "text": str}`
       - 각 SRT entry → `build_drawtext_filter()` 호출
       - 리턴: `;`로 join된 drawtext 필터 체인
       - SRT 타임코드 형식: `00:00:01,500 → 00:00:04,000` (밀리초 콤마)

    3. `_find_korean_font() -> str`:
       - macOS: `/System/Library/Fonts/AppleSDGothicNeo.ttc` (기본)
       - Pretendard 설치 확인: `~/Library/Fonts/Pretendard-*.otf` glob
       - 폰트 없으면 Apple SD Gothic Neo fallback
       - 반환: 폰트 파일 경로 (없으면 `fontfile=None`, drawtext에서 font 지정 안 함)

    Per RESEARCH.md §3 자막 바운스: `sin(2*PI*t/0.5)` 주기 0.5초로 팝 효과, 최대 130% 오버슈트.
    drawtext enable 조건으로 SRT 타임코드 정확히 매칭 (±0.1초 오차 허용).
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.video_renderer import build_drawtext_filter, build_subtitle_filter_from_srt, _parse_srt
# Test drawtext
dt = build_drawtext_filter('Hello World', '/System/Library/Fonts/AppleSDGothicNeo.ttc', 0.0, 2.0, 'bounce')
assert 'drawtext' in dt
assert 'Hello World' in dt.replace(\"'\",'')
assert 'enable' in dt
# Test SRT parsing
import tempfile, os
srt_content = '''1
00:00:01,000 --> 00:00:04,000
Hello World

2
00:00:05,000 --> 00:00:08,500
Second subtitle line
'''
with tempfile.NamedTemporaryFile(mode='w', suffix='.srt', delete=False) as f:
    f.write(srt_content)
    srt_path = f.name()
entries = _parse_srt(srt_path)
os.unlink(srt_path)
assert len(entries) == 2
assert abs(entries[0]['start'] - 1.0) < 0.01
assert abs(entries[0]['end'] - 4.0) < 0.01
assert entries[1]['text'] == 'Second subtitle line'
print(f'drawtext: {dt[:80]}...')
print(f'SRT entries: {len(entries)}, first: {entries[0][\"text\"]}')
" 2>&1</automated>
  </verify>
  <done>drawtext filter generates valid FFmpeg syntax; SRT parser correctly extracts timing and text; Korean font fallback chain works</done>
</task>

<task type="auto">
  <name>Task 3: Build comprehensive FFmpeg command + full render pipeline</name>
  <files>
    pipeline/instagram/video_renderer.py
    pipeline/instagram/__init__.py
  </files>
  <action>
    Complete `video_renderer.py` with the full FFmpeg command builder:

    1. `build_render_command(
         images: list[str],
         audio: str | None,
         subtitles_srt: str | None,
         output_path: str,
         output_size: str = "1080x1920",
         fps: int = 30,
         slide_duration: float = 2.5,
         transition_duration: float = 0.5,
         hwaccel: bool = True
       ) -> list[str]`:
       - 전체 FFmpeg 명령어 리스트 반환 (subprocess.run용)
       - 인자 순서:
         1. `ffmpeg` (shutil.which or hardcoded path)
         2. `-hwaccel videotoolbox` (macOS, hwaccel=True)
         3. `-i` 각 이미지 (image inputs)
         4. `-i` audio (있을 경우)
         5. `-filter_complex`:
            - 각 이미지에 Ken Burns (zoompan)
            - 이미지 간 xfade 전환
            - drawtext 자막 오버레이
            - concat으로 연결
         6. `-map [v]` 비디오 출력
         7. `-map [a]` 오디오 출력 (있을 경우)
         8. `-c:v libx264 -preset medium -crf 20`
         9. `-c:a aac -b:a 128k` (오디오 있을 경우)
         10. `-pix_fmt yuv420p -r 30`
         11. `-t` 총 길이 (계산)
         12. output_path
       - FFmpeg 필터그래프를 단일 filter_complex로 구성
       - 필터그래프가 너무 길면 filter 파일로 저장: `_write_filter_file(filter_str: str) -> str`
         - 임시 `.txt` 파일 파이프: `-filter_complex_script {filter_path}`

    2. `render_carousel_video(slides: list[Path], output_dir: str | Path, scenes: list[InstagramReelScene] | None = None) -> Path`:
       - Carousel(1080×1350) 비디오 생성
       - slides: PNG 경로 리스트 (17-02 출력)
       - scenes: ReelScene 리스트 (씬별 타이밍/전환 정보, 없으면 기본값 사용)
       - 출력: `{output_dir}/carousel_{date}.mp4`
       - 각 슬라이드 2.5초 표시, 전환 0.5초 (Carousel은 4:5, 타임라인 느리게)

    3. `render_reel_video(slides: list[Path], audio: str, subtitles_srt: str, output_dir: str | Path, scenes: list[InstagramReelScene]) -> Path`:
       - Reels(1080×1920) 비디오 생성
       - slides: PNG 경로 리스트
       - audio: TTS MP3 경로 (17-04 출력)
       - subtitles_srt: SRT 경로 (17-04 출력)
       - scenes: 씬별 타이밍/전환 정보
       - 출력: `{output_dir}/reel_{date}.mp4`
       - 비디오 길이 = TTS 길이에 맞춤 (오디오 기반)

    4. `get_video_duration(video_path: str) -> float`:
       - `ffprobe -v error -show_entries format=duration -of csv=p=0 {path}`
       - 반환: 초 단위 길이, 실패 시 0.0

    5. `get_audio_duration(audio_path: str) -> float`:
       - 동일한 ffprobe 방식

    6. `validate_rendered_video(video_path: str, expected_width: int, expected_height: int, max_duration: float = 35.0) -> dict`:
       - ffprobe로 해상도/길이/코덱 검증
       - 반환: `{"valid": bool, "codec": str, "resolution": str, "duration": float, "size_mb": float}`

    Per CONTEXT.md §4(성공 기준): CRF 20~23, 10초 미만 Reels, H.264 + AAC.
    Per RESEARCH.md §6(업그레이드 경로): 1단계 FFmpeg Ken Burns 유지.
    slop 체크: FFmpeg 필터그래프 문자열을 직접 생성할 때 `;` 누락으로 필터 체인 끊김 방지 — 각 drawtext 사이에 `;` 구분자 항상 추가.
  </action>
  <verify>
    <automated>python3 -c "
from pipeline.instagram.video_renderer import (
    build_render_command, render_carousel_video, render_reel_video,
    validate_rendered_video, _write_filter_file
)
import inspect, tempfile
# Test command builder
cmd = build_render_command(['/tmp/img1.png', '/tmp/img2.png'], '/tmp/audio.mp3', '/tmp/sub.srt', '/tmp/out.mp4', hwaccel=True)
assert 'ffmpeg' in cmd[0] or 'ffmpeg' in str(cmd)
assert '-hwaccel' in cmd
assert len(cmd) > 10
# Test filter file writer
with tempfile.NamedTemporaryFile(suffix='.txt', delete=False) as f:
    path = _write_filter_file('test filter graph')
    assert path.endswith('.txt')
    import os
    os.unlink(path)
# Test validate function signature
sig = inspect.signature(validate_rendered_video)
print(f'Command: {\" \".join(cmd[:6])}...')
print(f'Arguments: {len(cmd)} tokens')
print(f'validate_rendered_video{sig}')
" 2>&1</automated>
  </verify>
  <done>build_render_command produces valid FFmpeg invocation; render_carousel_video and render_reel_video produce matching output paths; filter file writer works</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 파일 시스템 → FFmpeg | PNG/MP3/SRT 파일을 FFmpeg 입력으로 사용. 존재하지 않는 파일 경로는 FFmpeg 에러 유발. |
| FFmpeg 출력 | MP4 파일을 로컬 파일 시스템에 씀. 디스크 용량 초과 주의. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | DoS | FFmpeg subprocess | mitigate | timeout=120초, 실패 시 stderr 로그 + 재시도 없이 실패 반환 |
| T-17-03-02 | DoS | filter_complex 길이 | mitigate | 필터그래프가 너무 긴 경우 `_write_filter_file`로 파일 분리 (셸 인자 길이 제한 회피) |
| T-17-03-03 | Tampering | FFmpeg stderr | mitigate | stderr 로그 기록, 민감 경로는 log scrubbing 위임 |
</threat_model>

<verification>
1. `python3 -c "from pipeline.instagram.video_renderer import build_render_command, render_carousel_video, render_reel_video, validate_rendered_video"` ✅
2. `build_render_command()`가 유효한 FFmpeg 명령어 리스트 생성 (2개 이미지 + 오디오 + 자막)
3. filter_complex에 zoompan, xfade, drawtext 모두 포함 확인
4. `_parse_srt()`가 표준 SRT 파싱 (타임코드 정확도 ±0.01초)
5. `validate_rendered_video()`가 ffprobe로 해상도/길이/코덱 검증
</verification>

<success_criteria>
1. build_render_command()가 ffmpeg + 모든 입력 + filter_complex 포함 명령어 생성
2. Ken Burns zoompan이 정상 filter 문자열 생성 (1.0x→1.12x, 75프레임)
3. xfade 전환 4종(wipeleft, circlecrop, dissolve, smoothleft) 모두 지원
4. drawtext 바운스 애니메이션이 `sin(2*PI*t/0.5)` 수식으로 생성
5. SRT 파서가 타임코드 정확도 ±0.01초로 파싱
6. macOS 폰트 fallback: Apple SD Gothic Neo 우선 탐색
7. HW 가속 플래그가 macOS에서 videotoolbox 분기
</success_criteria>

<output>
Create `.planning/phases/17-instagram-carousel-shorts/17-03-SUMMARY.md` when done
</output>