---
phase: quick-260924-4rx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/auto_email_sender.py
autonomous: true
requirements:
  - NEWSLETTER-SEND-01
user_setup: []
estimate:
  tokens: 25000
  raw_tokens: 15000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - Brevo list#2 구독자 목록을 API로 동적으로 조회해 온다
    - listIds 파라미터 제거, to 필드에 각 구독자별로 개별 발송
    - sample/test/example 이메일 자동 필터링
    - SUBSCRIBER_EMAIL 폴백 유지 (Brevo 실패 시)
  artifacts:
    - scripts/auto_email_sender.py (get_subscribers_from_brevo + send_email_via_brevo 수정)
  key_links:
    - get_subscribers_from_brevo -> send_email_via_brevo -> Brevo /v3/smtp/email POST
---

objective:
  auto_email_sender.py의 send_email_via_brevo 함수를 수정하여 Brevo list#2 구독자를 동적으로 조회하고, listIds를 제거하고 to 필드로 전원 개별 발송한다. sample/test 이메일은 자동 필터링하고 SUBSCRIBER_EMAIL 폴백을 유지한다.

purpose: 뉴스레터 발송이 정상적으로 동작하도록 하여 구독자 전체에게 브리핑을 전달한다.

output: scripts/auto_email_sender.py (수정)

<execution_context>
@/Users/twinssn/Projects/aikorea24/.planning/STATE.md
@/Users/twinssn/Projects/aikorea24/AGENTS.md
@/Users/twinssn/Projects/aikorea24/.planning/quick/260924-4rx-auto-email-sender-py-send-email-via-brev/260924-4rx-CONTEXT.md
</execution_context>

<context>
@/Users/twinssn/Projects/aikorea24/scripts/auto_email_sender.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: get_subscribers_from_brevo 구현 + send_email_via_brevo 수정</name>
  <files>scripts/auto_email_sender.py</files>
  <action>
get_subscribers_from_brevo(list_id=2) 신규 함수 추가 — Brevo GET /v3/contacts?listIds=2&limit=500 으로 구독자 목록 조회, JSON 응답에서 contacts 배열의 email 주소만 추출.

send_email_via_brevo 수정:
1. listIds 파라미터 제거 (기존 payload["listIds"] = list_ids 블록 제거)
2. get_subscribers_from_brevo()로 구독자 목록 조회
3. sample@ / test- / verify-test@ / @example. 패턴 필터링 (CONTEXT.md agent discretion)
4. 필터링 후 빈 목록이면 SUBSCRIBER_EMAIL 폴백 사용
5. Brevo 실패 시 SUBSCRIBER_EMAIL 폴백, 둘 다 빈 경우 False 반환 + 로그
6. to 필드에 각 구독자별로 개별 발송 (payload["to"] = [{"email": e} for e in emails])
7. sender 정보는 기존과 동일: {name: "AI코리아24", email: "info@aikorea24.kr"}
8. 기존 HTML 생성 로직은完全히 변경하지 것 (generate_email_html 그대로)

get_subscribers_from_brevo 구체:
- load_env() 후 BREVO_API_KEY 확인, 없으면 []
- requests.get(url, headers={"api-key": api_key}, params={"listIds": 2, "limit": 500})
- resp.status_code != 200 → [] (예외는 catch하여 [])
- resp.json().get("contacts", []) 에서 c.get("email")만 추출, None/빈값 제거
- list#2는 8 contacts로 500 limit 초과 없음 (CONTEXT.md agent discretion)
</action>
<verify>
python3 -c "
import sys; sys.path.insert(0, 'scripts')
import auto_email_sender as a
# get_subscribers_from_brevo exists
assert callable(a.get_subscribers_from_brevo), 'get_subscribers_from_brevo not found'
# listIds removed from send_email_via_brevo source
src = open('scripts/auto_email_sender.py').read()
assert 'listIds' not in src.split('get_subscribers_from_brevo')[1].split('def send_email_via_brevo')[0] if 'get_subscribers_from_brevo' in src.split('def send_email_via_brevo')[0] else True, 'listIds in get_subscribers'
# Check listIds removed from send payload (not from Brevo contacts query)
send_part = src.split('def send_email_via_brevo')[1] if 'def send_email_via_brevo' in src else ''
assert '"listIds"' not in send_part and "'listIds'" not in send_part, 'listIds still in send payload'
# test email filtering present
assert 'sample@' in src and 'test-' in src and '@example.' in src, 'test filter patterns missing'
# SUBSCRIBER_EMAIL fallback present
assert 'SUBSCRIBER_EMAIL' in src, 'SUBSCRIBER_EMAIL fallback missing'
# sender unchanged
assert 'info@aikorea24.kr' in src, 'sender email changed'
print('All checks passed')
"
</verify>
<done>get_subscribers_from_brevo가 구현되고, send_email_via_brevo에서 listIds가 제거되며, to 필드에 구독자 목록이 개별 발송용으로 설정됨. sample/test 이메일 필터링과 SUBSCRIBER_EMAIL 폴백이 유지됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brevo API → auto_email_sender | BREVO_API_KEY 환경변수 인증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4rx-01 | Tampering | BREVO_API_KEY 미설정 시 발송 실패 | low | accept | load_env() 후 BREVO_API_KEY 확인, 없으면 False 반환 + 로그 출력 |
| T-4rx-02 | Information Disclosure | 이메일 주소 로그 출력 | low | accept | 구독자 이메일은 로그에 출력하지 않음 (목록 수만 출력) |
</threat_model>

<verification>
python3 -c "
import sys; sys.path.insert(0, 'scripts')
import auto_email_sender as a
assert callable(a.get_subscribers_from_brevo)
src = open('scripts/auto_email_sender.py').read()
assert 'listIds' not in src
assert 'sample@' in src and 'test-' in src and '@example.' in src
assert 'SUBSCRIBER_EMAIL' in src
assert 'info@aikorea24.kr' in src
print('VERIFICATION PASSED')
"
</verification>

<success_criteria>
- get_subscribers_from_brevo()가 Brevo GET /v3/contacts API를 호출하여 list#2 구독자 이메일 목록을 반환
- send_email_via_brevo()에서 listIds 파라미터가 제거되고 to 필드에 각 구독자별로 개별 발송 설정
- sample@, test-, verify-test@, @example. 패턴 이메일이 자동 필터링
- Brevo 조회 실패 시 SUBSCRIBER_EMAIL로 폴백, 둘 다 빈 경우 False 반환
- sender 정보는 기존과 동일하게 유지
</success_criteria>

<output>
Create .planning/quick/260924-4rx-auto-email-sender-py-send-email-via-brev/260924-4rx-SUMMARY.md when done
</output>