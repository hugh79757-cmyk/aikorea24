---
phase: quick-cafe-copy-line
plan: 260924-0lz
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ToolForm.astro]
autonomous: true
requirements: [cafe-copy-line]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "도구 등록 성공 후 카페 공유 문구 복사에 마지막 줄이 베어 카페 URL만 들어간다"
  artifacts:
    - "src/components/ToolForm.astro shareText 마지막 줄 변경"
  key_links:
    - "cafe-copy-btn dataset.shareText -> clipboard.writeText"
---

<objective>
카페 공유 문구 복사 마지막 줄을 카페 URL만으로 정리 (SEO 문구 제거).

Purpose: 사용자 요청 — 공유 문구에 붙는 `에서 더 많은 AI 활용법을 확인하세요` 꼬리 문구 제거, URL만 남김.
Output: ToolForm.astro 1줄 수정.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/ToolForm.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: shareText 마지막 줄 URL-only로 변경</name>
  <files>src/components/ToolForm.astro</files>
  <action>src/components/ToolForm.astro 565-566줄 dataset.shareText 템플릿에서 마지막 줄 꼬리 문구 ` 에서 더 많은 AI 활용법을 확인하세요` 제거. 변경 후: `[${toolName}] ${toolUrl}\n${toolDesc}\n${CAFE_URL}`. CAFE_URL 상수(267줄, https://cafe.naver.com/aikorea24)는 확인됨 — 절대 변경 금지. 복사 핸들러(493-502줄), 제출 흐름, R2/my/PUT-DELETE 영역 손대지 말 것.</action>
  <verify>
    <automated>grep -n "shareText" src/components/ToolForm.astro && grep -c "에서 더 많은 AI 활용법을 확인하세요" src/components/ToolForm.astro | grep -q "^0$" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>shareText 템플릿이 URL-only 3줄 형식이며, 잔여 SEO 문구 0건, 빌드 통과</done>
</task>

</tasks>

<verification>
grep 잔여 문구 0건 + npm run build 통과
</verification>

<success_criteria>
- 복사 문구 마지막 줄 = 베어 카페 URL만
- CAFE_URL 변경 없음
- 빌드 통과
</success_criteria>

<output>
Create `.planning/quick/260924-0lz-cafe-copy-line/260924-0lz-SUMMARY.md` when done
</output>
