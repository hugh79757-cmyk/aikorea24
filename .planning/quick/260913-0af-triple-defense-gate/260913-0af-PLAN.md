---
phase: quick-260913-0af
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/auto_briefing.py
  - tests/test_briefing_gates.py
autonomous: true
requirements: [QUICK-260913-0AF]
estimate:
  tokens: 35000
  raw_tokens: 30000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - 영어/메타/프롬프트 미러링이 섞인 코멘트는 3개 게이트(생성 직후, 저장 직전, 저장 후) 중 최소 1곳에서 차단 또는 플래그된다
    - 기존 함수 시그니처 변경 0건 — 새 함수 추가 + 기존 함수 내부 호출 추가만 (additive)
    - 기존 전체 테스트 스위트 기준선 green 유지
  artifacts:
    - scripts/auto_briefing.py (신규 함수 2개: validate_briefing_comment, verify_briefing_items + 게이트 연결)
    - tests/test_briefing_gates.py (3개 게이트 각각 테스트)
  key_links:
    - generate_comment() → validate_briefing_comment() → 실패 시 재생성(최대 2회) → comment=""
    - save_briefing() → INSERT 직전 detect_prompt_leak() → 릭 시 해당 아이템 저장 스킵
    - main() → save_briefing 성공 후 verify_briefing_items() D1 read-back 배치 검증
---

<objective>
브리핑 파이프라인 3중 방어 게이트 구현 (FINDINGS.md §5 권고 실행) + git-history 회귀 원인 확정.

Purpose: 260912-vyc 진단 판정 — 브리핑 체인에 언어/릭 게이트 전무(구조적 취약). 영어 경향 tier가 응답하면 그대로 D1 적재·라이브 렌더링됨. 3중 방어 원칙(AGENTS.md)에 따라 생성/저장/저장 후 3개 지점에 독립 게이트 배치.

Output: auto_briefing.py 3개 게이트 + 테스트 파일 + 기존 기준선 회귀 0 확인.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260912-vyc-briefing-pipeline-prompt-leak-rCA/FINDINGS.md
@scripts/auto_briefing.py
@pipeline/threads/pitch.py (L28-108: detect_prompt_leak, validate_korean_output)
</context>

## Git-History 회귀 분석 — 플래닝 단계에서 확정된 결론 (Task 1에서 재검증만)

**질문: 과거엔 브리핑 릭이 발생 안 했는데 무엇이 바뀌었나?**

**[검증됨] 답: 게이트가 제거된 것이 아니라, 안전했던 원인(유료 DeepSeek 1순위)이 제거되었다.**

| 시점 | 커밋 | 변경 | 영향 |
|---|---|---|---|
| 2026-07-12 | `40838ab0` Phase 26 | remove_chinese() + 한국어 system prompt 추가 | 중국어만 차단 — 영어는 계속 무방비 |
| 2026-08-12 | `4f448784` | **17-tier 무료 폴백 체인 도입** — 유료 DeepSeek(한국어 안정) 최우선 → 무료 회전 체인 | gpt-oss-20b 등 영어 경향 tier가 브리핑 코멘트 생성에 투입 시작 |
| 2026-08-13 | `5b573491` | 무료 체인 전환 확정 | 동일 |
| 2026-09-10 | `828e4e16` | 순수 회전 + codestral-latest(코드 특화·영어 톤) 등 4종 추가 | 영어 경향 tier 비중 상승 |
| 2026-09-12 | working tree 미커밋 + triage 20260912 | nex-pro/mini, nemotron:free, ling-vl 4종 추가 | 영어 경향 "높음" tier 6~7개 누적 — 트리거 확률 최대 |

- `git show 4f448784^:scripts/auto_briefing.py` → lang gate 0건 — **브리핑 경로에 언어 게이트는 역사상 한 번도 없었다** (FINDINGS 후보 A 재확정).
- auto_briefing.py 커밋 이력 전수(`git log --follow` 7개): 언어 검증 추가/삭제 커밋 없음.
- 유료 DeepSeek 시대엔 모델이 한국어를 안정적으로 출력 → 게이트 없이도 릭 발생 안 함. 무료 체인 전환(8/12) + 확장(9/10, 9/12)이 "과거엔 없던" 릭의 회귀 시점.

<tasks>

<task type="auto">
  <name>Task 1: 기준선 확인 + git 회귀 결론 재검증</name>
  <files>(수정 없음 — 읽기/실행만)</files>
  <action>
  수정 전 기준선 확보. 두 가지만 실행:
  1. 기존 관련 테스트 기준선 실행: `python3 -m pytest tests/test_pitch.py tests/test_briefing_enricher.py tests/test_briefing_scorer.py -x` — 통과 결과 기록. (참고: STATE.md 기준선 — retention_from_env, hook STAR 2건 pre-existing fail 허용)
  2. 위 git-history 결론 표를 아래 명령으로 재확인 (executor가 플래너 결론을 신뢰하지 않고 독립 검증):
     - `git show 4f448784^:scripts/auto_briefing.py | grep -in 'validate\|korean\|gate\|leak'` → 매칭 0건 확인 (게이트 부재 재현)
     - `git log --format='%h %ad %s' --date=short --follow -- scripts/auto_briefing.py` → 언어 검증 관련 커밋 없음 확인
  결과를 구조화된 형태로 SUMMARY에 기록 (3분법 태그 + 근거 명시, AGENTS.md 섹션2 준수).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_pitch.py tests/test_briefing_enricher.py tests/test_briefing_scorer.py -x 2>&1 | tail -3 && git show 4f448784^:scripts/auto_briefing.py | grep -ic 'validate\|korean\|gate\|leak' ; echo "(0이어야 함)"</automated>
  </verify>
  <done>기준선 테스트 통과 수치 기록됨. git 재검증 명령 2종 실행 결과가 플래너 결론 표(게이트 부재 + 체인 전환 시점)와 일치함이 SUMMARY에 근거와 함께 기록됨.</done>
</task>

<task type="auto">
  <name>Task 2: 3중 방어 게이트 구현 — auto_briefing.py (additive)</name>
  <files>scripts/auto_briefing.py</files>
  <action>
  FINDINGS.md §5 권고 표 그대로 구현. 기존 함수 시그니처 변경 금지 — 새 함수 추가 + 기존 함수 내부에 검증 호출 삽입만.

  **공통**: `pipeline/threads/pitch.py`에서 `detect_prompt_leak`, `validate_korean_output`을 lazy import (함수 내부 import — pitch 모듈 import 시 side-effect 최소화, pitch.py 자체도 L93에서 lazy import 패턴 사용).

  **새 함수 1: `validate_briefing_comment(comment)` → (bool, str)** — 1차 방어용 순수 검증기.
  - 내부: `detect_prompt_leak(comment)` 실패 시 (False, 사유). 통과 시 `validate_korean_output(comment[:100], comment[100:300])` 호출해 결과 그대로 반환 (FINDINGS §5 분할 전달 방식).
  - 주의: 코멘트는 1~2문장으로 짧다. validate_korean_output의 한글 비율 15% 임계가 정상 한국어 코멘트를 오탐하는지 Task 3 테스트로 검증. 오탐 시 이 새 함수 내부에서만 보정 (pitch.py 원본 수정 금지 — additive 규칙).

  **1차 게이트 (생성 직후)**: `generate_comment()` 내부 — `chat_completion` 반환 후 `remove_chinese()`와 병행하여 `validate_briefing_comment()` 통과 시에만 반환. 실패 시 재생성 루프 최대 2회 추가 시도 (총 3회). 전부 실패 시 기존 실패 경로 그대로 (None 반환 → main에서 comment=""). 시도 실패마다 로그 (기존 log() 사용, 사유 포함).

  **2차 게이트 (저장 직전)**: `save_briefing()` items 루프 내부 — `sql_item` INSERT 직전 `detect_prompt_leak(comment)` 1회. 릭 감지 시 해당 아이템 INSERT 스킵 + `log()` 로그. (언어 비율은 1차에서 이미 검증 — 2차는 릭 패턴만. FINDINGS 표와 동일.)

  **새 함수 2: `verify_briefing_items(briefing_id)` → int** — 3차 방어 (저장 후 read-back 배치 검증).
  - `d1_query`로 해당 briefing_id의 전 comment 조회 → 각각 `detect_prompt_leak` + `validate_briefing_comment` 검사.
  - 릭 감지 시: 로그 + 감지된 row id 목록 로그. 이후 감지 row에 한해 `d1_execute("UPDATE briefing_items SET comment='' WHERE id=<id>")` — 라이브 렌더링 차단 (FINDINGS "문제 row 제외 처리"). UPDATE 전 감지 수 로그로 기록 (destructive 사전 카운트).
  - 반환값: 감지/처리 수. main()에서 `record_briefing` 호출 뒤 `verify_briefing_items(briefing_id)` 호출 추가 — try/except로 감싸 저장 흐름에 영향 0 (Phase 38 performance_log 패턴과 동일한 append-only 방식).
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'scripts'); sys.path.insert(0,'.'); import auto_briefing; assert callable(auto_briefing.validate_briefing_comment); assert callable(auto_briefing.verify_briefing_items); print('import OK')" && python3 -m pytest tests/test_briefing_gates.py -x</automated>
  </verify>
  <done>auto_briefing.py에 2개 신규 함수 + 3개 게이트 지점 연결 완료. 기존 함수 시그니처(generate_comment, save_briefing, build_briefing, main의 반환값/인자) 변화 없음. import 성공. 새 게이트 테스트 전부 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 3중 게이트 테스트 — 각 층위별 검증</name>
  <files>tests/test_briefing_gates.py</files>
  <behavior>
  - validate_briefing_comment 순수 함수: 순수 한국어 코멘트 → 통과. "Sure, here is a comment about AI" 영어 코멘트 → 실패. "제목: ... 코멘트:" 템플릿 미러링 → 실패. "오픈AI가 GPT-5를 공개했다" (제품명 영어 포함 한국어) → 통과 (오탐 방지 핵심 케이스).
  - 1차: mock chat_completion이 영어 2회 → 한국어 순 반환 시 최종 한국어 코멘트 반환. 영어만 3회 반환 시 None.
  - 2차: save_briefing에 릭 코멘트 아이템 포함 시 해당 INSERT 스킵 (mock d1_execute 호출 검사).
  - 3차: verify_briefing_items에 릭 row 1건 포함 D1 조회 (mock d1_query) → 감지 1, UPDATE 호출 1.
  </behavior>
  <action>
  tests/test_briefing_gates.py 신규 작성. conftest.py의 sys.path 설정(scripts/ 포함)과 monkeypatch 패턴 활용 — auto_briefing import 시 model_router까지 로드되므로, model_router 관련 무거운 의존은 conftest의 기존 mock fixture 방식(monkeypatch_openai 참조)으로 처리하거나 이미 로드 가능하면 그대로 사용. d1_execute/d1_query는 unittest.mock.patch로 auto_briefing 모듈에 바인딩된 이름을 교체. 위 behavior 4그룹 (순수 검증기 / 1차 / 2차 / 3차) 각각 최소 1테스트 — 3중 방어 원칙 "검증은 각 층위별로 테스트" 준수. 테스트는 production code 수정 없이 통과해야 함.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_briefing_gates.py -v 2>&1 | tail -5 && python3 -m pytest tests/test_pitch.py tests/test_briefing_enricher.py tests/test_briefing_scorer.py tests/test_write_thread_validation.py -x 2>&1 | tail -3</automated>
  </verify>
  <done>새 테스트 파일 통과 (순수 검증기 오탐 방지 케이스 포함). Task 1 기준선과 동일 스위트 재실행 결과 동일 통과 수치 — 회귀 0. 테스트 코드만 수정한 production 코드 수정 없이 통과.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_briefing_gates.py -v` — 3개 게이트 각각 커버
- 기준선 스위트 재실행 — Task 1 수치와 동일 (회귀 0)
- `grep -n 'def generate_comment\|def save_briefing\|def build_briefing\|def main' scripts/auto_briefing.py` — 시그니처 변경 없음 육안 확인
</verification>

<success_criteria>
- 3개 게이트가 서로 다른 3개 지점(생성/저장/저장 후)에서 독립 동작 — 어느 하나 우회해도 다음 게이트가 잡음
- 영어 코멘트가 D1에 적재되는 경로: 1차 재생성 → 2차 INSERT 스킵 → 3차 read-back 플래그 중 최소 1곳에서 반드시 차단됨을 테스트로 입증
- additive 규칙 준수: 기존 시그니처/동작 보존, pitch.py 원본 무수정
- git 회귀 결론(무료 체인 전환 = 회귀 시점)이 재검증 명령으로 재현됨
</success_criteria>

<output>
Create `.planning/quick/260913-0af-triple-defense-gate/260913-0af-SUMMARY.md` when done
</output>
