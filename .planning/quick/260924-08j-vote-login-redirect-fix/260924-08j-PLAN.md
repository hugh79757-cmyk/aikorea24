---
phase: quick-08j-vote-login-redirect-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/tools/[id].astro
  - src/pages/api/auth/me.ts
autonomous: true
requirements:
  - VOTE-01
estimate:
  tokens: 15000
  raw_tokens: 10000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - 로그인 사용자가 추천 버튼 클릭 시 투표가 등록/취소되고 카운트가 갱신된다 (로그인 페이지로 이동하지 않는다)
    - 비로그인 사용자가 추천 클릭 시 로그인 페이지로 이동한다
    - 로그인 사용자에게 리뷰 쓰기 버튼이 표시된다
  artifacts:
    - src/pages/tools/[id].astro (수정)
    - src/pages/api/auth/me.ts (신규)
  key_links:
    - vote-btn click handler → POST /api/tools/vote → 401 분기 → /api/auth/login
    - checkAuth → GET /api/auth/me → review-write-btn 표시
---

<objective>
툴 상세 페이지 추천 버튼이 로그인 사용자를 로그인 페이지로 튕기는 버그 수정.

Purpose: 세션 쿠키가 httpOnly라 document.cookie에 보이지 않는데, vote 핸들러가
document.cookie.includes('session=')로 로그인 여부를 판단 → 항상 false →
항상 /api/auth/login으로 리다이렉트. submit 폼이 정상 동작하는 이유도 동일:
ToolForm.astro는 쿠키 사전 체크 없이 바로 POST하고 서버 401만 처리함.
Output: 수정된 [id].astro + 신규 /api/auth/me + 빌드 통과.
</objective>

<execution_context>
@/Users/twinssn/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/twinssn/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/tools/[id].astro
@src/pages/api/tools/vote.ts
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: vote 핸들러 쿠키 스니핑 제거 + /api/auth/me 신규</name>
  <files>src/pages/tools/[id].astro, src/pages/api/auth/me.ts</files>
  <action>근본원인: 카카오/구글 콜백이 session 쿠키를 httpOnly:true로 설정하므로
  클라이언트 document.cookie에는 session이 절대 보이지 않음. [id].astro 456행의
  사전 체크가 항상 false → 로그인 사용자도 /api/auth/login으로 이동.
  수정 (a): vote-btn click 핸들러에서 document.cookie 사전 체크 삭제.
  무조건 POST /api/tools/vote 수행 후, res.status===401일 때만
  window.location.href='/api/auth/login'으로 이동. 그 외 비정상 응답은
  console.error + 버튼 상태 변경 없음 (조용한 실패 금지: 사용자에게
  '추천 처리 중 오류' 정도는 vote-label에 일시 표시하거나 alert 한 줄).
  성공 시 기존 토글 UI 갱신 로직 그대로 유지.
  수정 (b): checkAuth()의 동일 쿠키 스니핑(447행)도 동일 버그 —
  로그인 사용자에게 리뷰 쓰기 버튼이 계속 숨겨짐. 신규 GET /api/auth/me
  엔드포인트 추가 (vote.ts GET 패턴 재사용: cookies session →
  verifySession(sessionSecret) → {loggedIn:true,email} 또는 {loggedIn:false},
  항상 200). checkAuth는 이 엔드포인트를 fetch해 loggedIn일 때만
  review-write-btn 표시. loadVoteData/loadReviewData는 그대로 유지.
  주의: fetch는 same-origin 기본 credentials로 쿠키 전송되므로
  credentials 옵션 추가 불필요 — 괜히 credentials:'include' 등 추가 금지.</action>
  <verify><automated>grep -n "document.cookie" src/pages/tools/\[id\].astro → 0건 (대괄호 이스케이프 주의: grep -F "document.cookie" 사용)</automated></verify>
  <done>로그인 사용자 클릭이 POST까지 도달하고, 401일 때만 로그인 리다이렉트하며, 쿠키 스니핑 코드가 파일에 남아있지 않음</done>
</task>

<task type="auto">
  <name>Task 2: 빌드 검증 + 회귀 확인</name>
  <files>src/pages/tools/[id].astro, src/pages/api/auth/me.ts</files>
  <action>npm run build 통과 확인. 회귀 확인: (1) submit 폼(ToolForm.astro)
  무수정 — git diff --name-only에 ToolForm 없음을 확인. (2) vote.ts 서버
  로직 무수정 — 401/200 토글 동작 그대로. (3) 브라우저 수동 확인 절차를
  SUMMARY에 기록: 로그인 후 툴 상세에서 추천 클릭 → 카운트 ±1 및 버튼
  상태 변경, 시크릿 창(비로그인)에서 클릭 → /api/auth/login 이동.
  에이전트가 브라우저 테스트를 수행할 수 없으면 미수행으로 명시하고
  사용자에게 수동 확인 요청.</action>
  <verify><automated>npm run build (통과) + git diff --name-only가 본 plan의 files_modified 2개만 포함</automated></verify>
  <done>빌드 통과, 무관 파일 변경 없음, 수동 확인 절차 SUMMARY에 기록됨</done>
</task>

</tasks>

<verification>
npm run build 통과. grep -F "document.cookie" src/pages/tools/[id].astro 0건.
</verification>

<success_criteria>
로그인 사용자 추천 클릭이 투표로 처리되고 로그인 페이지로 튕기지 않음.
비로그인 클릭은 로그인 페이지로 이동. 빌드 통과.
</success_criteria>

<output>
Create `.planning/quick/260924-08j-vote-login-redirect-fix/260924-08j-SUMMARY.md` when done
</output>
