---
phase: quick-08j-vote-login-redirect-fix
plan: 01
subsystem: tools-vote
tags: [vote, auth, httpOnly-cookie, bugfix]
dependency_graph:
  requires: [VOTE-01]
  provides: [vote-login-redirect-fix]
  affects: [tools-detail-page, review-visibility]
tech_stack:
  added: [src/pages/api/auth/me.ts]
  patterns: [POST-first-then-401-redirect, server-side-auth-check]
key_files:
  created: [src/pages/api/auth/me.ts]
  modified: [src/pages/tools/[id].astro]
decisions: []
metrics:
  duration: ~5min
  completed: 2026-09-24
status: complete
actuals:
  tokens: 12000
  tasks: 2
  commits: 1
---

# Phase quick-08j Plan 01: Vote Login Redirect Fix Summary

로그인 사용자의 툴 상세 추천 클릭이 로그인 페이지로 튕기던 버그 수정.
원인: session 쿠키 httpOnly → 클라이언트 쿠키 스니핑 항상 false.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | vote 핸들러 쿠키 스니핑 제거 + /api/auth/me 신규 | e7f2b609 | src/pages/tools/[id].astro, src/pages/api/auth/me.ts |
| 2 | 빌드 검증 + 회귀 확인 (검증 전용, 코드 변경 없음) | — (no code change) | — |

## Changes

- **[id].astro vote 핸들러**: 사전 쿠키 체크 삭제 → 무조건 POST /api/tools/vote → 401일 때만 /api/auth/login 이동. 비-401 실패는 vote-label에 '추천 처리 중 오류' 표시 + console.error.
- **[id].astro checkAuth()**: 쿠키 스니핑 → GET /api/auth/me fetch로 교체, loggedIn일 때만 review-write-btn 표시.
- **신규 GET /api/auth/me**: vote.ts GET 패턴 재사용 (cookies session → verifySession → {loggedIn:true,email} 또는 {loggedIn:false}, 항상 200).

## Verification

- [검증됨] 쿠키 스니핑 제거. 근거: grep -F "document.cookie" src/pages/tools/[id].astro 결과 0건.
- [검증됨] 빌드 통과. 근거: npm run build → "[build] Complete!" (2.09s static + 8.37s server).
- [검증됨] 회귀 없음. 근거: git diff --name-only HEAD~1 HEAD = 2개 파일만 ([id].astro, me.ts). ToolForm.astro·vote.ts 무수정.
- [검증불가] 브라우저 수동 확인 미수행 (에이전트 브라우저 테스트 불가). 복구 계획: 아래 수동 절차를 사용자가 수행.

## 수동 확인 절차 (사용자용)

1. 로그인 후 툴 상세 페이지에서 추천 클릭 → 카운트 ±1 및 버튼 상태 변경 (추천↔추천함), 로그인 페이지로 이동하지 않음.
2. 시크릿 창(비로그인)에서 추천 클릭 → /api/auth/login으로 이동.
3. 로그인 사용자에게 리뷰 쓰기 버튼(✍️ 리뷰 쓰기) 표시 확인.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

없음.

## Residual Risks

- 잔존 위험: 없음이 확인된 것은 아님 — 수동 브라우저 확인이 남아 있음 (위 절차 참조). 서버 401 분기는 vote.ts 기존 동작 그대로이므로 위험 낮음.
